interface:
  display_name: "Review Contracts"
  short_description: "Audit contracts and their derived bead"
  default_prompt: "Use $review-contracts to audit and promote the selected draft contracts as bounded reusable understanding, then review any directly derived bead."

policy:
  allow_implicit_invocation: false
