interface:
  display_name: "Contract Task Prep"
  short_description: "Research specification contracts and prepare an implementation bead"
  default_prompt: "Prepare this implementation task by creating or updating compact specification contracts and deriving or refining the bead. Stop before implementation."

policy:
  allow_implicit_invocation: false
