{"id":"ms-0ha","title":"Implement reaction-scoped pulse foundation with Merge","description":"## Objective\n\nImplement Premium Foundation Roadmap item 24 as one bounded vertical slice: introduce reaction-scoped Pulse transport through the established public authoring, validation, dependency, compilation, evaluation, input-artifact, machine, and transaction layers, using Merge as the first pulse primitive.\n\n## Contract basis\n\n### Reviewed contracts governing this slice\n\n- `mossignal.runtime.reaction_scoped_pulse_foundation`\n  - `docs/specs/contracts/reaction-scoped-pulse-foundation.yaml`\n  - status: reviewed\n  - role: governs pulse batch semantics, typed endpoints, pulse observations in exact-bound inputs, reaction storage and fan-out, initialization and ready transactions, Pulsed events, checked overflow, grouped provenance, verification, and the pre-stability fingerprint extension boundary.\n- `mossignal.node.pulse_merge`\n  - `docs/specs/contracts/pulse-merge.yaml`\n  - status: reviewed\n  - role: governs the first variadic pulse primitive, its checked summation law, stable port multiplicity, authoring, causality, compilation, evaluation, diagnostics, provenance, and conformance.\n\n### Reviewed contracts reused unchanged where their recorded evidence remains unchanged\n\n- `mossignal.graph.authored_network_definition`, `mossignal.authoring.typed_network_builder`, `mossignal.diagnostics.artifact_reports`, and `mossignal.diagnostics.collections`\n  - role: established canonical authored graph, typed builder ownership, artifact suppression, and deterministic diagnostic collection boundaries.\n- `mossignal.graph.current_reaction_dependency_graph`, `mossignal.graph.current_reaction_cycle_validation`, `mossignal.compile.compiled_network`, and `mossignal.runtime.restricted_full_topological_evaluator`\n  - role: established dependency, acyclicity, compilation, and full-reference settlement patterns extended by pulse incidences.\n- `mossignal.runtime.machine_lifecycle`, `mossignal.runtime.initialization_transaction`, `mossignal.runtime.ready_level_transaction`, and `mossignal.runtime.policy`\n  - role: established lifecycle, atomic publication, exact transaction binding, provenance ownership, and budget patterns retained while adding reaction-scoped pulse facts.\n- `mossignal.runtime.level_input_snapshot`, `mossignal.runtime.level_input_delta`, `mossignal.identity.input_schema_fingerprint`, and `mossignal.identity.network_fingerprint`\n  - role: established exact-bound input artifact and semantic identity boundaries extended to typed pulse endpoints under the explicit pre-stability projection policy.\n\nThe two pulse contracts have completed independent review at their recorded evidence baselines. No changed reviewed-contract rule is being silently reused as pulse authority.\n\n## Included scope\n\n- Extend typed and dynamic authored definitions with external Pulse inputs and outputs, stable pulse ports and connections, and the closed Merge node kind.\n- Add convenience and exact keyed builder forms for pulse endpoints and Merge, preserving builder ownership, stable variadic port identity, and immediate duplicate-key rejection where determinable.\n- Extend structural validation, duplicate-source and empty/unary advisories, reaction dependencies, cycle validation, compilation descriptors, invariant checks, and full topological settlement for Pulse and Merge.\n- Extend exact-bound InputSnapshot and InputDelta builders with typed pulse observations; absent pulse inputs mean zero and never require establishment.\n- Add reaction-scoped pulse counts to initialization and ready-machine execution without retaining them as current machine state after a reaction.\n- Publish deterministic Pulsed output events with exact count, time, revision, and result-resolvable grouped provenance, including initialization pulses.\n- Reject checked Merge overflow atomically as `runtime.pulse_count_overflow`.\n- Extend the pre-stability network and input-schema fingerprint projections to represent Pulse signal kinds, pulse endpoints and ports, and Merge; synchronize the authoritative exact projections, applicable contracts, and affected golden vectors in the accepted change.\n- Add permanent focused pulse-foundation and Merge conformance verification.\n\n## Explicit exclusions and non-goals\n\n- No Coalesce, Zip, PulseGate, PulseSelect, PulseRoute, edge detector, Toggle, latch, sample-hold, stateful, or temporal primitive.\n- No previous or proposed state, persistent pulse value, pending event calendar, schedule, temporal deadline, or topology patch.\n- No module, standard catalogue, binding projector, persistence artifact decoder, replay, snapshot restoration, or reconfiguration implementation.\n- No incremental dirty propagation, optimized sparse evaluator, retained pulse history, subscription delivery, or complete explanation rendering.\n- Do not change PulseCount representation or add wrapping, saturating, or infallible arithmetic.\n- Do not add callbacks, trait-object evaluator extension, implicit Pulse and Level conversion, or a splitter primitive.\n\n## Implementation obligations\n\n- Implement `reaction-scoped-simultaneous-count`, `checked-fixed-width-count`, `typed-pulse-boundaries`, `exact-bound-pulse-inputs`, `fanout-and-reaction-settlement`, `lifecycle-transaction-integration`, `external-pulse-publication`, `overflow-is-atomic-rejection`, `grouped-pulse-provenance`, `pre-stability-pulse-identity-extension`, and `pulse-foundation-verification` from the pulse-foundation contract.\n- Implement every requirement of `mossignal.node.pulse_merge`.\n- Reuse the existing authoritative graph, validation report, dependency graph, compiler, machine lifecycle, transaction, runtime-policy, and provenance paths; do not create parallel pulse-only lifecycle or execution architectures.\n- Preserve the exact existing level behavior and level-only transaction observations while widening the same input artifacts and reaction executor to pulse batches.\n- Keep each stable pulse incidence distinct through authoring, validation, canonical identity, compilation, evaluation, and grouped provenance.\n- Treat pulse count zero as semantic absence for the reaction while retaining duplicate-observation and schema-validation guarantees during input construction.\n\n## Allowed implementation freedom\n\n- Choose the simplest deterministic private pulse slot, batch, descriptor, evaluator, and grouped-provenance representations.\n- Choose and test one deterministic locally allocated Merge port order and record it at close-out; exact keyed forms remain available for durable independent reconstruction.\n- Either retain explicit zero pulse observations or normalize them after validation, provided semantic results and duplicate-observation behavior are unchanged.\n- Choose the simplest exact pre-stability canonical extension consistent with the existing record and tagged-variant style, then synchronize its specification text, applicable contract representation, and golden vectors in the accepted change. No historical reader or migration is required before stabilization.\n- Extend existing failure evidence minimally when `runtime.pulse_count_overflow` remains structured and identifies the failed pulse arithmetic sufficiently.\n\n## Expected change surface\n\n- Signal-aware authored and builder endpoint/node families.\n- Structural validation, reaction dependency extraction, cycle integration, compilation, full evaluation, machine storage, transactions, provenance, and output events.\n- InputSnapshot and InputDelta pulse construction and schema membership.\n- Pre-stability network and input-schema fingerprint projections and golden vectors.\n- Focused unit, integration, and conformance tests.\n- The two reviewed pulse contracts only when implementation discovers reusable specification-backed corrections or records the accepted exact projection; such changes still require independent review before acceptance.\n- Synchronized bead state at implementation acceptance.\n\n## Required verification\n\n1. Build pulse input to Merge to pulse output networks through both typed and dynamic public authoring paths and compare validation, compilation, fingerprints, initialization, and later transactions.\n2. Cover Merge empty, unary, multi-input, permutation, duplicate-source, and fan-out cases with counts zero, one, two, and a larger representable value.\n3. Verify initialization and ready pulse batches, omission as zero, explicit zero behavior, input insertion-order invariance, same-time event ordering, exact Pulsed counts, and no pulse persistence into later reactions.\n4. Verify pulse and level endpoints remain kind-safe; unknown, duplicate, wrong-kind, wrong-network, foreign-schema, and stale-schema input failures preserve existing structured categories.\n5. Verify Merge dependencies participate in cycle rejection and deterministic topological compilation with stable subject correspondence.\n6. Verify grouped provenance retains each contributing port and count without invented order and every event CauseRef resolves through its result.\n7. Test checked arithmetic at maximum representable boundaries; overflow rejects the complete transaction with `runtime.pulse_count_overflow` and a complete before/after machine comparison.\n8. Verify same-topology insertion-order fingerprint invariance, sensitivity to Pulse versus Level schema and structure, exact Merge representation, and updated hand-reviewed golden bytes and digests.\n9. Run narrow focused tests while iterating, then `make check-dev` and `make check-final`.\n\n## Blocking questions\n\nNone. The exact pre-stability projection spelling and private pulse representation are bounded deterministic implementation choices, not frozen compatibility or missing semantics.\n\n## Readiness\n\nReady for implementation against the two reviewed pulse contracts and reused reviewed foundations. This bounded bead has completed contract review; no product or design blocker prevents implementation progress.","notes":"Implemented the bounded Pulse foundation and Merge vertical slice across typed/dynamic authoring, validation and cycle dependencies, deterministic compilation/evaluation, exact-bound pulse snapshots/deltas, initialization and ready transactions, Pulsed events, grouped port/count provenance, and atomic checked overflow. Locally allocated Merge input ports follow immediate IntoIterator order; exact-keyed ports remain reconstruction authority. Explicit zero observations are retained through input construction for duplicate/conflict validation and evaluate semantically as reaction-local zero without machine persistence. Extended the pre-stability version-1 canonical projection with merge, pulse, and reaction_scoped tags, synchronized authoritative spec text, reviewed affected contract records, and hand-reviewed golden bytes/digests. Added typed/dynamic equivalence, cycle, arity, multiplicity, fanout, lifecycle, schema-failure, provenance, ordering, fingerprint, and overflow tests. Corrective acceptance review rejected cross-kind external-output/source definitions during structural validation and strengthened maximum-count, complete failure-atomicity, permutation, advisory, mixed-event-order, and input-order coverage. make check-dev and make check-final pass. Affected contract edits completed independent evidence review.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-28T12:16:30.185647Z","created_by":"mikkofinell","updated_at":"2026-07-28T14:08:09.756773Z","closed_at":"2026-07-28T12:47:23.364792Z","close_reason":"Implemented reaction-scoped Pulse transport and variadic Merge with synchronized specifications, focused conformance coverage, and passing final gate","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["pulse","roadmap","runtime"]}
{"id":"ms-1ou","title":"Expand the standard catalogue with AtMost and AllEqual","description":"## Objective\n\nImplement Premium Continuation Roadmap item 38 by extending the accepted standard catalogue with canonical stateless `AtMost` and `AllEqual` descriptors, construction paths, inspection, diagnostics, identity, and exhaustive conformance while preserving `Exactly` and the ordinary module execution boundary.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 38\n- Title: Stateless standard-module expansion\n\nThis provenance records task sequencing only. Authoritative specifications and the referenced contracts govern semantics.\n\n## Contract basis\n\n### Reviewed feature contract accepted for reuse\n\n- `mossignal.standard.stateless_expansion`\n  - `docs/specs/contracts/standard-stateless-expansion.yaml`\n  - status: reviewed\n  - role: governs AtMost and AllEqual identity, interfaces, total laws, dependencies, exact expansions, typed construction, diagnostics, inspection, explanation, and exhaustive conformance.\n\n### Reviewed contracts accepted for reuse without modification\n\n- `mossignal.standard.catalogue_foundation` (`docs/specs/contracts/standard-catalogue-foundation.yaml`): catalogue identity and discovery, declarations, canonical keys and fingerprints, dynamic construction, ordinary instantiation, diagnostics, inspection, and verification hooks.\n- `mossignal.standard.exactly` (`docs/specs/contracts/standard-exactly.yaml`): accepted first-descriptor pattern and regression boundary; Exactly semantics remain unchanged.\n- `mossignal.node.level_combinational_expansion` (`docs/specs/contracts/level-combinational-expansion.yaml`) and `mossignal.node.variadic_all` (`docs/specs/contracts/variadic-all.yaml`): Constant, Not, Any, All, and AtLeast semantics, typed ports, stable variadic multiplicity, validation, compilation, and settlement used by the canonical expansions. Only unchanged primitive-law and execution facets are reused; changed historical fingerprint evidence is not needed here.\n- `mossignal.module.instantiation_hierarchy` (`docs/specs/contracts/module-instantiation-hierarchy.yaml`) and `mossignal.module.aware_execution` (`docs/specs/contracts/module-aware-execution.yaml`): generic typed instantiation, hierarchy, lowering, runtime behavior, provenance, and observation.\n- `mossignal.identity.module_fingerprint` (`docs/specs/contracts/module-fingerprint.yaml`) and `mossignal.identity.network_fingerprint` (`docs/specs/contracts/network-fingerprint.yaml`): semantic definition and containing-network identity; standard declaration projection remains governed by the catalogue foundation.\n- `mossignal.diagnostics.problem_model`, `mossignal.diagnostics.subject_references`, `mossignal.diagnostics.collections`, and `mossignal.diagnostics.artifact_reports`: structured evidence, stable subjects, deterministic coexistence and merging, and blocking artifact omission. Existing standard diagnostic categories are reused without contract changes.\n\n## Included scope\n\n- Add exact AtMost and AllEqual descriptors to canonical catalogue discovery order with their provisional semantic and expansion version 1 identities, fixed result roles, variadic Level interfaces, and threshold schema only for AtMost.\n- Generalize catalogue-backed dynamic validation and generation across the three implemented descriptors without weakening malformed-request retention, deterministic finding collection, or LibraryDefect handling.\n- Generate the exact canonical AtMost and AllEqual primitive case tables with deterministic stable keys, roles, expansion fingerprints, dependency signatures, and standard-origin module fingerprints.\n- Add concise and explicit-keyed AtMost and AllEqual constructors to NetworkBuilder and ModuleBuilder, retaining equivalence with dynamic catalogue construction and generic ModuleDef instantiation.\n- Add module-level diagnostics with exact empty, unary, constant-result, and duplicate-source coexistence and suppression behavior; prevent redundant primitive and unused-input warnings created solely by canonical implementation structure.\n- Extend structured module inspection and explanation with AtMost allowance or excess facts and AllEqual common-value or mixed-group facts while preserving exact declaration, hierarchy, internal roles, and primitive drill-down.\n- Execute both modules solely through existing generic module compilation, primitive evaluation, transactions, budgets, provenance, and inspection.\n- Add deterministic descriptor and expansion checks plus exhaustive bounded-domain conformance for both modules, including preservation of existing Exactly behavior.\n\n## Exclusions and non-goals\n\n- No stateful or temporal standard module, including resettable toggles or sample-hold.\n- No new primitive node kind, privileged evaluator, callback, hidden simplification, module-specific transaction, hidden state, or pending work.\n- No primitive aliases, builder-only conveniences, metadata-only conveniences, or application binding layer.\n- No persistence envelope or decoder, restoration, snapshot/replay integration, topology patch, module replacement, descriptor-version migration, state migration, or internal-edit enforcement.\n- No complete generic prose renderer, generated documentation system, editor palette, query planner, or subscription system.\n\n## Implementation obligations\n\n- Reuse the reviewed `mossignal.standard.stateless_expansion` contract as the module-specific semantic and conformance baseline.\n- Reuse and generalize the existing catalogue, declaration, canonical key derivation, fingerprint, validation, builder, inspection, and ordinary module-runtime paths rather than creating descriptor-specific parallel infrastructure.\n- Preserve the exact specified canonical expansions even when a smaller or behaviorally equivalent primitive graph exists.\n- Keep catalogue and descriptor versions provisional under the repository pre-release policy; synchronize the current inventory and identities without compatibility shims or migration ceremony.\n- Canonicalize variadic public keys by stable identity, preserve duplicate-source multiplicity, and ensure deterministic unique internal keys and roles independent of request insertion order.\n- Keep malformed dynamic requests caller-representable and collect every independent safe finding deterministically; produce a ModuleDef only when blocking diagnostics are absent.\n- Preserve existing Exactly declarations, fingerprints, diagnostics, construction, execution, and inspection except for deliberate catalogue-wide representation generalization that remains semantically identical and tested.\n- Record non-obvious canonical case and derivation invariants with brief SPEC traces; production code remains free of unwrap and expect.\n\n## Allowed implementation freedom\n\n- Choose the smallest deterministic descriptor dispatch and shared helper structure; a compact enum or private table is acceptable when exact public descriptor identity and behavior remain unchanged.\n- Share cardinality counting, variadic canonicalization, diagnostics, inspection, and expansion helpers where module-specific laws and permanent roles remain explicit.\n- Choose compact private structured inspection and explanation value shapes that expose every required fact; complete prose rendering is outside this task.\n- Choose the simplest deterministic larger-arity property strategy supported by current test tooling, and record the concrete choice in tests or close-out.\n\n## Expected change surface\n\n- `crates/mossignal/src/standard.rs` or a small focused standard-module split for descriptors, requests, declarations, canonical expansions, dependencies, inspection, and explanation.\n- `crates/mossignal/src/builder.rs`, `machine.rs`, `validation.rs`, `diagnostics.rs`, and `lib.rs` for typed construction, runtime inspection projection, shared validation, evidence reuse, and deliberate exports.\n- Focused standard-module integration tests plus fingerprint, cycle, hierarchy, provenance, execution, and diagnostics regressions.\n- `docs/premium_continuation_roadmap.md` only for completion marking after accepted implementation.\n- `.beads/` when implementation is later closed and synchronized.\n\n## Required verification\n\n1. Verify canonical catalogue order, exact and latest lookup, descriptor identity, interfaces, parameter schemas, fixed result keys, and discovery determinism for all three descriptors.\n2. Exercise AtMost and AllEqual dynamic request defects, safe coexistence, deterministic evidence ordering and merging, artifact omission, and shipped-descriptor LibraryDefect classification.\n3. Compare concise, explicit-keyed, dynamic plus generic, and direct canonical paths for declarations, public keys, parameters, expansion, roles, dependencies, diagnostics, fingerprints, hierarchy, execution, provenance, and inspection.\n4. Exhaust every Level valuation for arities 0 through at least 5; enumerate AtMost thresholds 0, 1, arity minus one where defined, arity, arity plus one, and u64::MAX using independent reference laws.\n5. Cover every AtMost and AllEqual canonical branch and verify exact primitive kinds and configurations, qualified ports, connections, exports, stable roles, unique derived keys, and absence of implementation-artifact warnings.\n6. Permute stable public input and request order without changing behavior, dependencies, expansion fingerprints, or module fingerprints; verify sensitivity to keys, thresholds, descriptor references, roles, and incidence.\n7. Verify empty, unary, constant-result, and duplicate-source diagnostics with exact suppression and coexistence; constant expansions must not trigger generic unused-input findings.\n8. Verify public dependency signatures equal expanded graph dependencies and preserve cycle detection across module boundaries for both constant and nonconstant declarations.\n9. Instantiate definitions repeatedly and nest both modules inside user modules; verify standard origin, hierarchy, qualified provenance, diagnostics, structured inspection and explanation, and independent machine execution.\n10. Compare canonical module behavior with explicit primitive equivalents after normalizing intentional identity and hierarchy differences; verify ordinary budget enforcement and failure atomicity.\n11. Verify user-origin behaviorally identical modules retain distinct ModuleFingerprint and containing NetworkFingerprint sensitivity to declaration, instance, bindings, and hierarchy.\n12. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Blocking questions\n\nNone. The two public laws, exact expansion tables, dependency signatures, diagnostics, and inspection facts are specified. Private helper decomposition and structured value spelling remain bounded implementation freedom.\n\n## Readiness\n\nReady as a bounded roadmap-item-38 implementation bead. No product or compatibility blocker prevents implementation.","notes":"Implemented AtMost and AllEqual as ordinary canonical ModuleDef expansions over Constant, Not, Any, All, and AtLeast. Generalized descriptor dispatch, dynamic parameter validation, stable result keys, declaration dependencies, module diagnostics, typed NetworkBuilder/ModuleBuilder construction, and runtime inspection without adding evaluator or transaction special cases. AtMost inspection exposes allowance/excess and AllEqual inspection exposes common/mixed groups through compact public enums. Locally allocated variadic keys retain iterator order while canonical declarations sort explicit stable keys. Exhaustive truth tables cover arities 0 through 5 and the required AtMost thresholds; canonical cases, malformed requests, ordering, diagnostics, nesting, identity, and Exactly regressions are covered. make check-dev and make check-final pass.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-29T22:12:34.701908Z","created_by":"mikkofinell","updated_at":"2026-07-29T22:46:20.985625Z","closed_at":"2026-07-29T22:46:20.985576Z","close_reason":"Implemented canonical AtMost and AllEqual catalogue modules with exhaustive bounded conformance and passing final gate.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["catalogue","modules","roadmap"]}
{"id":"ms-244","title":"Implement remaining level-combinational primitives","description":"## Objective\n\nImplement the first half of Premium Foundation Roadmap item 23 by adding Any, Parity, AtLeast, and Select through the established authored graph, typed builder, validation, dependency, compilation, settlement, transaction, and pre-stability fingerprint boundaries.\n\n## Contract basis\n\n- `mossignal.node.level_combinational_expansion`\n  - `docs/specs/contracts/level-combinational-expansion.yaml`\n  - status: reviewed at the accepted evidence baseline\n  - role: the four primitive laws, port identity, authoring, advisories, causality, execution, conformance, and exact fingerprint boundary.\n- `mossignal.identity.network_fingerprint`\n  - `docs/specs/contracts/network-fingerprint.yaml`\n  - status: reviewed at the accepted evidence baseline\n  - role: exact pre-stability v1 node-kind, AtLeast threshold, Select role, ordering, sensitivity, and golden-vector requirements.\n- `mossignal.node.variadic_all`\n  - `docs/specs/contracts/variadic-all.yaml`\n  - status: reviewed, used unchanged\n  - role: established variadic stable-port pattern and implementation boundary.\n- `mossignal.authoring.typed_network_builder` and `mossignal.graph.authored_network_definition`\n  - reviewed, used unchanged\n  - role: typed convenience lowering, explicit stable-key authoring, malformed dynamic claims, and canonical authored storage.\n- `mossignal.graph.structural_validation`, `mossignal.graph.current_reaction_dependency_graph`, `mossignal.graph.current_reaction_cycle_validation`, `mossignal.compile.compiled_network`, and `mossignal.runtime.restricted_full_topological_evaluator`\n  - reviewed contextual contracts, used unchanged for their represented boundaries\n  - role: existing graph, validation, compilation, and full-settlement layers extended by the new family.\n- `mossignal.runtime.initialization_transaction` and `mossignal.runtime.ready_level_transaction`\n  - reviewed contextual contracts, used unchanged for their represented level transaction and output-event facets.\n\nNo known-uncovered facet of these contracts is required for this bead. Explanation rendering and durable reconstruction of locally allocated individual variadic ports remain outside scope.\n\n## Included scope\n\n- Add closed dynamic node kinds and public configuration for Any, Parity, AtLeast, and Select.\n- Add typed convenience construction plus explicit keyed construction, preserving builder ownership checks, stable ports, and deterministic local allocation.\n- Validate each fixed or variadic port shape and emit applicable non-blocking empty, unary, duplicate-source, and constant-result findings without suppressing valid artifacts.\n- Extend current-reaction dependency extraction, cycle validation, compilation, full evaluation, initialization, and ready transactions for the four laws.\n- Extend the pre-stability v1 fingerprint projection with exact Any, Parity, AtLeast, and Select variants, AtLeast threshold sensitivity, and distinct Select selector/branch roles.\n- Add bounded exhaustive primitive, permutation, simultaneous-change, typed-versus-dynamic, lifecycle, and golden-vector verification.\n\n## Exclusions and non-goals\n\n- No half-adder, full-adder, ripple-carry, or reusable cross-topology equivalence harness; that is the dependent child bead.\n- No pulse, stateful, temporal, standard-module, persistence-decoding, topology-reconfiguration, provenance, or explanation work.\n- Do not replace the established authored graph, report model, dependency graph, compiler, evaluator, or transaction architecture.\n- Do not add a successor fingerprint domain, historical reader, upgrader, or migration path.\n\n## Implementation obligations\n\n- Honor `any-total-law`, `parity-total-law`, `at-least-total-law`, `select-branch-law`, `variadic-port-identity-and-multiplicity`, `typed-and-dynamic-authoring`, `causality-and-settlement`, `nonblocking-quality-findings`, `primitive-conformance`, and `canonical-fingerprint-boundary`.\n- Honor `canonical-restricted-projection`, `pre-stability-projection-policy`, `parameter-and-role-sensitivity`, and `golden-vector-verification` from the fingerprint contract.\n- Preserve port-based multiplicity for repeated sources and retain all three Select inputs in the static dependency graph regardless of the currently selected branch.\n- Keep direct dynamic construction capable of retaining malformed definitions for structured validation.\n- Extend existing restricted layer types mechanically; do not create parallel validation, compilation, evaluation, or transaction paths.\n\n## Allowed implementation freedom\n\n- Choose the simplest deterministic private descriptor layout, Boolean reduction/counting strategy, evaluator dispatch, and local port-allocation order.\n- Choose exact explicit-keyed constructor grouping consistent with the established builder and dynamic-authoring contracts.\n- Record and test concrete deterministic choices without promoting them to reusable product policy.\n\n## Expected change surface\n\n- Authored node/configuration types, typed builder, structural validation and diagnostics\n- Reaction dependency extraction, cycle integration, compilation descriptors, full evaluation, and level transaction tests\n- Canonical fingerprint encoding and golden vectors\n- Focused conformance and regression tests\n- Synchronized bead state at implementation acceptance\n\n## Required verification\n\n1. Exhaust bounded arities and valuations for Any, Parity, and AtLeast, including empty, unary, threshold zero, threshold equal to arity, and threshold greater than arity.\n2. Exhaust all Select valuations and simultaneous selector/branch changes; prove both branches remain static dependencies while only the selected branch determines the settled output.\n3. Verify duplicate-source port multiplicity and diagnostics, commutative port-permutation behavior with stable identities retained, and deterministic convenience allocation.\n4. Verify typed and dynamic equivalent definitions validate, compile, fingerprint, initialize, and advance with equivalent observable level behavior.\n5. Verify all new kinds participate correctly in cycle rejection and deterministic topological compilation.\n6. Verify exact v1 canonical variants, AtLeast threshold sensitivity, Select role sensitivity, insertion-order invariance, and hand-reviewed golden bytes/digests.\n7. Run narrow relevant tests, then `make check-dev` and `make check-final`.\n\n## Blocking questions\n\nNone in the proposed contract set.\n\n## Readiness\n\nNo product or design blocker remains. The bounded bead is ready for implementation against the reviewed contract and authoritative specification baseline.","notes":"Implemented Any, Parity, AtLeast, and Select through authored definitions, typed and explicit-key builders, structured validation/advisories, dependency and cycle analysis, compilation, full evaluation, transactions, and pre-stability v1 identity. Added exhaustive small-domain laws, Select valuation and simultaneous-change coverage, duplicate-source multiplicity and permutation checks, typed/dynamic equivalence, deterministic allocation, cycle integration, role/parameter sensitivity, and canonical golden vectors. Verification: cargo test -p mossignal --all-targets (143 passed); make check-dev; make check-final.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T10:47:19.982985Z","created_by":"mikkofinell","updated_at":"2026-07-28T11:15:05.703922Z","closed_at":"2026-07-28T11:15:05.703866Z","close_reason":"Implemented and accepted; ms-244.1 remains open as the explicitly excluded dependent follow-up. All focused, development, and final verification passed.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["level-combinational","roadmap"]}
{"id":"ms-244.1","title":"Add composed level-circuit equivalence suite","description":"## Objective\n\nComplete the verification half of Premium Foundation Roadmap item 23 by adding a reusable public-API equivalence harness and exhaustive composed-adder tests over the accepted level-combinational primitives.\n\n## Existing contract basis\n\n- `mossignal.identity.network_fingerprint`\n  - `docs/specs/contracts/network-fingerprint.yaml`\n  - reviewed; governs same-topology insertion-order fingerprint invariance.\n- `mossignal.authoring.typed_network_builder`\n  - `docs/specs/contracts/typed-network-builder.yaml`\n  - reviewed; governs public construction and stable keyed authoring.\n- `mossignal.runtime.initialization_transaction` and `mossignal.runtime.ready_level_transaction`\n  - `docs/specs/contracts/initialization-transaction.yaml`\n  - `docs/specs/contracts/ready-level-transaction.yaml`\n  - reviewed; govern lifecycle, transaction results, ordered output events, and settled outputs.\n- `mossignal.node.variadic_all` and `mossignal.node.level_combinational_expansion`\n  - `docs/specs/contracts/variadic-all.yaml`\n  - `docs/specs/contracts/level-combinational-expansion.yaml`\n  - reviewed; govern the accepted primitive laws used by the composed circuits.\n\nVerification scope and depth follow section 39.1 of `docs/testing_and_verification_policy.md`. This task does not require a new specification contract.\n\n## Included scope\n\n- Add reusable test helpers that construct stable-keyed circuits through public authoring APIs, validate and compile them, drive initialization and later level transactions, and compare deterministic normalized observations.\n- Compare materially different construction orders of one stable-keyed semantic topology for equal fingerprints and equivalent observable behavior.\n- Compare structurally different but logically equivalent networks for behavior only, without requiring equal fingerprints.\n- Exhaust half adders, multiple full-adder constructions, and a four-bit ripple-carry adder over their complete finite domains.\n- Compare initialization outcomes, deterministic later-transaction sequences, ordered observable output events, and final settled external outputs.\n\n## Exclusions and non-goals\n\n- No production semantics or API changes are expected.\n- No private evaluator, compiler, or authored-storage test hooks.\n- No persistence fixture framework, benchmark, property-test framework, or generalized formal solver.\n- No pulse, stateful, temporal, module, topology-patch, replay, provenance-rendering, or explanation-equivalence suite.\n- Do not require equal fingerprints for structurally different logical implementations.\n\n## Implementation obligations\n\n- Use only public authoring and runtime APIs for the equivalence harness.\n- Same-topology variants retain identical stable semantic keys while varying available public construction order enough to exercise canonicalization.\n- Cross-topology comparisons normalize only permitted representation differences; stable external identity, logical time, revision, event semantics, and settled outputs remain observable.\n- Keep finite-domain enumeration, transaction driving, and normalized comparison machinery reusable across every required circuit family.\n\n## Implementation freedom\n\n- Choose the simplest deterministic test-module placement, fixture structures, input-vector representation, circuit adapter shape, construction-order variants, and normalized comparison representation.\n- Share deterministic stable-key fixtures where this does not erase the distinction between same-topology and cross-topology comparisons.\n- Use ordinary finite loops and record concrete helper choices in tests or close-out.\n\n## Expected change surface\n\n- Integration or conformance test modules and reusable test-support code.\n- No production source changes expected.\n- Synchronized bead state at implementation acceptance.\n\n## Required verification\n\n1. Exhaust every half-adder input pair for canonical primitives and a structurally different equivalent composition.\n2. Exhaust every full-adder input triple across direct canonical primitives, conventional gate composition, and NAND-only composition through `Not(All(...))`.\n3. Exhaust the four-bit ripple-carry adder across both carry-in levels and all 16-by-16 operand pairs.\n4. For every required family, compare initialization outcomes, a deterministic sequence of later level transactions, ordered output events, and final settled outputs.\n5. Rebuild one stable-keyed topology in materially different public construction orders and prove fingerprint and behavioral invariance.\n6. Demonstrate that structurally different logical implementations are compared without asserting fingerprint equality.\n7. Reuse the harness across circuit families and leave it suitable for later test modules.\n8. Run focused tests, then `make check-dev` and `make check-final`.\n\n## Dependencies\n\n- `ms-244` is closed and accepted.\n\n## Blocking questions\n\nNone.\n\n## Readiness\n\nReady for implementation. No contract review or additional specification work is required.","notes":"Implemented a reusable public-API equivalence harness that normalizes transaction time, revisions, ordered level-event semantics, and settled external outputs while treating result-scoped cause handles as representation-local. Added exhaustive half-adder coverage across direct and conventional compositions; full-adder coverage across direct canonical primitives, conventional gates, and NAND-only Not(All(...)) composition; stable-keyed endpoint insertion-order fingerprint invariance; and all 512 four-bit ripple-carry valuations. Verification: cargo test -p mossignal --test composed_level_equivalence (4 passed); make check-dev; make check-final.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T10:47:46.556712Z","created_by":"mikkofinell","updated_at":"2026-07-28T11:46:51.574245Z","closed_at":"2026-07-28T11:46:37.305071Z","close_reason":"Implemented reusable public-API composed level-circuit equivalence harness and exhaustive half-adder, full-adder, and four-bit ripple-adder coverage.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["level-combinational","roadmap","verification"],"dependencies":[{"issue_id":"ms-244.1","depends_on_id":"ms-244","type":"blocks","created_at":"2026-07-28T10:48:09.596549Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-2a7","title":"mossignal Implementation Contract: Diagnostic Metadata Values","description":"# `mossignal` Implementation Contract: Diagnostic Metadata Values\n\n**Status:** Ready for implementation-task transfer\n**Repository:** `Mikko-Finell/mossignal`\n**Specification baseline:** `24c0d4db3673915ca5c66a123ec781b7ccd374bb`\n**Implementation baseline:** `24c0d4db3673915ca5c66a123ec781b7ccd374bb`\n**Task position:** Premium Foundation Roadmap, Part I, Task 6\n\n---\n\n## 1. Objective\n\nImplement the complete initial owned diagnostic-metadata value foundation for `mossignal`.\n\nThe accepted implementation must establish:\n\n* `DiagnosticMeta`;\n* `DiagnosticPath`;\n* `OriginRef`;\n* owned names and descriptions;\n* owned ordered path segments;\n* owned origin information;\n* owned tags;\n* structural equality;\n* deep cloning;\n* default empty metadata;\n* exact preservation of caller-supplied text;\n* focused ownership and value-semantics tests.\n\nThese values describe authored semantic subjects for human-readable inspection, navigation, source correlation, diagnostics, and tooling.\n\nThey do not identify subjects and do not constitute the diagnostic reporting system.\n\nThe implementation must preserve this distinction:\n\n```text\nstable structural key\n    = authoritative semantic identity\n\ndiagnostic metadata\n    = optional descriptive information attached to that identity\n```\n\nChanging a name, description, path, origin, or tag must not imply that the described subject became a different semantic subject.\n\nThis task establishes metadata values only. It must not begin authored graph definitions, validation findings, diagnostic codes, reports, runtime diagnostics, provenance, rendering, persistence encoding, or reconfiguration.\n\n---\n\n## 2. Source of truth\n\nThe repository specifications under `docs/specs/` are authoritative.\n\nThis contract is derived from the following sections.\n\n### Primary concrete API authority\n\n* `docs/specs/concrete_rust_api_surface.md`\n\n  * § **Owned semantic artifacts**\n  * § **Public module organization**\n  * § **Diagnostic metadata**\n  * § **Metadata inputs**\n\nThis specification governs:\n\n* the public `metadata` module;\n* the non-generic `DiagnosticMeta` shape;\n* the exact initial standardized owned `OriginRef` family and constructors;\n* the exact public `DiagnosticPath` API and observable value semantics;\n* the requirement that metadata be owned;\n* support for non-`'static` caller text;\n* the prohibition against making runtime types generic over arbitrary metadata payloads;\n* the rule that metadata must not affect execution or semantic fingerprints.\n\n### Primary semantic authority\n\n* `docs/specs/api_and_semantics_spec.md`\n\n  * § **Scope and non-goals**\n  * § **Diagnostic metadata**\n  * § **Modules and hierarchy**\n\nThis specification establishes that:\n\n* human-readable identity is separate from structural identity and bindings;\n* metadata does not affect execution, state compatibility, or semantic fingerprints;\n* diagnostic paths may express hierarchy;\n* paths remain metadata rather than structural identity;\n* behavior must not be derived from names or diagnostic metadata.\n\nThe generic `DiagnosticMeta<Origin>` shown there is explicitly a semantic shape rather than a mandated concrete representation. The concrete Rust API specification governs the final non-generic public type.\n\n### Reconfiguration-adjacent authority\n\n* `docs/specs/reconfiguration_and_topology_patch_spec.md`\n\n  * § **Metadata operations**\n  * § **Current diagnostic metadata and paths**\n\nThese sections establish that metadata and rendered paths may change while semantic identity and runtime state remain preserved.\n\nTopology-patch values and metadata replacement operations remain excluded from this task.\n\n### Persistence-adjacent authority\n\n* `docs/specs/persistence_canonical_encoding_and_compatibility_spec.md`\n\n  * § **Sequences**\n  * § **Text**\n  * § **Network fingerprint**\n  * § **Metadata**\n  * § **Order sensitivity**\n\nThese sections establish that:\n\n* diagnostic paths are semantically ordered values whose hierarchical segment order must remain order-sensitive;\n* metadata text is retained byte-for-byte;\n* metadata is excluded from semantic network and module fingerprints;\n* metadata must not undergo Unicode normalization, case folding, whitespace normalization, or line-ending conversion.\n\nCanonical encoding and persistence artifacts remain excluded from this task.\n\n### Roadmap authority\n\n* `docs/premium_roadmap.md`\n\n  * § **Diagnostic metadata values**\n\nThe roadmap limits this task to the owned metadata values required by later authored definitions.\n\nIf this contract conflicts with an authoritative repository specification at the stated baseline, the specification governs and the conflict must be surfaced rather than silently resolved.\n\n### Authority check\n\n* **Exact public shape:** the concrete Rust API specification's **Diagnostic metadata** section defines `DiagnosticMeta`, `DiagnosticPath`, and the exact initial `OriginRef` family.\n* **Value and equality laws:** that section defines structural field equality, ordered path segments, exact tag sequences, empty-value distinctions, and opaque origin coordinates.\n* **Persistence consequences:** the persistence specification's **Sequences**, **Text**, **Metadata**, and **Order sensitivity** sections require exact text preservation and order-sensitive diagnostic paths.\n* **Identity and fingerprint consequences:** the API, concrete API, reconfiguration, and persistence specifications exclude diagnostic metadata from structural identity, execution, state compatibility, and semantic fingerprints.\n* **Bead-only public API or semantic decisions:** none.\n\n---\n\n## 3. Dependency boundary\n\nThis task is conceptually independent from signal values, logical-time values, and stable structural keys.\n\nThe implementation may coexist with the already implemented modules:\n\n```text\nmossignal::signal\nmossignal::time\nmossignal::key\n```\n\nThe metadata implementation must not alter their semantics, representations, or public guarantees.\n\nThe `metadata` module must not require:\n\n* `SignalType`;\n* logical-time domain parameters;\n* stable structural keys;\n* graph definitions;\n* diagnostic codes;\n* provenance references;\n* persistence contexts.\n\nMetadata values are ordinary owned presentation values and should remain independently usable.\n\n---\n\n## 4. Scope boundary\n\n### Included\n\n* one public `metadata` module;\n* crate integration through `lib.rs`;\n* `DiagnosticMeta`;\n* `DiagnosticPath`;\n* `OriginRef`;\n* owned `String` values;\n* owned `Vec<String>` collections;\n* default construction where specified;\n* structural equality;\n* deep cloning;\n* debug representation;\n* exact preservation of supplied strings;\n* construction from borrowed or owned path and origin text;\n* focused unit tests;\n* focused public API documentation.\n\n### Explicitly excluded\n\n* graph storage;\n* node definitions;\n* port definitions;\n* connection definitions;\n* external endpoint definitions;\n* module definitions;\n* `UncheckedNetwork`;\n* `ValidatedNetwork`;\n* `CompiledNetwork`;\n* `NetworkBuilder`;\n* metadata attachment APIs;\n* metadata lookup by stable key;\n* metadata inheritance;\n* automatic path derivation;\n* module-hierarchy traversal;\n* topology patches;\n* `SetDiagnosticMeta`;\n* metadata migration;\n* stable correspondence;\n* validation findings;\n* diagnostic codes;\n* severity;\n* responsibility;\n* evidence values;\n* suggestions;\n* `Problem`;\n* `Diagnostic`;\n* `DiagnosticSet`;\n* `Report`;\n* operation failures;\n* runtime diagnostic occurrences;\n* persistent diagnostic episodes;\n* internal defects;\n* diagnostic subject references;\n* `SubjectRef`;\n* rendering;\n* localization;\n* `Display` as a diagnostic-rendering system;\n* source-file loading;\n* URI resolution;\n* filesystem access;\n* source registries;\n* provenance;\n* `CauseRef`;\n* causal source locations;\n* fingerprints;\n* digests;\n* serialization;\n* deserialization;\n* canonical encoding;\n* persistence artifacts;\n* arbitrary caller-defined metadata payloads;\n* generic `DiagnosticMeta<Origin>`;\n* metadata interning;\n* global metadata registries;\n* `Arc<str>` or other shared-string optimization;\n* metadata query languages;\n* tag indexing;\n* tag normalization;\n* tag-set abstractions;\n* new dependencies;\n* prelude changes;\n* crate-root re-exports;\n* `no_std` policy changes.\n\n---\n\n## 5. Required public module surface\n\nThe implementation must expose:\n\n```text\nmossignal::metadata::DiagnosticMeta\nmossignal::metadata::DiagnosticPath\nmossignal::metadata::OriginRef\n```\n\nThe expected source shape is:\n\n```text\ncrates/mossignal/src/lib.rs\ncrates/mossignal/src/metadata.rs\n```\n\n`lib.rs` must publicly declare:\n\n```rust\npub mod metadata;\n```\n\nDo not create empty future modules.\n\nDo not add these types to a crate prelude or re-export them from the crate root.\n\n---\n\n## 6. Metadata semantic boundary\n\n## O1. Metadata is descriptive, not authoritative identity\n\n`DiagnosticMeta`, `DiagnosticPath`, and `OriginRef` must be documented as descriptive values.\n\nThey may help callers and tooling:\n\n* display a readable name;\n* show explanatory text;\n* organize subjects hierarchically;\n* correlate authored subjects with an external source;\n* categorize subjects with tags.\n\nThey must not be described or implemented as:\n\n* stable structural identity;\n* equality between network subjects;\n* graph ownership;\n* module containment;\n* execution semantics;\n* validation status;\n* diagnostic condition identity;\n* provenance;\n* source-of-truth application data.\n\nNo metadata field may invoke, derive, or depend on runtime behavior.\n\nThe implementation must not introduce APIs that interpret metadata to determine:\n\n* signal behavior;\n* node kind;\n* connection incidence;\n* stable correspondence;\n* migration;\n* state compatibility;\n* signal kind;\n* port direction;\n* graph structure.\n\n---\n\n## O2. Metadata does not affect network execution or identity\n\nThe implementation and documentation must preserve the rule:\n\n```text\nMetadata changes do not change signal-network semantics.\n```\n\nThis task does not yet implement semantic fingerprints, state compatibility, compilation, or execution.\n\nNevertheless, the value types and their documentation must not suggest that metadata will participate in those systems.\n\nIn particular:\n\n* names must not imply identity;\n* descriptions must not imply behavior;\n* path segments must not imply actual graph containment;\n* origins must not imply provenance;\n* tags must not imply evaluator categories.\n\nLater systems may retain, inspect, render, encode, or replace these values without treating them as execution semantics. This does not erase their own structural value semantics: path-segment order and tag order remain equality-significant.\n\n---\n\n## 7. `DiagnosticMeta`\n\n## O3. Provide the standard non-generic metadata value\n\nThe public type must be broadly equivalent to:\n\n```rust\n#[derive(Debug, Clone, Default, PartialEq, Eq)]\npub struct DiagnosticMeta {\n    pub name: Option<String>,\n    pub description: Option<String>,\n    pub path: Option<DiagnosticPath>,\n    pub origin: Option<OriginRef>,\n    pub tags: Vec<String>,\n}\n```\n\nThese fields are the complete initial metadata shape.\n\nThe fields must remain owned.\n\nThe public type must not carry:\n\n* lifetimes;\n* borrowed strings;\n* `Cow`;\n* generic origin parameters;\n* generic application payloads;\n* structural keys;\n* diagnostic codes;\n* provenance references.\n\nThe exact field order should follow the concrete API shape above.\n\n### Required trait behavior\n\n`DiagnosticMeta` must implement:\n\n```text\nDebug\nClone\nDefault\nPartialEq\nEq\n```\n\n`Default` must produce:\n\n```rust\nDiagnosticMeta {\n    name: None,\n    description: None,\n    path: None,\n    origin: None,\n    tags: Vec::new(),\n}\n```\n\nDo not add `Copy`.\n\nDo not require `Hash`, `PartialOrd`, or `Ord`.\n\nDo not implement ordering or hashing merely because the contained standard-library types support it.\n\n---\n\n## O4. Preserve optional-field distinctions\n\nThe implementation must preserve the distinction between absence and an explicitly supplied empty value.\n\nFor example:\n\n```rust\nDiagnosticMeta {\n    name: None,\n    ..\n}\n```\n\nmust not equal:\n\n```rust\nDiagnosticMeta {\n    name: Some(String::new()),\n    ..\n}\n```\n\nThe same rule applies to:\n\n* `description`;\n* `path`;\n* `origin`.\n\nIn particular:\n\n```text\npath: None\n```\n\nand:\n\n```text\npath: Some(DiagnosticPath::default())\n```\n\nare distinct values.\n\nNo constructor, accessor, clone, or equality implementation may silently collapse these distinctions.\n\n---\n\n## 8. Owned names and descriptions\n\n## O5. Store names and descriptions exactly\n\nNames and descriptions must use:\n\n```rust\nOption<String>\n```\n\nThe implementation must preserve their contents exactly as supplied.\n\nIt must not:\n\n* trim whitespace;\n* collapse repeated whitespace;\n* alter line endings;\n* reject empty strings;\n* reject multiline descriptions;\n* apply Unicode normalization;\n* apply case folding;\n* interpret markup;\n* localize text;\n* assign semantic meaning to particular strings.\n\nAll valid Rust `String` contents are valid names and descriptions.\n\nThe following must remain observably distinct:\n\n```text\nNone\nSome(\"\")\nSome(\"name\")\nSome(\"Name\")\nSome(\" name \")\nSome(\"name\\n\")\n```\n\nNo validation error type is required or permitted for these fields.\n\n---\n\n## 9. `DiagnosticPath`\n\n## O6. Represent a path as an owned ordered segment sequence\n\n`DiagnosticPath` must represent a hierarchical diagnostic path as an ordered sequence of owned strings.\n\nThe representation must be broadly equivalent to:\n\n```rust\n#[derive(Debug, Clone, Default, PartialEq, Eq)]\npub struct DiagnosticPath {\n    segments: Vec<String>,\n}\n```\n\nThe field must remain private.\n\nA diagnostic path is not:\n\n* an operating-system path;\n* a URI;\n* a slash-separated encoded string;\n* a stable key;\n* a graph path;\n* an ownership relation;\n* an authoritative module hierarchy;\n* a filesystem location;\n* a provenance chain.\n\nExamples of conceptual paths include:\n\n```text\n[\"west wing\", \"portcullis trap\", \"control circuit\"]\n[\"module A\", \"inner latch\"]\n[]\n```\n\nSegment order is part of the value.\n\nRepeated segments are valid.\n\nEmpty segments are valid.\n\nAn empty path is valid.\n\n### Required traits\n\n`DiagnosticPath` must implement:\n\n```text\nDebug\nClone\nDefault\nPartialEq\nEq\n```\n\n`Default` must produce an empty segment sequence.\n\nDo not add `Copy`.\n\nDo not require `Hash`, `PartialOrd`, or `Ord`.\n\n---\n\n## O7. Provide minimal path construction and access\n\n`DiagnosticPath` must provide an API broadly equivalent to:\n\n```rust\nimpl DiagnosticPath {\n    #[must_use]\n    pub const fn new() -> Self;\n\n    pub fn from_segments<I, S>(segments: I) -> Self\n    where\n        I: IntoIterator<Item = S>,\n        S: Into<String>;\n\n    #[must_use]\n    pub fn segments(&self) -> &[String];\n\n    #[must_use]\n    pub fn len(&self) -> usize;\n\n    #[must_use]\n    pub fn is_empty(&self) -> bool;\n\n    #[must_use]\n    pub fn into_segments(self) -> Vec<String>;\n}\n```\n\nThe exact generic parameter names may differ.\n\nRequired behavior:\n\n* `new()` creates an empty path;\n* `from_segments` retains iterator order;\n* every supplied segment is converted exactly once into owned storage;\n* `segments()` returns the complete ordered segment sequence;\n* `len()` returns the number of segments;\n* `is_empty()` reports whether the segment sequence is empty;\n* `into_segments()` returns ownership of the complete stored vector without cloning it.\n\n`from_segments` must accept both borrowed and owned text.\n\nRepresentative valid uses include:\n\n```rust\nlet borrowed = [\"module\", \"inner node\"];\nlet path = DiagnosticPath::from_segments(borrowed);\n\nlet owned = vec![\n    String::from(\"module\"),\n    String::from(\"inner node\"),\n];\nlet path = DiagnosticPath::from_segments(owned);\n```\n\nDo not require `'static` segments.\n\nNo parsing constructor from one separator-delimited string is required.\n\nNo path mutation or normalization API is required.\n\n---\n\n## O8. Preserve path text and order exactly\n\n`DiagnosticPath::from_segments` must not:\n\n* split segments on `/`, `\\`, `.`, `::`, or another delimiter;\n* join adjacent segments;\n* discard empty segments;\n* trim segments;\n* deduplicate repeated segments;\n* sort segments;\n* canonicalize case;\n* perform Unicode normalization;\n* interpret `\".\"` or `\"..\"`;\n* consult filesystem path rules.\n\nThe following paths must remain distinct:\n\n```text\n[\"a\", \"b\"]\n[\"b\", \"a\"]\n[\"a/b\"]\n[\"a\", \"\", \"b\"]\n[\"a\", \"a\"]\n[]\n```\n\nPath equality must compare the exact ordered segment sequence.\n\n---\n\n## 10. `OriginRef`\n\n## O9. Provide the standardized owned origin family\n\n`OriginRef` must be the standardized owned value family:\n\n```rust\n#[non_exhaustive]\n#[derive(Debug, Clone, PartialEq, Eq)]\npub enum OriginRef {\n    Text(String),\n    SourceLocation {\n        source: String,\n        line: u32,\n        column: u32,\n    },\n}\n```\n\nThe initial variants are exactly:\n\n```text\nText\nSourceLocation\n```\n\n`OriginRef` represents caller-supplied source correlation.\n\nIt may refer conceptually to:\n\n* an editor object;\n* an imported asset;\n* a source document;\n* a generated definition;\n* a file-like source;\n* an arbitrary caller-defined source label.\n\nThe core library must not interpret what the source text identifies.\n\n### Required traits\n\n`OriginRef` must implement:\n\n```text\nDebug\nClone\nPartialEq\nEq\n```\n\nDo not add `Copy`.\n\nDo not require `Default`, `Hash`, `PartialOrd`, or `Ord`.\n\nThe enum must be marked `#[non_exhaustive]`.\n\n---\n\n## O10. Provide owned origin constructors\n\n`OriginRef` must provide construction broadly equivalent to:\n\n```rust\nimpl OriginRef {\n    pub fn text(text: impl Into<String>) -> Self;\n\n    pub fn source_location(\n        source: impl Into<String>,\n        line: u32,\n        column: u32,\n    ) -> Self;\n}\n```\n\nRequired behavior:\n\n* `text` constructs `OriginRef::Text`;\n* `source_location` constructs `OriginRef::SourceLocation`;\n* borrowed source text becomes independently owned;\n* owned source text may move directly into the result;\n* the supplied text is retained exactly;\n* line and column are retained exactly.\n\nThese constructors must not require `'static` strings.\n\nDirect enum construction must remain possible because the variants and their fields are public.\n\n---\n\n## O11. Treat source coordinates as opaque caller data\n\n`OriginRef::SourceLocation` must not impose an undocumented coordinate convention.\n\nEvery `u32` line and column value is valid, including zero and `u32::MAX`.\n\nThe library must not:\n\n* require one-based coordinates;\n* require zero-based coordinates;\n* reject line zero;\n* reject column zero;\n* inspect the referenced source;\n* verify that the source exists;\n* check line length;\n* canonicalize file paths;\n* resolve URIs;\n* access the filesystem;\n* convert the source to `PathBuf`;\n* consult a source registry.\n\nDocumentation should describe line and column as caller-supplied source coordinates whose interpretation belongs to the caller and its tooling.\n\n---\n\n## O12. Keep origin separate from provenance\n\n`OriginRef` must be documented as authored source correlation, not causal provenance.\n\nIt must not contain or alias:\n\n* `CauseRef`;\n* `CauseDigest`;\n* runtime event identity;\n* transaction identity;\n* diagnostic episode identity;\n* a causal predecessor chain;\n* execution history.\n\nFor example:\n\n```text\nOriginRef::SourceLocation\n```\n\nmay explain where a node definition came from.\n\nIt does not explain why a runtime output currently has a particular value.\n\nNo provenance module or placeholder provenance type should be introduced.\n\n---\n\n## 11. Tags\n\n## O13. Treat tags as an exact owned sequence\n\nTags must use:\n\n```rust\nVec<String>\n```\n\nThe initial value semantics are exact sequence semantics.\n\nThe implementation must preserve:\n\n* tag contents;\n* tag order;\n* duplicate tags;\n* empty tags.\n\nFor example, all of the following are distinct metadata values:\n\n```rust\ntags: vec![\"debug\".into(), \"input\".into()]\ntags: vec![\"input\".into(), \"debug\".into()]\ntags: vec![\"debug\".into()]\ntags: vec![\"debug\".into(), \"debug\".into()]\ntags: vec![\"\".into()]\ntags: vec![]\n```\n\nThe implementation must not:\n\n* sort tags;\n* deduplicate tags;\n* trim tags;\n* case-fold tags;\n* reject empty tags;\n* interpret namespaces;\n* parse key-value tags;\n* convert tags into a set.\n\nLater callers may interpret tags according to their own tooling conventions, but the core value must preserve exactly what was supplied.\n\nNo tag-specific newtype or tag-query API is required.\n\n---\n\n## 12. Equality semantics\n\n## O14. Use complete structural equality\n\nEquality for all three public values must be structural.\n\n`DiagnosticMeta` values are equal exactly when:\n\n* their `name` values are equal;\n* their `description` values are equal;\n* their `path` values are equal;\n* their `origin` values are equal;\n* their tag sequences are equal.\n\n`DiagnosticPath` values are equal exactly when their ordered segment sequences are equal.\n\n`OriginRef` values are equal exactly when:\n\n* they use the same variant; and\n* every contained value is equal.\n\nEquality must not:\n\n* ignore fields;\n* ignore case;\n* ignore whitespace;\n* normalize text;\n* ignore tag order;\n* ignore duplicate tags;\n* treat absent and empty values as equal;\n* compare rendered strings instead of structured values.\n\nDerived `PartialEq` and `Eq` implementations are appropriate where they provide these semantics.\n\n---\n\n## 13. Clone and ownership semantics\n\n## O15. Cloning must create independently owned metadata\n\nCloning any metadata value must produce a structurally equal independently owned value.\n\nAfter cloning:\n\n* mutating the clone's name must not change the original;\n* mutating the clone's description must not change the original;\n* mutating cloned path segments through an owned projection must not change the original;\n* mutating the clone's tags must not change the original;\n* replacing the clone's origin must not change the original.\n\nThe implementation must not rely on shared mutable caller storage.\n\nOrdinary immutable implementation details are not required and should not be introduced merely to optimize cloning.\n\nUse ordinary owned standard-library values unless a concrete existing repository convention requires otherwise.\n\n---\n\n## O16. Metadata must outlive caller input storage\n\nThe values must not borrow caller input.\n\nTests must demonstrate that metadata remains usable after temporary or local source strings have been dropped.\n\nRepresentative cases include:\n\n```rust\nlet path = {\n    let first = String::from(\"outer\");\n    let second = String::from(\"inner\");\n\n    DiagnosticPath::from_segments([\n        first.as_str(),\n        second.as_str(),\n    ])\n};\n\nassert_eq!(\n    path.segments(),\n    &[String::from(\"outer\"), String::from(\"inner\")],\n);\n```\n\nand:\n\n```rust\nlet origin = {\n    let source = String::from(\"generated.definition\");\n\n    OriginRef::source_location(source.as_str(), 4, 7)\n};\n\nassert_eq!(\n    origin,\n    OriginRef::SourceLocation {\n        source: String::from(\"generated.definition\"),\n        line: 4,\n        column: 7,\n    },\n);\n```\n\nThe exact tests may use less allocation-heavy assertions, but they must establish the ownership property.\n\n---\n\n## 14. Text preservation\n\n## O17. Preserve valid Rust strings byte-for-byte\n\nAll metadata text must be retained exactly.\n\nRequired representative cases include:\n\n* ASCII text;\n* empty strings;\n* leading whitespace;\n* trailing whitespace;\n* embedded newlines;\n* different line endings;\n* repeated separators;\n* non-ASCII Unicode;\n* canonically distinct Unicode sequences where representable as distinct Rust strings.\n\nThe implementation must not normalize text during:\n\n* construction;\n* cloning;\n* path access;\n* origin access;\n* equality comparison;\n* debug representation.\n\nThis task does not implement persistence encoding, but the in-memory values must remain capable of later byte-preserving encoding.\n\n---\n\n## 15. Public documentation requirements\n\n## O18. Document the distinction from identity and diagnostics\n\nPublic module and type documentation must clearly state:\n\n* metadata is descriptive;\n* metadata is owned;\n* metadata does not define stable subject identity;\n* metadata does not affect signal behavior;\n* `DiagnosticPath` is an ordered descriptive hierarchy;\n* `DiagnosticPath` is not an operating-system path;\n* `OriginRef` is authored source correlation;\n* `OriginRef` is not runtime provenance;\n* tags retain caller-supplied sequence order and duplicates;\n* no text normalization occurs.\n\nDocumentation should remain focused on the implemented value types.\n\nDo not add extensive documentation for future graph, diagnostic, persistence, or reconfiguration APIs.\n\n---\n\n## 16. Trait and representation constraints\n\n## O19. Do not impose unnecessary trait requirements\n\nThe required trait surface is:\n\n```text\nDiagnosticMeta:\n    Debug\n    Clone\n    Default\n    PartialEq\n    Eq\n\nDiagnosticPath:\n    Debug\n    Clone\n    Default\n    PartialEq\n    Eq\n\nOriginRef:\n    Debug\n    Clone\n    PartialEq\n    Eq\n```\n\nThe implementation must not add trait bounds to unrelated types because none of these values are generic.\n\nThe implementation need not provide:\n\n```text\nCopy\nHash\nPartialOrd\nOrd\nSerialize\nDeserialize\nDisplay\nError\n```\n\nDo not add these traits speculatively.\n\nA normal derived `Debug` implementation is sufficient.\n\n`Debug` output is not rendered diagnostic prose and must not become a stable persistence or user-interface format.\n\n---\n\n## O20. Avoid unnecessary abstraction\n\nThe implementation should use ordinary standard-library ownership:\n\n```text\nString\nVec<String>\nOption<T>\n```\n\nDo not introduce:\n\n* string interning;\n* reference counting;\n* custom allocators;\n* small-string optimization;\n* custom collection types;\n* metadata trait objects;\n* metadata visitor traits;\n* generic string parameters stored in public types;\n* hidden global state;\n* unsafe code.\n\nThe task should remain a small independently auditable value module.\n\n---\n\n## 17. Required verification\n\n## O21. Default metadata\n\nTest that:\n\n```rust\nDiagnosticMeta::default()\n```\n\nhas:\n\n```text\nname = None\ndescription = None\npath = None\norigin = None\ntags = []\n```\n\nAlso test equality between independently created default values.\n\n---\n\n## O22. Complete metadata equality\n\nConstruct equal and unequal `DiagnosticMeta` values.\n\nVerify that changing each field independently changes equality:\n\n* name;\n* description;\n* path;\n* origin;\n* tags.\n\nVerify that absent and explicitly empty values remain distinct.\n\nAt minimum, include:\n\n```text\nNone name versus Some(\"\")\nNone description versus Some(\"\")\nNone path versus Some(empty path)\nempty tags versus one empty tag\n```\n\n---\n\n## O23. Diagnostic-path ordering and preservation\n\nTest:\n\n* empty path construction;\n* borrowed segment construction;\n* owned segment construction;\n* `segments`;\n* `len`;\n* `is_empty`;\n* `into_segments`;\n* segment-order preservation;\n* repeated-segment preservation;\n* empty-segment preservation;\n* path equality;\n* path inequality when segment order changes.\n\nAt minimum:\n\n```text\n[\"a\", \"b\"] != [\"b\", \"a\"]\n[\"a\", \"a\"] retains both segments\n[\"a\", \"\", \"b\"] retains the empty segment\n[\"a/b\"] != [\"a\", \"b\"]\n```\n\n---\n\n## O24. Origin values\n\nTest both origin variants.\n\nFor `Text`, verify exact text retention.\n\nFor `SourceLocation`, verify:\n\n* exact source retention;\n* exact line retention;\n* exact column retention;\n* inequality when source changes;\n* inequality when line changes;\n* inequality when column changes;\n* inequality from the `Text` variant.\n\nInclude boundary values demonstrating that zero coordinates are accepted.\n\nNo filesystem or external-source fixture is required.\n\n---\n\n## O25. Tag sequence semantics\n\nTest that:\n\n* insertion order is retained;\n* duplicates are retained;\n* empty tags are retained;\n* different order produces unequal metadata;\n* duplicate count affects equality;\n* cloning retains the complete sequence.\n\n---\n\n## O26. Deep clone independence\n\nConstruct a populated `DiagnosticMeta` containing:\n\n* a name;\n* a multiline description;\n* a multi-segment path;\n* one origin;\n* several tags including a duplicate.\n\nClone it.\n\nVerify:\n\n1. the clone initially equals the original;\n2. mutating the clone's public fields does not alter the original;\n3. consuming or replacing the clone's path does not alter the original;\n4. mutating the clone's tags does not alter the original.\n\nNo pointer-address assertion is required. Behavioral independence is the required property.\n\n---\n\n## O27. Non-`'static` ownership\n\nTest that:\n\n* `DiagnosticPath::from_segments` accepts borrowed local strings;\n* `OriginRef::text` accepts borrowed local text;\n* `OriginRef::source_location` accepts borrowed local source text;\n* the resulting values remain valid after the local source strings are dropped.\n\nThe public API must contain no lifetime parameter tied to caller storage.\n\n---\n\n## O28. Exact text preservation\n\nUse representative metadata strings containing:\n\n* leading and trailing spaces;\n* newline characters;\n* non-ASCII text;\n* an empty string.\n\nVerify exact equality after construction and cloning.\n\nDo not introduce normalization libraries or locale-dependent assertions.\n\n---\n\n## 18. Compile and repository checks\n\nThe implementation must pass the repository's existing required checks.\n\nAt minimum:\n\n```text\ncargo test --workspace\ncargo clippy --workspace --all-targets --all-features -- -D warnings\ncargo fmt --all -- --check\nmake check-final\n```\n\nUse the repository's actual authoritative commands if they differ at implementation time.\n\nAny warning, failure, unavailable required command, or ignored test is blocking.\n\nDo not weaken existing lints or test gates.\n\n---\n\n## 19. Expected change surface\n\nExpected files:\n\n```text\ncrates/mossignal/src/lib.rs\ncrates/mossignal/src/metadata.rs\n```\n\nNo dependency change is expected.\n\nNo specification change is expected.\n\nNo unrelated existing source module should require semantic modification.\n\nA different file layout is acceptable only when clearly required by an established repository convention. Do not split this small value family across several modules without a concrete reason.\n\n---\n\n## 20. Forbidden implementation outcomes\n\nThe implementation is unacceptable if it:\n\n* makes `DiagnosticMeta` generic;\n* gives metadata borrowed lifetimes;\n* stores `&str` or `Cow<'_, str>`;\n* treats metadata as stable identity;\n* derives graph behavior from metadata;\n* introduces graph definitions;\n* introduces diagnostic codes or findings;\n* introduces `Problem`, `DiagnosticSet`, or `Report`;\n* introduces provenance or `CauseRef`;\n* treats `OriginRef` as runtime causality;\n* resolves or opens source files;\n* represents `DiagnosticPath` as `PathBuf`;\n* parses path separators;\n* automatically derives paths from modules;\n* normalizes names, descriptions, paths, origins, or tags;\n* deduplicates or sorts tags;\n* rejects empty strings;\n* rejects empty paths;\n* rejects repeated path segments;\n* validates line or column conventions;\n* silently equates absent and empty metadata;\n* adds persistence encoding;\n* adds serde derives;\n* adds fingerprinting;\n* adds reconfiguration operations;\n* adds metadata inheritance;\n* adds new dependencies;\n* modifies existing signal, time, or key semantics;\n* introduces global state;\n* introduces unsafe code;\n* adds speculative future abstractions;\n* creates empty placeholder modules.\n\n---\n\n## 21. Acceptance criteria\n\nThe task is complete only when all of the following hold.\n\n### Public surface\n\n* `mossignal::metadata` exists publicly.\n* `DiagnosticMeta` exists with the required non-generic owned fields.\n* `DiagnosticPath` exists as an owned ordered segment value.\n* `OriginRef` exists with `Text` and `SourceLocation`.\n* no crate-root or prelude re-export was introduced.\n\n### Ownership\n\n* every stored string is owned;\n* no public metadata type has a lifetime parameter;\n* borrowed construction inputs are accepted where required;\n* constructed values outlive their source strings;\n* cloning produces independent owned values.\n\n### Value semantics\n\n* default metadata is completely empty;\n* equality includes every field;\n* path order is retained and equality-sensitive;\n* tags retain order and duplicates;\n* origin variants and coordinates participate in equality;\n* absent and empty values remain distinct;\n* valid strings are preserved exactly.\n\n### Semantic boundary\n\n* documentation states that metadata is descriptive and does not affect network execution, structural identity, state compatibility, or semantic fingerprints;\n* paths are not presented as structural identity;\n* origins are not presented as provenance;\n* no diagnostic-reporting architecture was introduced;\n* no execution, validation, graph, persistence, or reconfiguration behavior was introduced.\n\n### Verification\n\n* focused unit tests cover every obligation;\n* repository formatting passes;\n* repository linting passes with warnings denied;\n* all workspace tests pass;\n* the final repository quality gate passes;\n* no unrelated changes remain.\n\n---\n\n## 22. Completion evidence\n\nThe implementation report must identify:\n\n* the files changed;\n* the final public type shapes;\n* the `DiagnosticPath` representation and accessors;\n* the `OriginRef` variants and constructors;\n* how exact text preservation was verified;\n* how deep cloning and ownership were verified;\n* how path and tag ordering were verified;\n* confirmation that metadata remains non-generic and excluded from network execution, structural identity, state compatibility, and semantic fingerprints;\n* the exact commands run;\n* the outcome of each command.\n\nA report that only states that tests passed is insufficient.\n\n---\n\n## 23. Final implementation principle\n\nThe accepted implementation should make this statement plainly true:\n\n> `DiagnosticMeta` is a small, owned, structurally comparable description of an authored subject. It can be cloned, stored, inspected, and later persisted without borrowing caller data, but it never becomes the subject's identity, behavior, diagnostic condition, or provenance.\n","notes":"Implementation approval:\nStatus: Approved\nReviewed against specification and implementation baseline: 24c0d4db3673915ca5c66a123ec781b7ccd374bb\n\nVerification:\n- make check-final: passed\n\nAccepted scope:\n- public non-generic DiagnosticMeta with exact owned fields and structural equality\n- owned semantically ordered DiagnosticPath with exact preservation and required accessors\n- exact initial non-exhaustive OriginRef family with owned constructors and opaque u32 coordinates\n- exact tag sequence, absence-versus-empty, cloning, and non-'static ownership behavior\n- focused public documentation and unit coverage\n- no graph, diagnostic-reporting, provenance, persistence, reconfiguration, dependency, or prelude expansion","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-13T21:20:10.507591Z","created_by":"mikkofinell","updated_at":"2026-07-13T22:17:33.298092Z","closed_at":"2026-07-13T22:17:33.298037Z","close_reason":"Implemented the authoritative owned diagnostic metadata value API with focused verification.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-2x1","title":"Execute user modules through ordinary runtime machinery","description":"## Objective\n\nImplement Premium Continuation Roadmap item 36 as one bounded capability slice: recursively lower validated user-module instances into the existing primitive compiled/runtime machinery while preserving instance-qualified identity, hierarchy, fingerprints, causality, atomicity, and supported runtime observation.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 36\n- Title: Module-aware compilation, execution, and observation\n\nThis provenance records task sequencing only. Authoritative specifications and the referenced contracts govern semantics.\n\n## Contract basis\n\n### Reviewed contracts\n\n- `mossignal.module.aware_execution`\n  - `docs/specs/contracts/module-aware-execution.yaml`\n  - role: governs recursive ordinary-primitive lowering, stable qualified correspondence, deterministic module compilation, retained compiled/runtime hierarchy, ordinary state and temporal ownership, atomicity, budgets, provenance, observation, and module-execution verification.\n- `mossignal.module.instantiation_hierarchy`\n  - `docs/specs/contracts/module-instantiation-hierarchy.yaml`\n  - role: governs instantiation, binding, retained validated hierarchy, causality, and module-aware semantic identity. Its temporary compilation-boundary rule and now-covered lowering gap were independently retired in this planning pass.\n\n### Additional reviewed contracts reused without modification\n\n- `mossignal.compile.compiled_network` (`docs/specs/contracts/compiled-network.yaml`): validated-only compilation, immutable topology, stable identity, private dense layout, reaction correspondence, invariants, and deterministic compilation.\n- `mossignal.identity.network_fingerprint` (`docs/specs/contracts/network-fingerprint.yaml`): validated fingerprint ownership and canonical exclusion of private layout for its represented families; module-instance sensitivity is governed by `mossignal.module.instantiation_hierarchy`.\n- `mossignal.runtime.stateful_reaction_foundation` (`docs/specs/contracts/stateful-reaction-foundation.yaml`): independent family state, previous/proposed state, current output, and atomic successor commitment.\n- `mossignal.runtime.temporal_event_execution` (`docs/specs/contracts/temporal-event-execution.yaml`): exact-time pending work, due-event execution, cancellation, and schedule.\n- `mossignal.runtime.reaction_scoped_pulse_foundation` (`docs/specs/contracts/reaction-scoped-pulse-foundation.yaml`): simultaneous Pulse multiplicity and reaction-scoped clearing.\n- `mossignal.runtime.restricted_full_topological_evaluator` (`docs/specs/contracts/restricted-full-topological-evaluator.yaml`): ordinary primitive full-evaluation semantics and causal correspondence.\n- `mossignal.runtime.policy` (`docs/specs/contracts/runtime-policy.yaml`): explicit work limits and complete budget-failure atomicity.\n- `mossignal.runtime.machine_lifecycle` (`docs/specs/contracts/machine-lifecycle.yaml`): immutable compiled ownership, independent spawned state, initialization, and ready-machine boundaries.\n- `mossignal.diagnostics.artifact_reports` (`docs/specs/contracts/artifact-reports.yaml`): compilation artifact/report behavior and blocking omission.\n\nAll governing contracts are reviewed at unchanged authoritative evidence baselines for the facets used here.\n\n## Included scope\n\n- Make both compile forms succeed for every otherwise supported validated user-module instance, including mixed containing primitives, multiple instances, and nested instances.\n- Recursively lower module-local nodes, connections, public input bindings, and public output mappings into the existing primitive descriptors and dependency graph.\n- Preserve complete instance-path plus module-local-key correspondence for internal nodes, ports, connections, state, temporal work, diagnostics, provenance, and supported inspection.\n- Retain module instances, parent-child hierarchy, public interfaces, bindings, and canonical expanded subjects in compiled graph/inspection access even when execution is flattened.\n- Execute lowered Constant, Not, All, Any, Parity, AtLeast, Select, Merge, Toggle, and PulseDelay through their ordinary existing reaction, state, pulse, temporal, transaction, provenance, and runtime-policy paths.\n- Preserve exact Level and Pulse interface behavior, fan-out, simultaneous multiplicity, cross-boundary current-reaction causality, state barriers, and temporal barriers through arbitrary supported nesting.\n- Keep each stateful and temporal primitive independent per instance path, including when several instances share one `ModuleDef`.\n- Carry the validated `NetworkFingerprint` and `InputSchemaFingerprint` unchanged through compilation.\n- Add owned user-module `ModuleInspection` with a public boundary view and grouped expanded internal view, and extend focused structural/runtime observation to identify supported module-qualified subjects and hierarchy.\n- Remove the temporary `compilation.unsupported_module_instances` rejection, evidence/discriminator implementation, catalogue/API/specification text, and rejection tests; confirm no contract reintroduces the retired staged rule.\n- Establish behavioral differential conformance between an explicit primitive graph and its module-wrapped equivalent after normalizing intentionally distinct identity and hierarchy.\n\n## Exclusions and non-goals\n\n- No standard catalogue discovery, standard typed constructor family, generated standard modules, or new standard-origin behavior.\n- No persistence envelopes, snapshots, restoration, replay, or module-qualified artifact encoding.\n- No module replacement, topology patches, compatible revision migration, state migration, or pending-event migration.\n- No module-specialized evaluator, callback evaluator, hidden module state, module-specific transactions, incremental module compiler, descriptor sharing optimization, or new crate.\n- No complete generic inspection query planner, subscriptions, or complete explanation rendering beyond the user-module and currently supported primitive observation paths.\n- No identity equivalence promise between an explicit primitive graph and a module-wrapped graph; only required behavioral equivalence.\n\n## Implementation obligations\n\n- Implement every represented requirement in reviewed `mossignal.module.aware_execution` while preserving reviewed `mossignal.module.instantiation_hierarchy`.\n- Reuse one recursive lowering path for top-level and nested instances and the existing closed primitive descriptor/evaluator dispatch; module boundaries must not introduce runtime semantic variants.\n- Resolve each module input from its exact binding and each module output from its exact internal mapping. Preserve kind, incidence, fan-out, Pulse counts, and conservative dependency edges.\n- Use collision-free module-qualified logical identity. Dense indices and any derived private lookup keys remain revision-local implementation details and must not leak into public identity, diagnostics, provenance, or fingerprints.\n- Allocate state slots, temporal ownership, event records, and causal subjects independently for every qualified primitive occurrence.\n- Preserve one immutable compiled topology with independent spawned machines and a single failure-atomic transaction publication point.\n- Count expanded primitive work through existing runtime budgets consistently with the equivalent explicit graph; rejection must publish no partial hierarchy-associated state or output.\n- Retain the authored module hierarchy alongside any flattened executable projection for compiled graph access and supported runtime inspection.\n- Remove the entire temporary unsupported-module surface unless implementation discovers a separately authoritative unsupported case; do not keep compatibility shims.\n- Keep production code free of `unwrap` and `expect`, preserve structured failures, and add brief `SPEC:` traces only for non-obvious normative invariants.\n\n## Allowed implementation freedom\n\n- Choose direct recursive flattening or an equivalent recursive descriptor builder, favoring the smallest deterministic extension of the existing compiler.\n- Choose collision-free private derived keys, path interning, lookup maps, hierarchy indices, and descriptor ordering; record and test the chosen deterministic correspondence without exposing dense layout.\n- Choose whether module bindings are resolved eagerly into a temporary canonical graph or directly into descriptors, provided error/report behavior, compiled invariants, and retained hierarchy are identical.\n- Add the smallest typed module-qualified subject/reference values and owned `ModuleInspection` structures needed by existing graph, inspection, and provenance APIs. Broader query, subscription, and explanation frameworks remain future work.\n\n## Expected change surface\n\n- `crates/mossignal/src/compile.rs` for recursive lowering, qualified descriptor correspondence, retained hierarchy metadata, endpoint resolution, and compiled invariants.\n- `crates/mossignal/src/validation.rs` only to replace the temporary compile rejection boundary and expose/reuse already-validated dependency and definition data without revalidating semantics.\n- `crates/mossignal/src/module.rs`, `crates/mossignal/src/key.rs`, and `crates/mossignal/src/authored.rs` for focused reusable qualified subject/path projections where existing values are insufficient.\n- `crates/mossignal/src/machine.rs` and `crates/mossignal/src/transaction.rs` for module-qualified state, pending-work, provenance, and supported runtime inspection ownership.\n- `crates/mossignal/src/diagnostics.rs` and `crates/mossignal/src/lib.rs` for temporary diagnostic retirement and deliberate public exports.\n- `crates/mossignal/tests/module_instantiation.rs` to remove the staged rejection oracle and retain authoring/validation coverage.\n- New focused integration tests for module compilation/execution/observation, plus existing compiler, machine, and transaction tests where their private oracles need qualified subjects.\n- `docs/specs/api_and_semantics_spec.md`, `docs/specs/concrete_rust_api_surface.md`, and `docs/specs/exhaustive_diagnostic_code_catalogue.md` to retire only the explicitly temporary staged-boundary text and catalogue entry.\n- The roadmap item state and synchronized `.beads` metadata.\n\nTreat this as an expected surface, not permission for unrelated refactoring. If a required correction materially exceeds it, stop for renewed planning.\n\n## Required verification\n\n1. Verify module-free compilation metadata and runtime behavior remain unchanged.\n2. Compile and execute single, multiple, mixed containing/internal, and at least two-level nested user modules over Level-only, Pulse-only, and mixed interfaces.\n3. Compare explicit primitive and module-wrapped graphs for initialization, ready reactions, outputs, Pulse multiplicity, state successors, pending events, schedule, diagnostics, normalized provenance, budgets, and rejection outcomes while allowing distinct identity/hierarchy.\n4. Verify exact binding/output resolution, cross-boundary fan-out, nested output projection, and no accidental connection to private internals.\n5. Verify current-reaction causal chains across module boundaries, including reconvergence, stateful current outputs, true state barriers, temporal barriers, and nested paths.\n6. Instantiate one stateful or temporal module more than once and prove independent state, pending events, deadlines, causes, and inspection subjects per instance path.\n7. Exercise Toggle and PulseDelay inside nested modules through initialization, repeated transactions, direct time jumps, equal deadlines, cancellation/scheduling behavior, and exact overflow or budget failures.\n8. Verify hierarchy and module-qualified nodes/connections remain discoverable after compilation and supported node, pending-work, output, and provenance inspection identifies the exact owning path.\n9. Verify compilation and observations are invariant under network/module insertion order and deterministic hierarchy traversal; validate compiled static invariants for nested expansions.\n10. Verify the compiled artifact retains the validated fingerprints exactly; private flattening/layout changes do not alter them, while instance/binding/module/hierarchy semantic changes retain existing sensitivity tests.\n11. Capture complete machine observations before every applicable structured failure and prove failure atomicity for module-expanded overflow, time arithmetic, and runtime-budget boundaries.\n12. Remove the item-35 compile-rejection tests and prove `compilation.unsupported_module_instances` no longer exists in production code, catalogue/API text, contract rules, or public diagnostic evidence.\n13. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Readiness\n\nReady and free of unresolved product semantics. Independent review promoted both governing contracts and confirmed this bounded bead against them. Ordinary private lowering/layout choices are implementation freedom, not blockers.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-29T13:09:59.754401Z","created_by":"mikkofinell","updated_at":"2026-07-29T14:06:37.043935Z","closed_at":"2026-07-29T14:06:37.043892Z","close_reason":"Implemented recursive module lowering, qualified runtime observation and provenance, temporal/state isolation, diagnostic retirement, and full verification.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["compilation","module","roadmap","runtime"],"dependencies":[{"issue_id":"ms-2x1","depends_on_id":"ms-pr8","type":"blocks","created_at":"2026-07-29T13:09:59.754401Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-4ev","title":"Add level aliases and builder-only conveniences","description":"## Objective\n\nImplement Premium Continuation Roadmap item 39 as the supported level-only convenience slice: add xor, level_gate, majority, nand, nor, and xnor to both typed builders while preserving exact primitive lowering, ordinary identity, diagnostics, inspection, and execution.\n\n## Roadmap provenance\n\n- Document: docs/premium_continuation_roadmap.md\n- Item: 39\n- Title: Supported aliases and builder-only conveniences\n\nThis provenance records sequencing only. The referenced contracts and authoritative specifications govern product semantics.\n\n## Contract basis\n\n### Reviewed feature contract\n\n- mossignal.authoring.level_conveniences\n  - docs/specs/contracts/level-conveniences.yaml\n  - status: reviewed\n  - role: governs exact classification, lowering, semantic-boundary consequences, typed builder behavior, and conformance for the six level conveniences.\n\n### Reviewed contracts reused without modification\n\n- mossignal.node.level_combinational_expansion (docs/specs/contracts/level-combinational-expansion.yaml): Any, Parity, and AtLeast total laws, stable variadic multiplicity, typed authoring, diagnostics, causality, execution, and conformance. Only unchanged semantic, authoring, diagnostic, and verification facets are reused; changed historical fingerprint evidence is outside this task.\n- mossignal.node.variadic_all (docs/specs/contracts/variadic-all.yaml): All total law, stable variadic multiplicity, authoring, diagnostics, causality, execution, and conformance. Only unchanged non-fingerprint facets are reused.\n- mossignal.runtime.restricted_full_topological_evaluator (docs/specs/contracts/restricted-full-topological-evaluator.yaml): ordinary Not law and complete primitive-graph settlement.\n- mossignal.authoring.typed_module_builder (docs/specs/contracts/typed-module-builder.yaml): ModuleBuilder primitive-family parity, builder-scoped signals, foreign-signal rejection, canonical lowering, and ordinary validation.\n- mossignal.authoring.typed_network_builder (docs/specs/contracts/typed-network-builder.yaml): NetworkBuilder scope and structured foreign-signal failure behavior.\n- mossignal.identity.module_fingerprint (docs/specs/contracts/module-fingerprint.yaml) and mossignal.identity.network_fingerprint (docs/specs/contracts/network-fingerprint.yaml): existing semantic graph identity and metadata exclusions. Only unchanged represented rules apply; changed unrelated validated-owner source evidence is not needed.\n\n## Included scope\n\n- Add concise NetworkBuilder and ModuleBuilder xor(a, b), level_gate(value, enable), and majority(inputs) primitive aliases.\n- Lower aliases exactly to binary Parity, binary All, and AtLeast(floor(arity / 2) + 1) respectively, including majority([]) as AtLeast(1, []) and therefore Low.\n- Add concise NetworkBuilder and ModuleBuilder nand(inputs), nor(inputs), and xnor(a, b) builder-only operations.\n- Lower builder-only operations exactly to Not(All(inputs)), Not(Any(inputs)), and Not(Parity([a, b])) using only ordinary primitive authoring.\n- Preserve immediate iterator consumption, stable per-port multiplicity, duplicate sources, builder ownership checks, deterministic local key allocation, primitive-owned diagnostics, and ordinary inspection, fingerprints, provenance, validation, compilation, and execution.\n- Document each method classification clearly enough that aliases cannot be mistaken for modules and builder-only compositions cannot be mistaken for one durable semantic object.\n- Add focused exact-lowering, identity, diagnostic, builder-parity, and bounded truth-table coverage.\n\n## Exclusions and non-goals\n\n- No add_xor, add_level_gate, add_majority, or single keyed form that creates alias identity; callers use the existing explicit keyed primitive forms when durable node keys are required.\n- No single keyed nand, nor, or xnor form and no hidden composition identity. Callers needing a durable boundary may author explicit primitives or wrap them in a user ModuleDef.\n- No standard catalogue descriptor, StandardModuleRef, ModuleInstance, module hierarchy, module-level diagnostic, compatibility version, migration boundary, or privileged runtime path for any convenience.\n- No debounce until InertialDelay is implemented.\n- No any_pulse until Coalesce is implemented.\n- No annotate_signal or other metadata-only Signal API.\n- No low-enabled gate variants, convenience proliferation across polarity or priority permutations, pulse conveniences, stateful or temporal conveniences, new primitive kinds, persistence encoding, topology patches, or migration.\n\n## Implementation obligations\n\n- Reuse the existing NetworkBuilder primitive constructors and ModuleBuilder delegation pattern rather than adding parallel authored-graph or evaluator machinery.\n- Preserve the exact convenience classification and exact primitive expansions from mossignal.authoring.level_conveniences.\n- Collect each variadic input iterator once so majority derives its threshold from the same complete multiplicity-preserving input sequence passed to AtLeast.\n- Ensure every signal-taking method returns AuthoringFailure::ForeignSignal for cross-builder inputs through the existing structured primitive-authoring path; do not partially author a multi-node builder-only operation before validating all supplied signals.\n- Keep convenience names outside semantic identity. Equivalent fresh-builder convenience and direct-primitive constructions must yield the same authored structure and fingerprints when allocation state and metadata are equivalent.\n- Preserve ordinary primitive diagnostics exactly, including empty, unary, duplicate-source, and constant-result findings; add no convenience-specific diagnostic codes or suppression.\n- Keep production code free of unwrap and expect.\n\n## Allowed implementation freedom\n\n- Choose direct method composition or a small private helper, favoring the simplest deterministic implementation that validates all inputs before mutating a multi-node composition.\n- Choose the smallest iterator collection strategy for variadic conveniences; record and test the deterministic primitive and port allocation order without making it a new reusable product promise.\n- Do not retain alias spelling as metadata unless existing APIs make that useful without changing semantic identity; omission is conforming.\n- Test helpers may normalize only presentation and locally allocated key values when comparing convenience and explicit construction. They must retain node kind, configuration, incidence, multiplicity, diagnostics, hierarchy absence, and semantic fingerprints where equivalent identities are supplied.\n\n## Expected change surface\n\n- crates/mossignal/src/builder.rs for NetworkBuilder methods and ModuleBuilder parity.\n- A focused integration test such as crates/mossignal/tests/level_conveniences.rs, plus only narrowly required existing regressions.\n- Public rustdoc for the six methods and classifications.\n- docs/premium_continuation_roadmap.md only for the completion heading after accepted implementation.\n- .beads when implementation is closed and synchronized.\n\n## Required verification\n\n1. Assert xor and xnor use exactly one binary Parity with stable two-port multiplicity; xnor adds exactly one downstream Not.\n2. Assert level_gate uses exactly one binary All with value and enable both retained; no implicit polarity variant or simplification is introduced.\n3. Assert majority uses exactly one AtLeast whose threshold is floor(arity / 2) + 1 for arities zero through at least five, preserves duplicate-source multiplicity, and handles empty input as AtLeast(1, []) with Low output and ordinary diagnostics.\n4. Assert nand and nor author exactly All then Not and Any then Not for empty, unary, and multi-input cases without hidden simplification.\n5. Exhaust bounded Level valuations for all six conveniences against independent Boolean laws, including duplicate-source cases and all empty and unary variadic boundaries.\n6. Compare convenience-authored graphs with direct primitive authoring from equivalent fresh builder states: primitive kinds, configurations, port incidence, connections, diagnostics, output behavior, and NetworkFingerprint or ModuleFingerprint must agree after only permitted normalization.\n7. Verify NetworkBuilder and ModuleBuilder expose equivalent methods, reject foreign signals structurally, retain no ModuleInstance or standard declaration, and execute solely through ordinary primitive machinery.\n8. Verify direct graph inspection and provenance expose only canonical primitive subjects and relationships, with no convenience-owned subject, hierarchy, diagnostic, state, or pending work.\n9. Verify existing primitive advisories remain exact for empty, unary, duplicate-source, and constant-result cases and no new diagnostic codes are introduced.\n10. Run focused Rust tests while iterating, then make check-dev. Final acceptance additionally runs make check-final.\n\n## Blocking questions\n\nNone. Exact classifications, primitive expansions, total boundary behavior, and identity consequences are specified. Helper structure, allocation order within existing deterministic builder rules, and metadata retention remain bounded implementation freedom.\n\n## Readiness\n\nReady. The governing mossignal.authoring.level_conveniences contract is independently reviewed, and no product or compatibility blocker prevents implementation.","notes":"Implemented documented NetworkBuilder and ModuleBuilder xor, level_gate, majority, nand, nor, and xnor conveniences through the existing primitive authoring paths. Primitive aliases retain exactly one canonical primitive; builder-only operations retain only their exact ordinary primitive graphs. Added focused public-surface tests for exact authored structure and fingerprints, module parity, majority arities zero through five, complete binary and variadic boundary laws, ordinary diagnostics, foreign-signal failure before mutation, canonical primitive-only provenance, and absence of pending work. Corrective acceptance verification: cargo test -p mossignal --test level_conveniences (7 passed); make check-dev; make check-final.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-30T05:07:02.782736Z","created_by":"mikkofinell","updated_at":"2026-07-30T05:41:11.891553Z","closed_at":"2026-07-30T05:19:17.356856Z","close_reason":"Implemented and accepted exact level aliases and builder-only conveniences on both typed builders","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["authoring","convenience","roadmap"]}
{"id":"ms-6fg","title":"Establish standard catalogue foundation with Exactly","description":"## Objective\n\nImplement Premium Continuation Roadmap item 37 as the first standard-module capability slice: establish the versioned descriptor and construction foundation and provide `Exactly` as an ordinary canonical standard-origin `ModuleDef` that instantiates and executes through the existing generic module and primitive machinery.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 37\n- Title: Standard catalogue foundation with `Exactly`\n\nThis provenance records task sequencing only. Authoritative specifications and the referenced contracts govern semantics.\n\n## Contract basis\n\n### Reviewed contracts accepted for reuse\n\n- `mossignal.standard.catalogue_foundation`\n  - `docs/specs/contracts/standard-catalogue-foundation.yaml`\n  - status: reviewed; accepted for reuse\n  - role: governs catalogue and descriptor identity, the pre-release evolving inventory and explicit later release freeze, discovery, parameters, standard declarations, canonical expansion and stable keys, construction equivalence, diagnostics, inspection, and verification.\n- `mossignal.standard.exactly`\n  - `docs/specs/contracts/standard-exactly.yaml`\n  - status: reviewed; accepted for reuse\n  - role: governs `Exactly` identity, interface, total cardinality law, dependency signature, exact case expansion, diagnostics, inspection, explanation, and exhaustive conformance.\n\n### Reviewed contracts reused without modification\n\n- `mossignal.module.instantiation_hierarchy` (`docs/specs/contracts/module-instantiation-hierarchy.yaml`): exact typed and dynamic instantiation, stable public-output identity, hierarchy, binding validation, and instance-sensitive network identity.\n- `mossignal.module.aware_execution` (`docs/specs/contracts/module-aware-execution.yaml`): ordinary recursive primitive lowering, retained hierarchy, qualified runtime ownership, atomicity, budgets, provenance, and observation. Its changed `api-validated` evidence only retired the old temporary rejection and does not alter the facets reused here.\n- `mossignal.identity.module_fingerprint` (`docs/specs/contracts/module-fingerprint.yaml`): public module-fingerprint value, domain separation, validated ownership, ordering, and user-origin distinction; its explicitly excluded standard declaration projection is supplied by the reviewed catalogue contract.\n- `mossignal.identity.network_fingerprint` (`docs/specs/contracts/network-fingerprint.yaml`): containing-network instance, binding, hierarchy, and module-fingerprint sensitivity.\n- `mossignal.diagnostics.problem_model`, `mossignal.diagnostics.subject_references`, `mossignal.diagnostics.collections`, and `mossignal.diagnostics.artifact_reports`: structured catalogue-backed problems, stable subjects, deterministic finding collection, and blocking artifact omission. Changed subject-reference evidence must be independently rechecked only for the new standard categories added by this bead.\n- `mossignal.node.level_combinational_expansion` and `mossignal.node.variadic_all`: ordinary Constant, Not, Any, All, and AtLeast semantics, stable variadic-port identity, causality, compilation, and settlement used by the canonical `Exactly` expansion. The changed restricted-fingerprint evidence is not reused for standard-origin identity.\n\n## Included scope\n\n- Add the public standard catalogue identity family, exact `StandardModuleRef`, immutable descriptor discovery, closed parameter values and schemas, dynamic requests, structured lookup failures, and catalogue-backed construction reports.\n- Extend `ModuleOrigin<D>` and `ModuleDef<D>` with an owned canonical `StandardModuleDeclaration<D>` retaining exact descriptor reference, parameters, public variadic keys, and expansion fingerprint.\n- Implement the specified deterministic public-role and internal-role key derivation, uniqueness checks, canonical expansion fingerprint, and standard-origin module/network fingerprint contribution.\n- Add catalogue-backed dynamic construction that retains malformed requests through structured validation and produces an ordinary validated standard-origin `ModuleDef` only on success.\n- Add `Exactly` with its fixed identity, required `threshold: u64`, zero-or-more stable Level inputs, one fixed result output, declaration-sensitive public dependency signature, and exact four-branch canonical primitive expansion.\n- Add concise and explicit-keyed `Exactly` construction to both `NetworkBuilder` and `ModuleBuilder`, plus generic instantiation of a catalogue-built definition; equivalent inputs must converge on the same declaration and canonical instance structure.\n- Add standard-module blocking and non-blocking diagnostic codes/evidence needed by catalogue lookup, request validation, canonical generation, and `Exactly` degeneracy, threshold, and duplicate-source findings with the specified coexistence and suppression laws.\n- Extend graph and module inspection with exact standard declaration, descriptor data, parameters, expansion fingerprint, internal roles, and the `Exactly` summary; preserve qualified primitive drill-down and module-level diagnostic grouping.\n- Execute instantiated `Exactly` solely through existing module compilation, ordinary primitive evaluation, runtime budgets, transaction atomicity, provenance, and inspection.\n- Add deterministic descriptor-generation checks and exhaustive bounded `Exactly` conformance.\n\n## Exclusions and non-goals\n\n- No `AtMost`, `AllEqual`, resettable stateful standard module, temporal standard module, primitive alias, metadata-only convenience, builder-only composition, or application-domain module implementation in this bead.\n- No persistence envelope or decoder, restoration, snapshot/replay integration, topology patch, module replacement, descriptor-version migration, state migration, pending-event migration, or internal-edit enforcement.\n- No privileged evaluator, callback, new primitive, new signal kind, hidden state, fixed-point execution, module-specific transaction, optimized standard-module execution, or new crate.\n- No generic query planner, subscription system, rendered documentation generator, editor palette, or complete prose explanation renderer beyond the smallest structured descriptor and inspection hooks required here.\n\n## Implementation obligations\n\n- Implement against the two reviewed contracts; independent contract review is complete.\n- Reuse the existing canonical authored module, validation, generic instantiation, compilation, runtime, provenance, inspection, diagnostic, and fingerprint paths; standard modules are ordinary exact ModuleDef values with additional declaration identity.\n- Treat catalogue, semantic, and expansion versions as provisional throughout pre-release roadmap development. Keep each build internally coherent and deterministic, but add or revise descriptors without compatibility shims until an explicit release-readiness decision freezes the inventory and versions; never substitute a behaviorally equivalent `Exactly` graph.\n- Keep dynamic request defects caller-representable and collect independent safe findings deterministically; shipped descriptor key collisions and descriptor/expansion disagreement remain structured LibraryDefect outcomes, never panics or silent repairs.\n- Canonicalize parameter assignments and variadic public keys deterministically; preserve duplicate-source multiplicity per public port and ensure all derived internal keys and roles remain collision-free and insertion-order independent.\n- Ensure typed concise, typed keyed, dynamic build plus generic instantiation, and direct canonical definition paths agree on declaration, expansion, interface, dependency signature, diagnostics, fingerprints, hierarchy, runtime behavior, and supported observation after normalizing intentionally allocated concise identities.\n- Record non-obvious permanent derivation and canonical-case invariants with brief `SPEC:` traces; production code remains free of `unwrap` and `expect`.\n\n## Allowed implementation freedom\n\n- Choose static tables, generated constants, or a zero-sized catalogue view, favoring the smallest deterministic representation with structured canonical discovery.\n- Choose compact private schema-view and iterator types and key-derivation helper decomposition without changing the specified public semantic inputs, domains, roles, or resulting stable identities.\n- Concise `exactly` may allocate instance and variadic public keys locally; `add_exactly` and dynamic requests provide durable explicit identity.\n- Choose the smallest structured `Exactly` inspection values and explanation hooks that expose every required fact and preserve primitive causal drill-down; complete generic explanation rendering remains outside this slice.\n\n## Expected change surface\n\n- New focused standard catalogue module(s) under `crates/mossignal/src/` for identities, descriptors, requests, schemas, declarations, canonical derivation, and `Exactly` generation.\n- `crates/mossignal/src/module.rs`, `authored.rs`, and `identity.rs` for standard origin, declaration ownership, canonical standard expansion identity, and module/network fingerprint integration.\n- `crates/mossignal/src/builder.rs` for concise and explicit-keyed `Exactly` constructors on both builder families and construction-path equivalence.\n- `crates/mossignal/src/validation.rs` and `diagnostics.rs` for standard request/declaration conformance, canonical expansion checks that are possible in this non-persistence slice, and catalogue conditions.\n- `crates/mossignal/src/compile.rs`, `machine.rs`, and `transaction.rs` only for focused descriptor/role/inspection projection; execution should otherwise remain generic.\n- `crates/mossignal/src/lib.rs` for deliberate public catalogue exports.\n- New focused standard catalogue and exhaustive `Exactly` integration tests, plus fingerprint, module-instantiation, and module-execution regression tests.\n- The pre-release catalogue policy in `docs/specs/standard_module_catalogue_spec.md` remains authoritative: implementation synchronizes descriptors, identities, fingerprints, diagnostics, and tests within each development revision without compatibility ceremony.\n- `.beads/` when later review accepts and synchronizes the implementation.\n\n## Required verification\n\n1. Verify exact identity wrappers, reserved id syntax, catalogue version, canonical descriptor order, exact and latest lookup, unsupported lookup failures, structured schema metadata, and insertion-order independence.\n2. Exercise every independent dynamic request defect and safe coexistence combination; verify blocking artifact omission, deterministic ordering/merge behavior, exact evidence, and LibraryDefect classification for shipped descriptor invariants.\n3. Compare concise, explicit-keyed, dynamic/generic, and direct canonical `Exactly` paths for declaration, public keys, parameters, expansion, roles, dependency signature, diagnostics, fingerprints, hierarchy, execution, provenance, and inspection.\n4. Exhaust all Level valuations for arities 0 through at least 5 with thresholds 0, 1, arity minus one where defined, arity, arity plus one, and u64::MAX; compare an independent exact-cardinality oracle with the canonical expansion.\n5. Cover all canonical expansion branches and verify exact primitive kinds, configurations, roles, port qualification, connections, exports, absence of redundant primitive warnings, and unique deterministic derived keys.\n6. Permute stable public input order and descriptor/request insertion order without changing behavior, dependency relation, expansion fingerprint, or module fingerprint; change stable keys, threshold, descriptor versions, expansion roles, or incidence and verify appropriate sensitivity.\n7. Include duplicate upstream sources and verify each public port contributes independently, the duplicate-source module warning coexists correctly, and generic unused-input warnings remain suppressed for intentional constant-result expansions.\n8. Verify `Exactly` public dependency signatures for constant and nonconstant declarations agree with the expanded reaction graph and preserve network cycle validation across standard-module boundaries.\n9. Instantiate one definition multiple times and nest `Exactly` inside user and standard-capable module builders; verify retained standard origin, exact hierarchy, standard declaration, internal roles, qualified provenance, diagnostics, inspection summary, and independent machine execution.\n10. Compare explicit primitive and standard-module-wrapped runtime behavior after normalizing intentional identity/hierarchy differences; verify ordinary budgets and complete transaction failure atomicity.\n11. Verify user-origin behaviorally identical modules remain User-origin with distinct ModuleFingerprint, while containing NetworkFingerprint remains sensitive to exact standard declaration, instance key, bindings, and hierarchy.\n12. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Blocking questions\n\nNone. The maintainer explicitly resolved the prior inventory conflict by keeping catalogue and descriptor versions provisional until a later release-readiness decision. Ordinary pre-release descriptor additions and revisions require synchronized specifications, identities, fingerprints, and tests, not compatibility shims or migrations.\n\n## Readiness\n\nReady as a bounded roadmap-item-37 implementation bead governed by the two independently reviewed contracts. No product or compatibility blocker prevents implementation.","notes":"Implemented the versioned standard catalogue foundation and canonical Exactly descriptor/module. Added typed and dynamic construction, standard-origin identity and fingerprints, structured diagnostics, fixed canonical expansion, public dependency and explanation inspection, builder nesting, and exhaustive arity 0-5 truth-table coverage. Verified with make check-dev and make check-final.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-29T16:06:50.754693Z","created_by":"mikkofinell","updated_at":"2026-07-29T17:27:06.546144Z","closed_at":"2026-07-29T17:27:06.546106Z","close_reason":"Implemented and accepted; development and final gates pass.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["catalogue","modules","roadmap"]}
{"id":"ms-6qf","title":"Implement typed NetworkBuilder foundation","description":"## Objective\n\nImplement Premium Foundation Roadmap item 21: establish the first typed NetworkBuilder<D> authoring path for the already-supported restricted level-only Constant/Not network. The builder must own authored identity and lower deterministically into the existing canonical UncheckedNetwork<D>, without creating a competing graph representation.\n\n## Contract basis\n\n- mossignal.authoring.typed_network_builder\n  - docs/specs/contracts/typed-network-builder.yaml\n  - status: reviewed, accepted by independent contract review\n  - role: typed builder ownership, Signal<S> scope, foreign-signal failure, level endpoint and Constant/Not responsibilities, and completion boundary.\n\n- mossignal.graph.authored_network_definition\n  - docs/specs/contracts/authored-network-definition.yaml\n  - status: reviewed, used unchanged\n  - role: canonical unchecked representation, stable-keyed lossless authored records, Constant/Not shapes, and metadata separation.\n\n- mossignal.identity.time_domain\n  - docs/specs/contracts/time-domain-identity.yaml\n  - status: reviewed, used unchanged\n  - role: caller-supplied TimeDomainId must bind before validation and remain intrinsic.\n\n- mossignal.identity.network_fingerprint\n  - docs/specs/contracts/network-fingerprint.yaml\n  - status: reviewed, contextual\n  - role: builder-produced equivalent authored structure must retain semantic identity independent of construction mechanics.\n\n## Included scope\n\n- Add the deliberate public NetworkBuilder<D> and builder-only typed Signal<S> surface.\n- Bind caller-supplied TimeDomainId at builder creation, expose bound network/time-domain identity, and use a builder-local stable-key allocator.\n- Support typed level external input and output construction, including explicit keyed forms with DiagnosticMeta.\n- Support typed Constant and Not construction, including explicit keyed forms that expose node identity.\n- Reject a Signal originating from a different builder as AuthoringFailure::ForeignSignal; never panic or silently wire it.\n- Lower the builder into the existing UncheckedNetwork<D> representation with stable keys, nodes, ports, connections, endpoints, metadata, and time-domain identity preserved deterministically.\n- Provide the bounded completion route appropriate to the current existing validation/report API, consuming the builder once.\n- Add focused typed-versus-dynamic equivalence, key preservation, foreign-signal, duplicate-key, and Constant/Not construction tests.\n\n## Exclusions and non-goals\n\n- No pulse endpoints or primitives.\n- No All or other variadic/combinational family; roadmap item 22 owns the variadic pattern.\n- No modules, hierarchy, bindings, persistence, reconfiguration, new validation behavior, compilation changes, evaluator changes, machine/transaction work, or graph-model replacement.\n- Do not implement branded lifetimes, generative closure scopes, public persistent Signal<S> values, or a full future primitive constructor family.\n- Do not alter canonical dynamic authored definitions except for minimal access/support needed for faithful lowering.\n\n## Implementation obligations\n\n- Honor typed_network_builder rules owning-identity-boundary, typed-builder-scoped-signals, foreign-signal-is-structured-failure, typed-endpoint-authoring, primitive-authoring-contract, and completion-boundary.\n- Keep Signal<S> opaque and scoped by internal builder identity; the selected endpoint and primitive signatures accept and return Signal<Level> only.\n- Make each builder-authored Constant distinct; do not merge equal values.\n- Ensure equivalent typed and direct dynamic definitions validate, compile, and exhibit identical network/input-schema fingerprints and restricted behavior.\n- Preserve caller-provided explicit stable keys and metadata exactly in lowered definitions. Deterministically allocate convenience-form keys from the builder-local allocator.\n- Keep lowering one-way into UncheckedNetwork<D>; direct dynamic authoring remains available for malformed/deserialized inputs.\n\n## Allowed implementation freedom\n\n- Exact constructor names/grouping, Signal<S> private layout, internal builder token, collection layout, lowering helpers, and module placement are free.\n- Choose the simplest deterministic allocation and lowering approach, test it, and record the concrete choice at implementation close-out.\n- A consuming finish may delegate through the existing unchecked validation route; do not create a parallel validation model.\n\n## Expected change surface\n\n- a focused builder module under crates/mossignal/src/\n- crates/mossignal/src/lib.rs for deliberate public exports\n- minimal authored/key/diagnostic support only where necessary to construct the established canonical definitions\n- focused unit and integration-style tests\n- docs/specs/contracts/typed-network-builder.yaml only for implementation-discovered specification-backed facts; any change remains draft\n- .beads/ state when implementation acceptance closes the bead\n\n## Required verification\n\n1. Build a representative level-input → Not → level-output network through NetworkBuilder and verify the lowered structure and compiled observable behavior match an equivalent direct UncheckedNetwork fixture.\n2. Verify explicit supplied network, endpoint, node, and port keys plus DiagnosticMeta survive lowering exactly; convenience allocation is deterministic for identical call sequences.\n3. Verify TimeDomainId is required/bound at creation and is retained through lowered, validated, and compiled artifacts.\n4. Verify the selected endpoint and primitive APIs accept and return Signal<Level> only.\n5. Verify passing a signal from another builder to Not or level_output returns AuthoringFailure::ForeignSignal and leaves the receiving builder usable.\n6. Verify duplicate explicit keys are rejected where immediately determinable.\n7. Verify equal-valued constants authored in separate calls have distinct stable node identities and are not silently merged.\n8. Verify builder Signal<S> cannot become a snapshot, binding, replay, or persistence artifact through the public API.\n9. Run make check-dev, then make check-final at implementation acceptance.\n\n## Dependencies\n\n- Premium Foundation Roadmap items 8–20 are accepted; ms-u88 is the immediately preceding completed roadmap slice.\n\n## Readiness\n\nReady for implementation after independent contract review. No unresolved product question blocks this bounded slice.","notes":"Implementation: added the public typed NetworkBuilder and opaque Signal handles for the restricted level-only Constant/Not slice. The builder owns its authored state and time-domain identity, lowers one-way into UncheckedNetwork, rejects foreign handles and immediate duplicate explicit keys, and preserves explicit network, endpoint, node, port, and diagnostic metadata values. Convenience allocation uses the deterministic local KeyAllocator namespace and skips explicitly used keys.\\n\\nVerification: focused builder tests passed; cargo test -p mossignal --no-fail-fast passed (121 unit tests and 15 doctests); make check-dev passed; make check-final passed.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T07:50:29.920955Z","created_by":"mikkofinell","updated_at":"2026-07-28T08:31:33.882842Z","closed_at":"2026-07-28T08:31:33.882791Z","close_reason":"Implemented typed NetworkBuilder foundation with validated canonical lowering","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-6qf","depends_on_id":"ms-u88","type":"blocks","created_at":"2026-07-28T07:50:29.920955Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-771","title":"Implement complete level InputSnapshot and builder","description":"## Objective\n\nImplement Premium Foundation Roadmap item 15: an owned level-only InputSnapshot and builder for the restricted Constant/Not compiled network. The successful artifact must bind exact compiled-network and input-schema identity and contain a complete authoritative value for every external level input before later initialization execution consumes it.\n\n## Contract basis\n\n- mossignal.runtime.level_input_snapshot\n  - docs/specs/contracts/level-input-snapshot.yaml\n  - status: reviewed, accepted by independent contract review\n  - role: compiled builder origin; owned opaque artifact; exact network/schema binding; complete level valuation; invalid-observation rejection; and completion rejection.\n\n- mossignal.identity.input_schema_fingerprint\n  - docs/specs/contracts/input-schema-fingerprint.yaml\n  - status: reviewed, used unchanged\n  - role: distinct deterministic input-schema identity that future snapshots carry beside network identity.\n\n- mossignal.identity.network_fingerprint\n  - docs/specs/contracts/network-fingerprint.yaml\n  - status: reviewed, used unchanged\n  - role: distinct immutable compiled-topology identity retained from successful validation.\n\n- mossignal.compile.compiled_network\n  - docs/specs/contracts/compiled-network.yaml\n  - status: reviewed, used unchanged\n  - role: immutable compiled topology and unchanged retained identities that own snapshot-builder context.\n\n## Included scope\n\n- Add an owned opaque level-only InputSnapshot<D> and InputSnapshotBuilder<D>.\n- Expose a CompiledNetwork<D> builder entry point bound to that compiled topology’s NetworkKey, NetworkFingerprint, and InputSchemaFingerprint.\n- Accept one LogicLevel assignment for each represented ExternalInputKey<Level>.\n- Reject an unknown external level key and a repeated equivalent or conflicting assignment.\n- Reject finish when any required external level key lacks an authoritative assignment; do not infer Low.\n- Keep successful snapshot data deterministic by stable external-input key and suitable for later initialization evaluation without discovering omission.\n- Add focused construction and failure tests.\n\n## Exclusions and non-goals\n\n- Do not implement Machine, lifecycle, initialization transactions, event emission, runtime policy, state storage, or evaluator integration.\n- Do not implement InputDelta, retained values, revision context, pulse input or PulseCount behavior.\n- Do not implement binding projection, caller observation projection, diagnostics rendering, broad diagnostic-kernel expansion, persistence artifact encoding/decoding, replay, snapshots, topology patches, or prepared-patch target builders.\n- Do not expose compiled dense indices, storage positions, or a public promise about a particular snapshot iteration representation.\n\n## Implementation obligations\n\n- Respect level_input_snapshot rules compiled-builder-origin, owned-opaque-network-bound-artifact, exact-identity-binding, complete-authoritative-level-valuation, rejects-invalid-observations, and finish-rejects-incomplete-snapshot.\n- Keep NetworkKey, NetworkFingerprint, and InputSchemaFingerprint separate and bind them from the exact CompiledNetwork context; no caller-supplied ad hoc identity or positional endpoint mapping.\n- Ensure successful construction contains exactly one value for every external level input and cannot carry an unknown or repeated assignment.\n- Make omission a construction failure rather than an implicit LogicLevel::Low value.\n- Keep the implementation restricted to currently represented level endpoints and preserve opaque boundaries around compiled storage and snapshot contents.\n\n## Allowed implementation freedom\n\n- Choose the simplest deterministic stable-keyed private storage, builder ownership style, error enum shape, module placement, and minimal read-only crate-internal access required by later work.\n- The typed level-only builder need not introduce an erased wrong-kind API. Record and test the concrete ordering/storage choice, but do not turn it into a permanent public iteration guarantee.\n- Prefer direct auditable validation over optimization; implementation may add a concise SPEC comment only for a non-obvious invariant safeguard.\n\n## Expected change surface\n\n- crates/mossignal/src/compile.rs or one focused input-artifact module\n- crates/mossignal/src/lib.rs for deliberate public InputSnapshot exports and compiled builder access\n- focused unit tests adjacent to the construction boundary\n- docs/specs/contracts/level-input-snapshot.yaml only for implementation-discovered reusable specification facts; any change remains draft\n- .beads/ when implementation acceptance closes the bead\n\n## Required verification\n\n1. Build a complete snapshot for a compiled network with multiple external level inputs and assert it retains the compiled NetworkKey, NetworkFingerprint, and InputSchemaFingerprint.\n2. Verify construction order does not affect the successful deterministic snapshot representation or its observable test inspection.\n3. Verify unknown level inputs fail.\n4. Verify a repeated equal assignment fails as duplicate and a repeated different assignment fails as conflicting.\n5. Verify finish fails for every omitted required level input and no omission defaults to Low.\n6. Verify an empty external-level schema can produce a complete empty level snapshot if the existing restricted compiled topology permits it.\n7. Run focused tests while iterating, then make check-dev; final acceptance additionally runs make check-final.\n\n## Acceptance Criteria\n\n- Public construction exposes an owned opaque level-only snapshot builder from CompiledNetwork and binds every successful artifact to that network’s NetworkKey, NetworkFingerprint, and InputSchemaFingerprint.\n- Completion succeeds only with exactly one LogicLevel for every current-schema external level input; incomplete, unknown, duplicate, and conflicting input cases fail.\n- Snapshot construction has focused deterministic-order and identity-binding tests, and the implementation passes the required repository gates.\n\n## Dependencies\n\n- Roadmap item 12 / ms-vbt is complete and supplies validated network and input-schema identity.\n- Roadmap item 13 / ms-d1f is complete and supplies immutable CompiledNetwork identity retention and private external-input descriptors.\n- Roadmap item 14 / ms-l0b is complete but intentionally remains raw-fact evaluator work; this bead does not integrate the artifact with evaluation.\n\n## Readiness\n\nReady for implementation. No conflict, missing fundamental semantic rule, or frozen compatibility promise blocks the bounded level-only slice. Deterministic private representation and error-API details are implementation freedom.","notes":"Independent contract review promoted mossignal.runtime.level_input_snapshot at the recorded source baseline. Review correction: exact compiled-network binding retains NetworkKey separately alongside NetworkFingerprint and InputSchemaFingerprint; prepared-patch target context remains known-uncovered and outside this bead.\n\nImplementation acceptance:\n- Corrected InputSnapshot to retain NetworkKey separately from both fingerprints.\n- Strengthened omission verification to cover each partially omitted required input as well as total omission and empty schemas.\n- Focused input tests passed (3 tests).\n- make check-dev passed.\n- make check-final passed.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T19:21:52.570495Z","created_by":"mikkofinell","updated_at":"2026-07-27T19:39:09.362582Z","closed_at":"2026-07-27T19:39:09.362535Z","close_reason":"Accepted complete exact-identity-bound level InputSnapshot construction after corrective review","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-771","depends_on_id":"ms-d1f","type":"blocks","created_at":"2026-07-27T19:22:07.057361Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-771","depends_on_id":"ms-l0b","type":"blocks","created_at":"2026-07-27T19:22:07.098557Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-771","depends_on_id":"ms-vbt","type":"blocks","created_at":"2026-07-27T19:22:07.017579Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1","title":"Establish bead-driven agent workflow skills","description":"Objective:\nEstablish the repository-local quality gates, focused agent skills, and concise repository instructions required to execute the lifecycle in docs/bead_review_implementation_commit_process.md with Codex and Gemini.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: complete lifecycle and role boundaries.\n- docs/testing_and_verification_policy.md: verification layers and gate classes.\n- All other files in docs/: authoritative product and architecture specifications consulted by role skills as applicable.\n\nScope:\nTrack the coherent delivery sequence for workflow conventions, quality gates, six role skills, and final AGENTS.md reconciliation.\n\nNon-goals:\nNo Claude-specific setup, plugin packaging, concurrent-agent reservation system, CI provider configuration, or product implementation.\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-07-12T09:03:32.891916Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.957156Z","closed_at":"2026-07-12T09:39:59.957110Z","close_reason":"Completed initial bead-driven agent workflow scaffolding.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"]}
{"id":"ms-7j1.1","title":"Define portable skill and bead workflow conventions","description":"Objective:\nDefine and implement the shared repository conventions used by all bead lifecycle skills.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: bead contract, lifecycle, evidence, and handoff semantics.\n- docs/testing_and_verification_policy.md: verification vocabulary and runtime classes.\n\nObligations:\n- O1: Establish .agents/skills as the canonical repository-local Agent Skills location discoverable by Codex and Gemini.\n- O2: Define the native br status plus mutually exclusive workflow label mapping for Draft, Bead Review, Ready, blocked states, implementation phases, Final Review, and Closed.\n- O3: Define the bead field template: locked contract in description and acceptance criteria, supporting rationale in design, and evolving evidence and findings in notes.\n- O4: Define expected change surface as advisory coordination evidence for sequential work, not a lock or permission boundary.\n- O5: Provide deterministic validation where practical for required sections, status-label consistency, and exactly one active workflow label.\n\nForbidden outcomes:\n- Do not add Claude-specific discovery paths or metadata.\n- Do not create a vendor-specific plugin.\n- Do not duplicate authoritative specifications into skill folders.\n- Do not treat arbitrary file lists as concurrency-safe reservations.\n\nScope:\nShared conventions, portable skill discovery, templates, and validation needed by later role skills.\n\nNon-goals:\nNo role skill implementation, final quality gate, AGENTS.md rewrite, or real parallel reservation mechanism.\n\nConcern flags:\n- Cross-agent discovery\n- br lifecycle semantics\n- Durable evidence format\n\nExpected change surface:\n- .agents/skills/\n- workflow validation scripts or configuration\n- .beads metadata only as required by this bead\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- O1: Codex and Gemini discovery are verified against the same canonical skill tree.\n- O2: Representative lifecycle transitions are exercised with br and preserve br ready behavior for Ready work.\n- O3: A representative bead passes the documented template and evidence rules.\n- O4: Documentation clearly distinguishes expected change surface from reservation.\n- O5: Positive and negative validator cases demonstrate concise diagnostics and nonzero failure.\n\nCompletion criteria:\nLater role-skill beads can reference one stable convention without inventing status, label, discovery, or evidence behavior.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:32.932143Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.390031Z","closed_at":"2026-07-12T09:39:59.389965Z","close_reason":"Established canonical Codex and Gemini skill layout and bead workflow conventions.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.1","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:32.932143Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.2","title":"Implement quiet development and final quality gates","description":"Objective:\nReplace the provisional check composition with truthful, quiet development and final acceptance gates over every verification mechanism currently applicable to the repository.\n\nAuthoritative sources:\n- docs/testing_and_verification_policy.md: per-change, extended, scheduled, fuzzing, and runtime-class requirements.\n- docs/bead_review_implementation_commit_process.md: mandatory gate and acceptance semantics.\n\nObligations:\n- O1: Provide individual Make targets for each real check and composite make check-dev and make check-final targets.\n- O2: check-dev runs a very fast subset suitable for active implementation and prints one concise OK result on success.\n- O3: check-final runs every finite repository-mandated acceptance check currently applicable, including strict formatting, compilation, linting, tests, documentation, dependency policy, architecture validation, and bounded fuzz or corpus checks when those facilities exist.\n- O4: Successful subcommands remain quiet; failure reports the check name, command, captured diagnostics, and nonzero status.\n- O5: Missing mandatory infrastructure is never reported as success. Future targets are excluded until applicable or fail explicitly if exposed as required.\n- O6: Define a finite fuzz-check boundary: deterministic corpus replay or bounded smoke checks in the final gate, with long campaigns classified as extended or bead-specific verification.\n- O7: Provide acceptance-record-check for the post-close, post-flush bead state without rerunning expensive Rust checks.\n- O8: Keep any pre-commit hook a thin optional wrapper around authoritative Make targets rather than a second implementation.\n\nForbidden outcomes:\n- No silent placeholder success.\n- No warning output that requires subjective acceptance.\n- No indefinite fuzz campaign in a commit gate.\n- No weakening checks merely to clear the gate.\n\nScope:\nMakefile check interface, quiet runner, current applicable checks, and focused verification of runner behavior.\n\nNon-goals:\nNo CI provider setup, long scheduled campaigns, product tests that have no implementation yet, or automatic hook installation without an explicit repository decision.\n\nExpected change surface:\n- Makefile\n- scripts/\n- focused runner tests or fixtures\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- O1-O4: success and injected-failure runs prove composite coverage and output contracts.\n- O5: an unavailable required command blocks rather than passing.\n- O6: fuzz checks are finite and reproducible.\n- O7: representative valid and invalid final bead records exercise acceptance-record-check.\n- Run focused checks for the runner, then the exact final gate produced by this bead.\n\nCompletion criteria:\nImplementers have one fast truthful gate, final reviewers have one comprehensive finite gate, and final bead-state validation can run after closure and flush.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.036956Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.456752Z","closed_at":"2026-07-12T09:39:59.456685Z","close_reason":"Implemented quiet development, comprehensive final, and post-close acceptance-record checks.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["quality-gate","tooling"],"dependencies":[{"issue_id":"ms-7j1.2","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.036956Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.2","depends_on_id":"ms-7j1.1","type":"blocks","created_at":"2026-07-12T09:03:33.137489Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.3","title":"Create the author-bead skill","description":"Objective:\nCreate a focused Agent Skill that derives one bounded draft implementation contract from authoritative docs.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: bead author responsibilities and approved-contract shape.\n- All files in docs/: authoritative source set to search and cite as applicable.\n\nObligations:\n- O1: Trigger on requests to author or materially revise a draft implementation bead.\n- O2: Require objective, directly governing and adjacent sources, obligations, forbidden outcomes, scope, non-goals, evidence, concern flags, dependencies, expected change surface, and an explicitly unapproved review section.\n- O3: Search docs with sufficient context and summarize rather than copy large specification passages.\n- O4: Detect unresolved architecture, semantics, contradiction, or incoherent scope and report the proper blocker instead of inventing an answer.\n- O5: Create only Draft work and never self-approve.\n- O6: Sync bead changes after mutation.\n\nForbidden outcomes:\nNo self-approval, implementation, speculative architecture, compatibility shims, or pseudocode-level overprescription.\n\nExpected change surface:\n- .agents/skills/author-bead/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A realistic authoring prompt produces a structurally valid Draft bead grounded in docs.\n- A deliberately ambiguous prompt produces a precise design blocker rather than invented obligations.\n- Skill validation passes and Codex and Gemini can discover the skill.\n\nCompletion criteria:\nA fresh agent can author a bounded, auditable, visibly unapproved bead without additional process prompting.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.192750Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.523951Z","closed_at":"2026-07-12T09:39:59.523901Z","close_reason":"Created and validated the author-bead skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.3","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.192750Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.3","depends_on_id":"ms-7j1.1","type":"blocks","created_at":"2026-07-12T09:03:33.295029Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.4","title":"Create the review-bead skill","description":"Objective:\nCreate a focused Agent Skill for independent auditing and direct correction of a Draft bead.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: bead review, convergence, and approval rules.\n- All files in docs/: authoritative comparison set as applicable.\n\nObligations:\n- O1: Audit the complete bead against governing and adjacent docs.\n- O2: Directly correct resolvable omissions, distortions, weak evidence, forbidden fallbacks, and accidental scope.\n- O3: Distinguish blocking findings from advisories and preserve persistent finding identities.\n- O4: Converge on exactly Ready, Blocked Design, or Blocked Rescoping.\n- O5: Verify the final corrected bead before approval and preserve locked-contract semantics.\n- O6: Sync bead mutations and leave br status and workflow label consistent.\n\nForbidden outcomes:\nNo generic changes-requested relay, endless refinement, implementation work, or invented design decision.\n\nExpected change surface:\n- .agents/skills/review-bead/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A flawed but resolvable draft is corrected and marked Ready.\n- A semantically undecided draft is blocked for design with the exact missing decision.\n- Advisory-only improvements do not prevent readiness.\n- Skill validation and Codex and Gemini discovery pass.\n\nCompletion criteria:\nA fresh reviewer can produce a correct executable contract or a precise blocker without relying on the author.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.349957Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.586088Z","closed_at":"2026-07-12T09:39:59.586043Z","close_reason":"Created and validated the review-bead skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.4","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.349957Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.4","depends_on_id":"ms-7j1.3","type":"blocks","created_at":"2026-07-12T09:03:33.450106Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.5","title":"Create the implement-bead skill","description":"Objective:\nCreate a focused Agent Skill for implementing an approved Ready bead as an uncommitted working-tree proposal on main.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: baseline, implementation, safety, and handoff rules.\n- The approved bead and all applicable files in docs/.\n\nObligations:\n- O1: Verify Ready approval, dependencies, main, HEAD, complete status, active work, and expected change surface before editing.\n- O2: Read the complete bead, governing docs, relevant types, call sites, and testing obligations.\n- O3: Implement the smallest coherent approved slice with tests and provisional obligation evidence.\n- O4: Run focused checks plus make check-dev while preserving additional bead-specific verification.\n- O5: Leave HEAD unchanged, all implementation uncommitted, and the bead in Implementation Review.\n- O6: Stop and return to review or design when implementation requires changing the locked contract.\n\nForbidden outcomes:\nNo intermediate implementation commit, self-acceptance, destructive cleanup of unexplained work, architectural invention, or silent failure.\n\nExpected change surface:\n- .agents/skills/implement-bead/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A representative Ready bead yields an uncommitted implementation handoff with accurate evidence and check results.\n- Unsafe dirty-tree and contract-ambiguity scenarios stop with precise blockers.\n- Skill validation and Codex and Gemini discovery pass.\n\nCompletion criteria:\nA fresh implementer can safely produce a complete reviewable working tree without committing or changing approved semantics.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.503496Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.650174Z","closed_at":"2026-07-12T09:39:59.650127Z","close_reason":"Created and validated the implement-bead skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.5","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.503496Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.5","depends_on_id":"ms-7j1.2","type":"blocks","created_at":"2026-07-12T09:03:33.676484Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.5","depends_on_id":"ms-7j1.4","type":"blocks","created_at":"2026-07-12T09:03:33.604436Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.6","title":"Create the review-implementation skill","description":"Objective:\nCreate a focused Agent Skill for independently reviewing and directly correcting an uncommitted bead implementation.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: implementation-review object, correction authority, and handoff.\n- The approved bead and all applicable files in docs/.\n\nObligations:\n- O1: Inspect HEAD, staged, unstaged, relevant untracked files, and surrounding unchanged code.\n- O2: Perform the three-way comparison among docs, approved bead, and implementation plus tests.\n- O3: Verify every obligation, negative constraint, relevant path, enforcement layer, adjacent contract, and test sensitivity.\n- O4: Directly correct in-scope defects, strengthen tests, update evidence, and retain material finding identities.\n- O5: Run focused verification and make check-dev after corrections.\n- O6: Move corrected work to Final Review or return a materially wrong contract to bead review without silently broadening it.\n\nForbidden outcomes:\nNo comment-relay ceremony, diff-only review, hidden contract changes, commit, closure, or destructive cleanup.\n\nExpected change surface:\n- .agents/skills/review-implementation/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A seeded implementation defect is detected, corrected directly, verified, and recorded.\n- A seeded bead-contract defect returns the bead to review instead of changing scope silently.\n- Skill validation and Codex and Gemini discovery pass.\n\nCompletion criteria:\nA fresh reviewer can turn proposed work into a corrected Final Review candidate or identify the correct upstream blocker.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.739032Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.705911Z","closed_at":"2026-07-12T09:39:59.705863Z","close_reason":"Created and validated the review-implementation skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.6","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.739032Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.6","depends_on_id":"ms-7j1.5","type":"blocks","created_at":"2026-07-12T09:03:33.851442Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.7","title":"Create the finalize-bead skill","description":"Objective:\nCreate a focused Agent Skill that independently accepts, closes, flushes, stages, and commits one completed bead.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: final review, quality gate, closure, synchronization, and commit semantics.\n- docs/testing_and_verification_policy.md: applicable comprehensive and bead-specific verification.\n- The approved bead and all applicable files in docs/.\n\nObligations:\n- O1: Independently inspect the complete working tree and verify all changes belong to the active Final Review bead.\n- O2: Recheck every obligation, recorded finding, evidence entry, governing constraint, and bead-specific command.\n- O3: Directly correct remaining in-scope defects, then re-review and rerun affected checks.\n- O4: Run make check-final against the exact accepted code tree and treat warnings, unavailable commands, hangs, flakes, and failures as blocking.\n- O5: Close the bead, run br sync --flush-only, inspect bead-store changes, and run acceptance-record-check.\n- O6: Stage the complete accepted change, inspect the staged diff, create one bead-prefixed semantic commit, and verify no bead-related tracked residue.\n- O7: Return to bead review when acceptance requires changing the locked contract.\n\nForbidden outcomes:\nNo premature commit, post-commit closure commit, omitted mandatory check, unrelated staged work, self-referential hash, or generic commit subject.\n\nExpected change surface:\n- .agents/skills/finalize-bead/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A representative Final Review tree follows the exact review, gate, close, flush, validation, stage, inspect, and commit order.\n- Gate, sync, unrelated-change, and contract-change failures prevent commitment with precise diagnostics.\n- The accepted commit contains implementation and closed synchronized bead state and begins with the bead ID.\n- Skill validation and Codex and Gemini discovery pass.\n\nCompletion criteria:\nA fresh final reviewer can create exactly one defensible accepted commit or stop at the correct blocker.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-12T09:03:33.911437Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.769785Z","closed_at":"2026-07-12T09:39:59.769734Z","close_reason":"Created and validated the finalize-bead skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","skills"],"dependencies":[{"issue_id":"ms-7j1.7","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:33.911437Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.7","depends_on_id":"ms-7j1.2","type":"blocks","created_at":"2026-07-12T09:03:34.085330Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.7","depends_on_id":"ms-7j1.6","type":"blocks","created_at":"2026-07-12T09:03:34.028786Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.8","title":"Create the audit-committed-bead skill","description":"Objective:\nCreate a focused Agent Skill for retrospective semantic audit of already accepted bead-backed commits.\n\nAuthoritative sources:\n- docs/bead_review_implementation_commit_process.md: committed-code audit and corrective bead semantics.\n- The original bead, accepted commit or range, and current applicable files in docs/.\n\nObligations:\n- O1: Compare accepted code, original bead, commit history, and current authoritative docs.\n- O2: Distinguish original acceptance context from later specification evolution.\n- O3: Produce only no correction required, a new corrective Draft bead, or design escalation.\n- O4: A corrective bead references the original bead, accepted commit, violated requirements, observed defect, and required evidence.\n- O5: Preserve the original bead as historical and sync any newly created corrective bead.\n\nForbidden outcomes:\nNo silent rewrite of a closed bead, immediate code modification, reopening accepted work as a substitute for correction, or invented resolution of ambiguity.\n\nExpected change surface:\n- .agents/skills/audit-committed-bead/\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- A clean historical case reports no corrective work.\n- A seeded substantive defect creates a correctly linked Draft corrective bead.\n- A seeded specification ambiguity escalates design without proposing invented code.\n- Skill validation and Codex and Gemini discovery pass.\n\nCompletion criteria:\nA fresh auditor can inspect accepted history without corrupting its record or bypassing the ordinary lifecycle.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-12T09:03:34.150556Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.832379Z","closed_at":"2026-07-12T09:39:59.832323Z","close_reason":"Created and validated the audit-committed-bead skill.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","audit","skills"],"dependencies":[{"issue_id":"ms-7j1.8","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:34.150556Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.8","depends_on_id":"ms-7j1.7","type":"blocks","created_at":"2026-07-12T09:03:34.259970Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-7j1.9","title":"Replace provisional AGENTS.md with mossignal workflow guidance","description":"Objective:\nReplace the copied provisional AGENTS.md with concise mossignal-specific repository guidance that exposes the finished skill workflow and authoritative commands to Codex and Gemini.\n\nAuthoritative sources:\n- All files in docs/, with docs/bead_review_implementation_commit_process.md governing lifecycle and docs/testing_and_verification_policy.md governing verification.\n- The implemented role skills and Make targets created by predecessor beads.\n\nObligations:\n- O1: Remove copied game-engine assumptions and describe only mossignal repository rules.\n- O2: Name the six focused skills and explain which user-assigned role task triggers each one.\n- O3: State that all docs are authoritative until a later explicit precedence policy exists.\n- O4: Document main-only uncommitted implementation, sequential code-changing agents, dirty-tree safety, and no architectural invention.\n- O5: Document make check-dev, make check-final, acceptance-record-check, and bead-specific verification responsibilities accurately.\n- O6: Keep durable global constraints in AGENTS.md and leave detailed role procedures in on-demand skills.\n\nForbidden outcomes:\nNo retention of copied domain rules, duplication of complete skill bodies, Claude-specific setup, or instructions that conflict with the process specification.\n\nExpected change surface:\n- AGENTS.md\n\nBead review:\nStatus: Unreviewed\nFindings: None recorded\nReadiness verdict: Not approved","acceptance_criteria":"Required evidence:\n- AGENTS.md contains no copied game-engine terminology or obsolete commands.\n- Every referenced skill and Make target exists and matches its actual behavior.\n- A cold Codex and Gemini session can discover the workflow entry points without loading every detailed procedure eagerly.\n- Documentation and final repository gates pass.\n\nCompletion criteria:\nThe default repository instructions are concise, mossignal-specific, and correctly route agents into the reviewed skills.","status":"closed","priority":1,"issue_type":"docs","created_at":"2026-07-12T09:03:34.315262Z","created_by":"mikkofinell","updated_at":"2026-07-12T09:39:59.895987Z","closed_at":"2026-07-12T09:39:59.895942Z","close_reason":"Replaced provisional repository guidance with mossignal-specific Codex and Gemini instructions.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["agent-workflow","documentation"],"dependencies":[{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1","type":"parent-child","created_at":"2026-07-12T09:03:34.315262Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.3","type":"blocks","created_at":"2026-07-12T09:03:34.421440Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.4","type":"blocks","created_at":"2026-07-12T09:03:34.482479Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.5","type":"blocks","created_at":"2026-07-12T09:03:34.542298Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.6","type":"blocks","created_at":"2026-07-12T09:03:34.602773Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.7","type":"blocks","created_at":"2026-07-12T09:03:34.660825Z","created_by":"mikkofinell","metadata":"{}","thread_id":""},{"issue_id":"ms-7j1.9","depends_on_id":"ms-7j1.8","type":"blocks","created_at":"2026-07-12T09:03:34.721318Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-8o7","title":"Implement temporal event foundation with PulseDelay","description":"## Objective\n\nImplement Premium Foundation Roadmap item 26 as one bounded vertical slice: establish exact pending-event ownership and chronological temporal execution through the existing machine and transaction architecture, using `PulseDelay` as the single opening temporal primitive.\n\n## Contract basis\n\n### Reviewed contracts created for this item\n\n- `mossignal.runtime.temporal_event_execution`\n  - `docs/specs/contracts/temporal-event-execution.yaml`\n  - status: reviewed; accepted reusable contract baseline\n  - role: governs the ordered semantic calendar, stable machine-local pending-event identity, strictly future scheduling, equal-deadline batches, chronological ready-machine advancement, schedule reporting, runtime budgets, atomicity, and reference-path verification.\n- `mossignal.node.pulse_delay`\n  - `docs/specs/contracts/pulse-delay.yaml`\n  - status: reviewed; accepted reusable contract baseline\n  - role: governs PulseDelay's fixed shape, positive delay, exact reproduction law, temporal causality barrier, authoring, compilation, initialization, pending groups, inspection, provenance, identity, and conformance.\n\n### Reviewed contracts reused unchanged\n\n- `mossignal.runtime.reaction_scoped_pulse_foundation`\n  - role: established typed Pulse batches, checked multiplicity, exact-bound initialization and delta input, Pulsed output events, reaction isolation, and grouped causal support. Its reviewed pulse semantics are reused only where cited evidence remains unchanged; its changed pre-stability fingerprint source is not silently reused as temporal authority.\n- `mossignal.runtime.stateful_reaction_foundation` and `mossignal.node.toggle`\n  - role: established previous/proposed state isolation, machine-owned family storage, same-reaction visibility, and single atomic publication patterns that temporal event proposals extend without conflating stored state with pending work.\n- `mossignal.graph.authored_network_definition`, `mossignal.authoring.typed_network_builder`, and `mossignal.graph.structural_validation`\n  - role: established dynamic definitions, typed and explicit-key construction, malformed-definition retention, and shape validation.\n- `mossignal.graph.current_reaction_dependency_graph`, `mossignal.graph.current_reaction_cycle_validation`, `mossignal.compile.compiled_network`, and `mossignal.runtime.restricted_full_topological_evaluator`\n  - role: established stable-subject dependency, acyclicity, immutable descriptor, private dense layout, deterministic settlement, and invariant patterns extended by the temporal barrier and due-event roots.\n- `mossignal.runtime.machine_lifecycle`, `mossignal.runtime.initialization_transaction`, `mossignal.runtime.ready_level_transaction`, and `mossignal.runtime.policy`\n  - role: established lifecycle, exact transaction binding, clone-and-swap staging, single publication, failure atomicity, result-owned provenance, and explicit semantic budgets.\n- `mossignal.identity.time_domain` and `mossignal.identity.network_fingerprint`\n  - role: established caller-owned time-domain identity, semantic parameter sensitivity, insertion-order independence, golden vectors, and the synchronized pre-stability projection policy.\n\nThe existing ready-level transaction contract explicitly leaves temporal-deadline advancement uncovered; the new temporal execution contract owns that facet rather than broadening the settled level-only record. Snapshot/restoration, topology migration, and generic inspection facets recorded as `known_uncovered` are outside this bead.\n\n## Included scope\n\n- Add public `PendingEventKey` as a machine-local monotonic u64 serial distinct from private calendar storage, plus the smallest focused pending-event observation needed to prove stable identity.\n- Add an exact ordered semantic event calendar mapping `Time<D>` to complete equal-deadline batches, with deterministic machine-owned records and no node-owned opaque queue.\n- Add public `Schedule<D>`, ready-machine `schedule()` and `next_deadline()`, result schedule reporting, and structured pre-initialization rejection.\n- Extend ready-machine application to process every deadline strictly before the requested time chronologically, combine obligations due exactly at the requested time with that current-time batch, and publish one atomic result containing chronological output events at their actual times.\n- Apply existing runtime-policy limits to internal reactions, pending events, newly created events, evaluated operations, and required provenance growth without partial progress.\n- Add `PulseDelayConfig<D>` with `NonZeroSpan<D>`, dynamic `NodeKind::PulseDelay`, typed convenience, explicit keyed construction, and exact stable fixed-port construction.\n- Extend structural validation, temporal causality extraction, cycle validation, compilation descriptors, invariant checks, and full reaction settlement for PulseDelay's future scheduling and due-event emission paths.\n- Initialize PulseDelay with no pending work, then process initialization and ready pulse input through the same exact delay law.\n- Retain pending-group identity, owner, origin, deadline, count, revision, and result-resolvable causal contributors; remove fired groups and update the next schedule.\n- Add focused owned PulseDelay inspection covering its definition before initialization and its pending groups, observed revision/time, and next deadline when ready.\n- Extend the pre-stability version-1 network fingerprint projection with exact PulseDelay kind, port roles, delay, and temporal schema; synchronize authoritative projection text, affected contracts, and golden vectors in the accepted implementation change.\n- Add permanent temporal-foundation, ordered-calendar, and PulseDelay conformance verification, including direct-jump versus chronological stepwise equivalence.\n\n## Exclusions and non-goals\n\n- No TransportDelay, InertialDelay, Periodic, edge detector, latch, sample-hold, counter, temporal standard module, timeout, watchdog, or one-shot.\n- No topology patch, duration-change migration, cancellation policy beyond semantic removal when a PulseDelay group fires, or reconfiguration-time handling of due events.\n- No snapshot encoding, restoration, replay, execution/observable-state digest implementation, persistence decoder, or retained canceled-event history.\n- No generic `inspect_pending` framework, compiled inspection plans, subscriptions, observer delivery, or complete explanation/why-not renderer; the focused PulseDelay inspection is the bounded opening hook.\n- No forecast implementation, optimized heap/timing wheel/calendar, incremental dirty propagation, sparse transaction overlay, or performance-oriented event arena.\n- No same-time scheduled events, timer threads, wall-clock reads, sleeping, automatic time advancement, or hidden global schedulers.\n- Do not treat a PulseDelay's current input as affecting its current output, coalesce work across distinct logical deadlines, or give an equal-deadline batch semantic order.\n\n## Implementation obligations\n\n- Implement every requirement within the represented scope of the two item-26 reviewed contracts; keep their `known_uncovered` facets outside this bead.\n- Reuse and extend the existing authored graph, report model, dependency/cycle path, compiler, full evaluator, machine store, transaction staging, runtime policy, output event, provenance, and fingerprint architectures; do not create a parallel temporal machine or transaction system.\n- Represent pending work separately from reaction-scoped Pulse values and previous/proposed stored state.\n- Allocate public pending-event keys deterministically and monotonically within each machine history, never derive them from private storage positions, and preserve the allocation cursor across every failed transaction.\n- Enforce `deadline > current reaction time`; add `NonZeroSpan<D>` through checked arithmetic and reject `runtime.time_overflow` atomically.\n- Process all deadlines before a requested target in least-deadline order and all events at one deadline as one unordered batch. A due PulseDelay group emits before removal; new same-time input schedules only later work.\n- Preserve external input retention across internal deadline reactions and use empty reaction-scoped external Pulse input at those internal times.\n- Publish each due external pulse at its actual deadline in deterministic chronological order, with every CauseRef resolvable through the outer transaction result.\n- Ensure event creation, due emission/removal, schedule changes, PulseDelay inspection, time, outputs, and provenance are all part of the same candidate machine and single commit point.\n- Preserve complete failure atomicity for deadline overflow, pulse aggregation overflow, and each applicable budget failure, including event records, serial cursor, logical time, state, outputs, and provenance.\n\n## Allowed implementation freedom\n\n- Use the ordered reference map as the initial production calendar and choose the simplest deterministic record/batch representation; test and record the concrete choice.\n- Choose one deterministic stable-subject order for event-key allocation when one reaction creates several events; test insertion-order independence and record the choice without treating it as stabilized compatibility policy.\n- Keep equal-deadline PulseDelay groups separate or aggregate them through checked arithmetic, provided public keys and grouped causal facts retain complete observable meaning; choose, test, and record the simplest deterministic form.\n- Choose focused inspection type and accessor names consistent with existing Toggle inspection patterns.\n- Choose and record the exact pre-stability PulseDelay kind, port-role, delay, and temporal-schema projection spelling; no historical reader or migration is required before fingerprint stabilization.\n- Extend existing runtime failure evidence minimally where `runtime.time_overflow` and named budget exhaustion remain structured and contextual.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs` and `crates/mossignal/src/builder.rs` for PulseDelay configuration, fixed ports, and typed/dynamic construction.\n- `crates/mossignal/src/validation.rs` and dependency/cycle integration for exact shape and the temporal current-reaction barrier.\n- `crates/mossignal/src/compile.rs` for PulseDelay descriptors, due-event roots, future proposals, evaluation, and invariant checks.\n- `crates/mossignal/src/machine.rs` plus a focused temporal/calendar module if useful for event records, serial allocation, ordered batches, schedule access, and PulseDelay inspection.\n- `crates/mossignal/src/transaction.rs` for chronological internal reactions, event proposal/fire handling, atomic publication, schedule/result integration, provenance, and structured rejections.\n- `crates/mossignal/src/identity.rs` and the authoritative restricted fingerprint projection for exact PulseDelay identity and golden vectors.\n- `crates/mossignal/src/key.rs`, `crates/mossignal/src/lib.rs`, and public exports for PendingEventKey, Schedule, configuration, and inspection values.\n- Focused unit, integration, property/differential, and conformance tests.\n- The two reviewed item-26 contracts and `docs/specs/contracts/network-fingerprint.yaml` only as required by independent review or the accepted exact projection; implementation-discovered contract changes remain draft pending independent review.\n- `.beads/` when implementation acceptance synchronizes the bead.\n\n## Required verification\n\n1. Build equivalent external Pulse → PulseDelay → external Pulse networks through typed, exact-keyed, and dynamic paths; compare validation, causality, compilation, fingerprint, initialization, ready execution, inspection, and outputs.\n2. Verify fresh initialization has no pending work absent input; initial counts zero, one, two, and larger values create only exact future work and no instantaneous delayed output.\n3. Cover arbitrary nonzero initial times and non-unit delays, multiple origins, multiple PulseDelay nodes, fan-out, equal and unequal deadlines, and target times before, exactly at, and after pending deadlines.\n4. Verify direct advancement across several deadlines equals chronological stepwise advancement in final semantic state, schedules, public event facts, provenance, and output events at actual times.\n5. Permute definition, connection, input, event-record, and equal-deadline representation order and verify invariant semantics plus deterministic reporting and public event-key allocation.\n6. Verify due input at T emits regardless of new input at T, while that new input creates only work after T; distinguish the due-event root from the future-only current-input path in dependency and cycle tests.\n7. Verify `schedule()` and `next_deadline()` are read-only, agree on the least deadline, transition correctly between Dormant and WakeAt, ignore fired work, and fail structurally before initialization.\n8. Verify every pending group inspection exposes stable key, owner, origin, exact deadline, count, revision, cause, and next deadline; private storage or batch ordering never becomes public identity.\n9. Exercise checked time overflow, pulse aggregation overflow if aggregation is chosen, and each applicable runtime-policy boundary one below, exactly at, and one above; compare the complete machine before and after every failure.\n10. Verify chronological Pulsed output ordering, grouped origin/provenance resolution, no pulse persistence, and no independently visible internal commit during a direct jump.\n11. Verify fingerprint insertion-order invariance, delay sensitivity, distinction from Merge and other node kinds, exact port/temporal schema, and synchronized hand-reviewed golden bytes and digests.\n12. Retain the ordered calendar reference path and compare any alternative calendar logic against it; run focused tests, then `make check-dev` and `make check-final` at implementation acceptance.\n\n## Blocking questions\n\nNone. Calendar representation, simultaneous key-allocation order, equal-deadline group storage, focused inspection spelling, and the exact pre-stability projection are bounded deterministic implementation choices, not conflicting requirements, missing fundamental semantics, or frozen compatibility blockers.\n\n## Readiness\n\nContract and bead review accepted. This is a bounded implementation-ready roadmap-item-26 slice with no product or design blocker.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-28T15:29:48.345363Z","created_by":"mikkofinell","updated_at":"2026-07-28T16:12:22.662347Z","closed_at":"2026-07-28T16:12:22.662297Z","close_reason":"Implemented temporal event foundation and PulseDelay with chronological atomic execution, inspection, provenance, policy enforcement, canonical identity, and conformance tests","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["roadmap","runtime","temporal"],"dependencies":[{"issue_id":"ms-8o7","depends_on_id":"ms-v7j","type":"blocks","created_at":"2026-07-28T15:29:55.395063Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-am4","title":"Add typed ModuleBuilder foundation","description":"## Objective\\n\\nImplement Premium Continuation Roadmap item 34 as one bounded typed-authoring slice: add ModuleBuilder<D> for reusable user modules, reuse the accepted builder-scoped Signal<S> and primitive-construction pattern, lower into the existing UncheckedModule<D>, and finish only through ordinary module validation.\\n\\n## Roadmap provenance\\n\\n- Document: docs/premium_continuation_roadmap.md\\n- Item: 34\\n- Title: Typed ModuleBuilder foundation\\n\\nThis provenance records task sequencing only. Authoritative specifications and the referenced specification contracts govern implementation semantics.\\n\\n## Contract basis\\n\\n### Reviewed contract for this item\\n\\n- mossignal.authoring.typed_module_builder\\n  - docs/specs/contracts/typed-module-builder.yaml\\n  - status: reviewed\\n  - role: governs ModuleBuilder ownership, scoped typed signals, typed Level/Pulse module interfaces, duplicate and foreign identity handling, built-in primitive constructor parity, canonical unchecked lowering, and ordinary validation completion.\\n\\n### Reviewed contracts reused without modification\\n\\n- mossignal.authoring.typed_network_builder\\n  - docs/specs/contracts/typed-network-builder.yaml\\n  - role: supplies the accepted owning typed-builder, Signal<S>, explicit stable-key, structured foreign-signal, and completion pattern being adapted.\\n- mossignal.module.unchecked_definition\\n  - docs/specs/contracts/unchecked-module-definition.yaml\\n  - role: supplies the canonical owned dynamic module interface and private primitive graph representation into which typed authoring lowers.\\n- mossignal.module.validated_user_definition\\n  - docs/specs/contracts/validated-user-module-definition.yaml\\n  - role: supplies the sole finish-time validation and immutable ModuleDef boundary.\\n- mossignal.node.variadic_all\\n  - docs/specs/contracts/variadic-all.yaml\\n  - role: governs All typed/dynamic authoring, stable variadic ports, and semantics.\\n- mossignal.node.level_combinational_expansion\\n  - docs/specs/contracts/level-combinational-expansion.yaml\\n  - role: governs Any, Parity, AtLeast, and Select typed/dynamic constructor families and stable ports.\\n- mossignal.runtime.reaction_scoped_pulse_foundation\\n  - docs/specs/contracts/reaction-scoped-pulse-foundation.yaml\\n  - role: governs typed Pulse authoring boundaries.\\n- mossignal.node.pulse_merge, mossignal.node.toggle, and mossignal.node.pulse_delay\\n  - docs/specs/contracts/pulse-merge.yaml\\n  - docs/specs/contracts/toggle.yaml\\n  - docs/specs/contracts/pulse-delay.yaml\\n  - role: govern the existing Merge, Toggle, and PulseDelay typed/dynamic constructor forms and semantic configurations.\\n- mossignal.diagnostics.artifact_reports\\n  - docs/specs/contracts/artifact-reports.yaml\\n  - role: supplies artifact omission and report behavior for finish.\\n\\nThe reviewed primitive contracts have changed citations only in fingerprint or broad verification facets that this bead does not use. Their typed-authoring rules cite unchanged sources and are reused without reopening.\\n\\n## Included scope\\n\\n- Add deliberate public ModuleBuilder<D> construction for a caller-authored User module, with owned builder state, one private builder scope, and deterministic caller-local stable-key allocation.\\n- Add typed Level and Pulse module-input conveniences returning ModuleInputKey<S> plus Signal<S>.\\n- Add typed Level and Pulse module-output conveniences accepting only same-kind local Signal<S> and returning ModuleOutputKey<S>.\\n- Add the smallest consistent explicit stable-key forms for module inputs and outputs; reject immediately determinable duplicate interface identities through structured authoring failure and never overwrite an earlier claim.\\n- Mirror every built-in primitive constructor currently implemented on NetworkBuilder: Constant, Not, All, Any, Parity, AtLeast, Select, Merge, Toggle, and PulseDelay.\\n- Mirror the corresponding explicit NodeKey and stable port-key forms already present on NetworkBuilder, preserving AddedNode results, semantic configurations, stable variadic-port identity, source multiplicity, and ordinary structured failure behavior.\\n- Reject signals from NetworkBuilder, another ModuleBuilder, or any other foreign builder scope with AuthoringFailure::ForeignSignal before mutating the receiver.\\n- Lower the complete typed interface, private nodes, ports, connections, mappings, metadata, and explicit User origin into the canonical UncheckedModule<D> representation.\\n- Provide consuming into_unchecked and finish boundaries; finish must return Report<ModuleDef<D>, D> with behavior observationally equivalent to ordinary validation of the lowered UncheckedModule<D>.\\n- Add typed-versus-dynamic equivalence verification for representative Level-only, Pulse-only, and mixed modules, including stable keys, interface incidence, primitive configurations, deterministic inspection, diagnostics, and ModuleFingerprint after successful finish.\\n\\n## Exclusions and non-goals\\n\\n- No module instantiation in NetworkBuilder or ModuleBuilder, ModuleBindingSet, AddedModuleInstance, instance outputs, nested modules, hierarchy, or instance-qualified internal identities.\\n- No standard catalogue descriptors, ModuleOrigin::Standard, StandardModuleDeclaration, standard-module constructors, aliases, or concise standard conveniences.\\n- No primitives beyond the constructor family already public on NetworkBuilder at this baseline.\\n- No changes to primitive semantics, dynamic node definitions, module validation rules, reaction causality, diagnostic taxonomy, or ModuleFingerprint schema.\\n- No network compilation, module flattening, runtime execution, machine state, transactions, observation, explanation, persistence, restoration, replay, reconfiguration, or migration.\\n- No macro authoring language, branded lifetime API, callback nodes, compatibility shims, speculative generic builder framework, or placeholder future methods.\\n- No public ability to use a builder Signal<S> as a durable module binding, persistence, runtime, snapshot, or replay artifact.\\n\\nThe typed-module-builder contract records standard-module parity, not-yet-implemented built-in primitive parity, module instantiation, and signal annotations as known-uncovered future facets. None is required by this bead and none blocks it.\\n\\n## Implementation obligations\\n\\n- Implement every requirement in mossignal.authoring.typed_module_builder within its represented foundation scope.\\n- Preserve Signal<S> as an opaque Copy/Clone authoring handle carrying signal kind, one builder scope identity, and one stable source reference.\\n- Make Level/Pulse kind mismatches and reversed module-interface direction unrepresentable through the typed surface where practical; send remaining dynamic defects through the established validation report.\\n- Check builder ownership before any mutation for every operation consuming one or more signals, including fixed, variadic, stateful, temporal, and output constructors.\\n- Detect duplicate explicit ModuleInputKey, ModuleOutputKey, NodeKey, InPortKey, and OutPortKey identities deterministically without overwriting or silently merging prior authored state.\\n- Preserve the accepted NetworkBuilder constructor semantics for the mirrored primitive family, including distinct Constant identity, stable variadic input-port multiplicity, exact AtLeast threshold, Select branch roles, Toggle configuration, and PulseDelay configuration.\\n- Reuse or faithfully share the ordinary NodeDef<D>, NodeKind<D>, NodePorts, ConnectionDef, ModuleInterfaceMapping, DiagnosticMeta, key allocator, and AddedNode representation. Do not introduce a module-only primitive graph or evaluator.\\n- Ensure into_unchecked preserves all successfully authored claims and User origin in the established UncheckedModule<D> shape.\\n- Ensure finish is observationally equivalent to calling into_unchecked followed by ordinary validation, aside from consuming ownership once.\\n- Keep ModuleBuilder scoped signals distinct from NetworkBuilder scoped signals even if locally allocated stable source keys happen to match.\\n- Keep production code free of unwrap and expect. Any panic must state and enforce a proven private allocator invariant rather than respond to caller-controlled input.\\n\\n## Allowed implementation freedom\\n\\n- Extract private shared builder storage and constructor helpers, compose existing helpers, or implement a focused ModuleBuilder path. Choose the smallest design that avoids public API churn and duplicate semantic models.\\n- Choose the exact explicit module-interface method names and return shape consistent with existing add_* conventions; test and record the concrete choice without treating it as frozen compatibility policy.\\n- Choose the private builder-scope identity allocator and local key-allocation namespaces, provided builder isolation and deterministic authored results hold.\\n- Decide whether ModuleBuilder lives in builder.rs or a focused sibling module and how shared private lowering helpers are arranged.\\n- Reuse public AddedNode<O> or a strictly necessary equivalent only if the existing result cannot represent the exact stable node identity and typed outputs; do not add speculative result abstractions.\\n- Add task-focused private test helpers for typed/dynamic equivalence rather than a broad new public comparison API.\\n\\n## Expected change surface\\n\\n- crates/mossignal/src/builder.rs or a focused typed-module-builder sibling for ModuleBuilder, scoped interface authoring, primitive constructors, duplicate checks, and lowering.\\n- crates/mossignal/src/authored.rs only for narrowly required UncheckedModule construction/access compatibility.\\n- crates/mossignal/src/module.rs only for direct reuse of existing validation completion where needed.\\n- crates/mossignal/src/lib.rs for deliberate public exports.\\n- Focused unit or integration tests for module builder construction, failure atomicity, lowering, validation, and typed/dynamic equivalence.\\n- docs/specs/contracts/typed-module-builder.yaml only if implementation discovers a specification-backed correction; any such change returns it to draft until independent review.\\n- docs/premium_continuation_roadmap.md and .beads for task state only.\\n\\n## Required verification\\n\\n1. Construct empty, Level-only, Pulse-only, and mixed typed modules and compare their unchecked definitions with equivalent dynamic UncheckedModule<D> values.\\n2. Verify convenience allocation is deterministic within a fresh builder and creates distinct stable interface, node, and port identities without silent merging.\\n3. Verify explicit module input/output identities and every explicit primitive identity/port form are preserved exactly; each duplicate category returns structured failure without changing prior authored state.\\n4. Verify same-kind module inputs can fan out, variadic constructors preserve each stable input-port identity and duplicate-source incidence, and outputs map to the exact selected internal source.\\n5. For every signal-consuming method, supply a signal from another ModuleBuilder and from NetworkBuilder, assert AuthoringFailure::ForeignSignal, and verify the receiver remains unchanged.\\n6. Exercise Constant, Not, All, Any, Parity, AtLeast, Select, Merge, Toggle, and PulseDelay through convenience and explicit forms and compare nodes, ports, connections, mappings, configuration, metadata, and validation results with equivalent dynamic definitions.\\n7. Verify into_unchecked retains explicit User origin and all authored interface/internal claims and does not validate, compile, or execute them.\\n8. Verify finish and direct UncheckedModule validation agree on artifact presence, deterministic diagnostics, ModuleDef inspection, and ModuleFingerprint for equivalent definitions.\\n9. Add compile-time or signature-level checks sufficient to show Level/Pulse interface and constructor mismatches cannot be expressed through the typed API.\\n10. Verify no instantiation, standard catalogue, nested hierarchy, compilation, runtime, persistence, reconfiguration, macro, or new authoring-language surface is introduced.\\n11. Run focused Rust tests while iterating, then make check-dev. Final implementation acceptance additionally runs make check-final.\\n\\n## Dependencies\\n\\n- Roadmap item 33 / ms-tjg is closed and accepted.\\n- The canonical UncheckedModule<D>, ModuleDef<D>, ModuleFingerprint, complete current NetworkBuilder primitive family, stable key vocabulary, structured diagnostics, and module validation path exist in the accepted baseline.\\n\\n## Blocking questions\\n\\nNone. Exact explicit module-interface naming, private helper sharing, builder identity allocation, and local allocation namespaces permit several conforming designs. Select the simplest deterministic choices, test them, and record the concrete result for review.\\n\\n## Readiness\\n\\nThe task is bounded and has no product or design blocker. The governing contract is reviewed and this bead is ready for implementation.","notes":"Implementation close-out:\n- Added public ModuleBuilder<D> with typed Level/Pulse module interfaces, explicit interface identities, scoped Signal<S> handling, and User-origin unchecked lowering.\n- Reused a private NetworkBuilder authoring core for the accepted primitive family, converting private external-input surrogates into ordinary module interface mappings during lowering.\n- Mirrored Constant, Not, All, Any, Parity, AtLeast, Select, Merge, Toggle, and PulseDelay convenience and exact-key forms.\n- Chose independent deterministic module-interface allocation namespaces and explicit add_level_input/add_pulse_input/add_level_output/add_pulse_output forms; these remain implementation choices rather than frozen compatibility policy.\n- Added focused equivalence, primitive-family, duplicate, cross-builder failure-atomicity, validation, fingerprint, and compile-fail kind-safety checks.\n\nVerification:\n- focused builder tests: passed\n- cargo doc tests: passed\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-29T08:05:43.528021Z","created_by":"mikkofinell","updated_at":"2026-07-29T08:37:04.615890Z","closed_at":"2026-07-29T08:37:04.615846Z","close_reason":"Implemented and verified typed ModuleBuilder foundation.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["authoring","module","roadmap"],"dependencies":[{"issue_id":"ms-am4","depends_on_id":"ms-tjg","type":"blocks","created_at":"2026-07-29T08:05:57.470988Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-bnh","title":"Establish unchecked reusable module definitions","description":"## Objective\n\nImplement Premium Continuation Roadmap item 32 as one bounded authored-structure slice: establish the owned unchecked representation for reusable user-defined modules, reusing the already-implemented stable module key vocabulary and ordinary primitive graph records without claiming validation, semantic identity, instantiation, or execution.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 32\n- Title: Module interface identities and unchecked definitions\n\nThis provenance records task sequencing only. The roadmap is not product authority; the cited specification contracts govern implementation semantics.\n\n## Contract basis\n\n### Reviewed contract created for this item\n\n- `mossignal.module.unchecked_definition`\n  - `docs/specs/contracts/unchecked-module-definition.yaml`\n  - status: reviewed; independently accepted for reuse\n  - role: governs stable module key vocabulary, the owned `UncheckedModule<D>` lifecycle boundary, typed public interface and private module-local structure, explicit user origin, metadata separation, and lossless malformed-claim retention.\n\n### Reviewed contract reused unchanged\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed\n  - role: supplies the already accepted ordinary dynamic node, typed-port, connection, metadata, and lossless unchecked-claim semantics reused by module internals. Its module exclusion remains intact; module-specific behavior is represented only by the module contract.\n\nThe existing `ModuleInstanceKey`, `ModuleInputKey<S>`, `ModuleOutputKey<S>`, `AnyModuleInputKey`, `AnyModuleOutputKey`, conversions, allocator methods, and deterministic key tests already implement the relevant key facets. This bead reuses and verifies them rather than replacing or duplicating them.\n\n## Included scope\n\n- Add the deliberate public owned `UncheckedModule<D>` artifact for a reusable user-defined module.\n- Add the smallest owned public interface-claim records needed to retain typed Level and Pulse module inputs and outputs, their stable keys, metadata, and same-kind mapping to private internals.\n- Represent private module-local internals with the existing closed `NodeDef<D>`, `NodeKind<D>`, typed ports, `ConnectionDef`, stable keys, semantic configurations, and `DiagnosticMeta` rather than a second graph or callback model.\n- Keep internal node, port, and connection keys module-local; do not qualify them with `ModuleInstanceKey` before later network instantiation.\n- Retain an explicit User origin on the unchecked module without introducing a standard catalogue declaration.\n- Provide owned construction and read-only inspection sufficient to recover every supplied interface and internal claim exactly, including invalid and duplicate claims.\n- Preserve malformed interface maps and internal graph claims for later module validation instead of rejecting, repairing, defaulting, overwriting, or deduplicating them during construction.\n- Add focused deterministic construction, value/equality, key-ordering, ownership, metadata, and malformed-retention tests for the new artifact while retaining the existing module-key conformance tests.\n\n## Exclusions and non-goals\n\n- No module structural validation, reaction-dependency validation, cycle validation, diagnostics, `ModuleDef<D>`, `ModuleFingerprint`, or module semantic fingerprinting.\n- No typed `ModuleBuilder<D>`, builder-scoped module signals, builder convenience, or changes to `NetworkBuilder<D>`.\n- No module instantiation, binding modules into networks, `AddedModuleInstance`, instance-qualified internal identity, hierarchy retention, or nested modules.\n- No compilation, flattening, evaluation, machine state, transactions, observation, inspection framework, or explanation support.\n- No standard catalogue descriptors, standard declarations, `ModuleOrigin::Standard`, canonical standard expansion, aliases, or conveniences.\n- No persistence envelope or encoding, restoration, replay, compatibility migration, topology patch, or reconfiguration.\n- Do not add placeholder validation methods, empty executable artifacts, compatibility shims, callback nodes, or speculative abstractions for later roadmap items.\n\n## Implementation obligations\n\n- Honor `stable-module-key-vocabulary`, `durable-and-local-key-construction`, and `erased-interface-keys-retain-kind` by reusing the existing module key types and conversion/allocation behavior; change them only if a concrete conformance defect is found.\n- Implement `owned-unchecked-module-artifact` as an owned public value that cannot be mistaken for a validated or executable module.\n- Implement `typed-interface-and-private-structure` so public interface direction and signal kind remain explicit and each exported source or internal input mapping retains enough category information for later validation.\n- Implement `ordinary-stable-keyed-primitive-internals` without duplicating primitive definitions or introducing an alternate semantic graph.\n- Implement `module-local-internal-identity`; no instance key or dense position participates in reusable-definition internal identity.\n- Implement `explicit-user-origin` without inferring origin from graph shape, names, or metadata and without pulling standard-module declarations into this slice.\n- Implement `malformed-claims-remain-observable` with lossless collections that preserve every caller-supplied claim and remain safely constructible without validation or panic.\n- Implement `metadata-is-not-structural-identity`; metadata remains owned and inspectable but never substitutes for stable interface or internal keys.\n- Keep all production code free of `unwrap` and `expect`; ordinary caller-controlled malformed module data must not panic.\n\n## Allowed implementation freedom\n\n- Choose the supporting interface-record names, constructor grouping, accessor names, and Rust module placement consistent with the existing public authored-definition style.\n- Reuse a private common authored-graph container or compose the established record types directly. Choose the simplest design that remains lossless and does not force `UncheckedNetwork<D>` to pretend that module interfaces are application external endpoints.\n- Choose the smallest explicit representation for User origin needed by this slice. Do not add placeholder standard declarations solely to complete the future `ModuleOrigin<D>` surface early.\n- Choose the simplest deterministic raw claim order and structural equality behavior, test it, and record the choice. This is not yet a semantic fingerprint, canonical persistence order, or compatibility promise.\n- Add narrowly useful consuming `into_parts` accessors when they avoid cloning; do not speculate about later validated graph-view APIs.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs` or one focused sibling module for `UncheckedModule<D>` and supporting owned interface records.\n- `crates/mossignal/src/lib.rs` for deliberate public exports if the existing module path alone is insufficient.\n- `crates/mossignal/src/key.rs` only for a concrete defect or narrowly missing test in the already-implemented module key surface.\n- Focused unit or integration tests for module construction, inspection, ownership, stable kinds, duplicate/malformed retention, equality, and deterministic ordering.\n- `docs/specs/contracts/unchecked-module-definition.yaml` only for implementation-discovered reusable specification-backed corrections; any implementation change remains draft pending independent review.\n- `.beads/` when the accepted implementation closes and synchronizes this bead.\n\n## Required verification\n\n1. Confirm the existing module-instance, typed module-input/output, erased module-input/output, conversion, hashing, ordering, and allocator tests continue to pass for both Level and Pulse.\n2. Construct a representative user module with mixed Level and Pulse public interfaces and ordinary primitive internals; verify every stable key, kind, mapping, node, port, connection, configuration, metadata value, and User origin is recoverable exactly.\n3. Construct empty and nonempty unchecked modules and verify owned construction, cloning or consuming decomposition as selected, and deterministic structural equality behavior without adding unnecessary bounds on `D`.\n4. Supply duplicate public interface keys, duplicate internal keys, dangling internal references, wrong-kind mappings, invalid directions, incomplete mappings, and malformed interfaces through safe public construction; verify every claim remains separately observable and no construction path panics or silently repairs it.\n5. Verify Level and Pulse interface keys cannot be interchanged through typed APIs, while erased forms retain the actual kind and wrong-kind conversion returns `KeyKindMismatch`.\n6. Verify module-local internal keys are stored without a `ModuleInstanceKey` qualifier and equal numeric payloads in different key categories remain distinct.\n7. Verify metadata, names, paths, and caller order do not replace stable structural identity. Exercise the selected raw-order/equality choice under construction permutations and record the concrete behavior without treating it as semantic module identity.\n8. Verify no module validation, `ModuleDef`, fingerprint, builder, instantiation, compilation, standard catalogue, persistence, or reconfiguration API is introduced by this slice.\n9. Run focused Rust tests while iterating, then `make check-dev`; final implementation acceptance additionally runs `make check-final`.\n\n## Dependencies\n\n- Premium Foundation Roadmap item 26 (`ms-8o7`) is closed and accepted.\n- The existing stable module key surface and ordinary authored primitive records are present in the accepted baseline.\n\n## Blocking questions\n\nNone. Supporting record spelling, internal collection composition, User-origin storage, consuming accessor shape, and raw claim order/equality are bounded deterministic implementation choices. They do not present conflicting requirements, missing fundamental semantics, or a frozen compatibility promise.\n\n## Readiness\n\nThe task is bounded, has no product or design blocker, and is implementation-ready. Independent review promoted the module contract and accepted this bead.","notes":"Implementation close-out:\n- Added public unchecked user-module declarations and raw interface mapping claims composed from existing module keys, authored node records, connection records, and endpoints.\n- Retained interface declarations, mappings, nodes, and connections as caller-ordered vectors so duplicates, omissions, dangling references, kind mismatches, and direction-invalid claims remain observable.\n- Chose caller-order-sensitive raw structural equality including metadata; this is explicitly not semantic module identity or a persistence-order promise.\n- Kept reusable-definition internal identities as direct module-local node, port, and connection keys without ModuleInstanceKey qualification.\n- Removed unnecessary marker-domain trait bounds from authored node/config value traits so UncheckedModule<D> supports Clone, Debug, and Eq for trait-hostile D.\n\nVerification:\n- focused authored module tests: passed\n- stable module key conformance tests: passed\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-29T05:50:45.878820Z","created_by":"mikkofinell","updated_at":"2026-07-29T06:14:59.903166Z","closed_at":"2026-07-29T06:14:29.051015Z","close_reason":"Implemented and verified unchecked reusable user-module definitions.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["authoring","module","roadmap"],"dependencies":[{"issue_id":"ms-bnh","depends_on_id":"ms-8o7","type":"blocks","created_at":"2026-07-29T05:50:45.878820Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-d1f","title":"Implement initial compiled representation","description":"## Objective\n\nImplement Premium Foundation Roadmap item 13 for the restricted level-only Constant/Not model: compile a successfully validated network into an opaque immutable `CompiledNetwork<D>` that resolves stable authored structure into revision-local dense execution topology while retaining the validated semantic identity unchanged.\n\n## Contract basis\n\n- `mossignal.compile.compiled_network`\n  - `docs/specs/contracts/compiled-network.yaml`\n  - status: reviewed, independently accepted in this review\n  - role: validated-only compilation, immutable topology boundary, category-safe dense resolution, restricted descriptors and adjacency, invariant checks, and deterministic compilation.\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed, used unchanged\n  - role: authoritative stable-keyed restricted authored structure and retained diagnostic metadata.\n\n- `mossignal.graph.current_reaction_cycle_validation`\n  - `docs/specs/contracts/current-reaction-cycle-validation.yaml`\n  - status: reviewed, used unchanged\n  - role: only acyclic restricted definitions become `ValidatedNetwork`; its deterministic order becomes compiled metadata.\n\n- `mossignal.graph.current_reaction_dependency_graph`\n  - `docs/specs/contracts/current-reaction-dependency-graph.yaml`\n  - status: reviewed, evidence refreshed in this review\n  - role: the stable-subject current-reaction relation that compiled operation descriptors and adjacency must preserve.\n\n- `mossignal.identity.network_fingerprint`, `mossignal.identity.input_schema_fingerprint`, and `mossignal.identity.time_domain`\n  - `docs/specs/contracts/network-fingerprint.yaml`, `docs/specs/contracts/input-schema-fingerprint.yaml`, and `docs/specs/contracts/time-domain-identity.yaml`\n  - status: reviewed, used unchanged\n  - role: validated identities that compilation carries unchanged and must not derive from dense layout.\n\n- `mossignal.diagnostics.artifact_reports`\n  - `docs/specs/contracts/artifact-reports.yaml`\n  - status: reviewed, used unchanged\n  - role: the common `Report` artifact boundary for public compilation forms.\n\n## Included scope\n\n- Introduce opaque `CompiledNetwork<D>` and consuming and borrowing compilation forms from `ValidatedNetwork<D>`.\n- Resolve restricted stable node, port, external-endpoint, and reaction-operation subjects to distinct private opaque dense index types.\n- Compile immutable Constant/Not node and typed-port descriptors, unambiguous stable-key lookup, complete external endpoint tables, immutable reaction predecessor/successor adjacency, and the validated deterministic topological order.\n- Retain `NetworkKey`, `TimeDomainId`, `NetworkFingerprint`, `InputSchemaFingerprint`, and required authored metadata unchanged.\n- Add test/debug-only compiled invariant checking and focused deterministic compilation tests.\n\n## Exclusions and non-goals\n\n- Do not implement evaluator execution, machine state, lifecycle, runtime policy, input artifacts, transactions, dirty propagation, or event storage.\n- Do not implement incremental compilation, dense public handles, resolved handles, the future public graph-query API (while retaining the stable authored structure and metadata it will require), regions, state-family layouts, temporal descriptors, modules, pulse execution, persistence, snapshots, replay, or topology patches.\n- Do not change validation, cycle analysis, canonical fingerprint construction, or add new public identity-binding stages.\n- Do not expose dense indices, adjacency storage, arenas, worklists, or raw category-interchangeable integers in the public API.\n\n## Implementation obligations\n\n- Compile only from an opaque successfully validated network and preserve the common `Report` behavior.\n- Make `CompiledNetwork<D>` immutable and suitable for later cheap sharing by independent machines, with no mutable execution state.\n- Preserve validated `NetworkKey`, `TimeDomainId`, `NetworkFingerprint`, and `InputSchemaFingerprint` exactly; compilation accepts no identity context and dense layout must not affect them.\n- Keep stable keys and dense indices separate. Dense node, port, operation, and endpoint positions are revision-local and must use distinct opaque private types.\n- Compile the restricted immutable descriptors, stable-key lookup, endpoint tables, reaction adjacency, and deterministic topological order required by the subsequent evaluator task.\n- Provide a compiled invariant checker that can establish the represented bounds, kind, incidence, adjacency-order, lookup, and endpoint-table invariants in debug and test builds. Internal invariant failures follow the existing internal-defect discipline rather than caller-input failures.\n- Ensure semantically equivalent insertion permutations produce equivalent externally observable compiled metadata and reaction relations; permitted dense-layout variation cannot affect behavior, diagnostics, persistence, or digests.\n\n## Allowed implementation freedom\n\n- Exact dense index widths, private table and descriptor layouts, adjacency encoding, compilation helper names, and immutable sharing mechanism are free.\n- Use the simplest deterministic restricted layout; compactness, state/temporal layouts, regions, graph indices, and incremental compilation are deferred.\n- Retaining versus rebuilding the validated definition internally is free if the immutable compiled boundary and requirements hold.\n\n## Expected change surface\n\n- `crates/mossignal/src/compile.rs` or a focused private compiled-topology module\n- `crates/mossignal/src/validation.rs` for `ValidatedNetwork` compilation entry points\n- `crates/mossignal/src/lib.rs` for the deliberate `CompiledNetwork` public boundary\n- focused unit tests for descriptors, lookup, adjacency, invariant checks, and construction-order invariance\n- `docs/specs/contracts/compiled-network.yaml` only for implementation-discovered reusable facts; any change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n1. Valid restricted Constant/Not definitions compile through both consuming and borrowing forms; unchecked or blocking-invalid definitions cannot produce a compiled artifact.\n2. Compiled topology retains the validated network key, time-domain identity, network fingerprint, and input-schema fingerprint exactly.\n3. Stable-key lookup is total for every represented compiled subject and unambiguous; each result has the correct distinct dense category.\n4. Node and port descriptors preserve the restricted validated kind and signal-kind facts, and external endpoint tables are complete and type-correct.\n5. Every compiled reaction dependency is in bounds, appears in immutable adjacency, and advances in the retained deterministic topological order.\n6. The invariant checker detects seeded violations of each represented compiled invariant without treating them as caller-controlled validation findings.\n7. Permuting semantically equivalent authored claim insertion order preserves externally observable compiled metadata and reaction relation; private dense allocation differences do not alter fingerprints.\n8. Run focused tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Dependencies\n\n- Roadmap item 12 / `ms-vbt` is complete and accepted.\n- This bead establishes the topology boundary consumed by roadmap item 14's full reference evaluator and later machine work.\n\n## Readiness\n\nReady for implementation. No unresolved product question blocks the bounded restricted slice; layout choices remain deterministic implementation freedom.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T18:48:48.868932Z","created_by":"mikkofinell","updated_at":"2026-07-27T18:59:17.338619Z","closed_at":"2026-07-27T18:59:17.338570Z","close_reason":"Implemented immutable restricted compiled topology","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-d1f","depends_on_id":"ms-vbt","type":"blocks","created_at":"2026-07-27T18:48:52.322593Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-f2d","title":"Validate restricted current-reaction cycles and ordering","description":"## Objective\n\nImplement Premium Foundation Roadmap item 11 for the restricted Constant/Not graph: analyze the derived current-reaction dependency graph with SCCs, reject cycles with structured diagnostics, select deterministic topological order for acyclic graphs, and create the opaque ValidatedNetwork only on complete success.\n\n## Contract basis\n\n- `mossignal.graph.current_reaction_dependency_graph`\n  - `docs/specs/contracts/current-reaction-dependency-graph.yaml`\n  - status: reviewed, used unchanged\n  - role: deterministic private graph, stable authored correspondence, and restricted Constant/Not signatures consumed by cycle analysis.\n\n- `mossignal.graph.structural_validation`\n  - `docs/specs/contracts/structural-validation.yaml`\n  - status: reviewed, used unchanged\n  - role: structurally usable private candidate and blocking-report boundary before semantic validation.\n\n- `mossignal.graph.current_reaction_cycle_validation`\n  - `docs/specs/contracts/current-reaction-cycle-validation.yaml`\n  - status: reviewed, accepted with the authoritative cycle diagnostic and verification rules\n  - role: SCC criterion, acyclicity lifecycle boundary, one-diagnostic-per-SCC identity, canonical witness, deterministic topological metadata, and verification.\n\n- `mossignal.diagnostics.problem_model`, `mossignal.diagnostics.collections`, and `mossignal.diagnostics.artifact_reports`\n  - reviewed, used unchanged\n  - role: typed code-backed diagnostic construction, canonical identity and ordering, and artifact omission for blocking findings.\n\n## Included scope\n\n- Run SCC decomposition over the private restricted reaction dependency graph and detect cyclic multi-vertex SCCs and self-loops.\n- Emit exactly one `validation.current_reaction_cycle` per cyclic SCC, using the network as primary subject and the canonical stable reaction-member set as its discriminator.\n- Construct the specified canonical witness without making it condition identity.\n- Select a deterministic topological linear extension for an acyclic graph, using stable ordering for ties.\n- Complete validation for the restricted model and introduce an opaque `ValidatedNetwork<D>` only when structural and current-reaction semantic validation both succeed.\n- Add focused graph-invariant, cycle-witness, self-loop, multiple-SCC, and insertion-order-invariance tests.\n\n## Exclusions and non-goals\n\n- Do not rederive structural validation or reaction dependencies.\n- Do not add dense runtime indices, compiled descriptors, evaluator execution, machine state, regions, persistence, modules, temporal nodes, or stateful node signatures.\n- Do not add a CompiledNetwork or expose compilation behavior.\n\n## Implementation obligations\n\n- Use stable reaction-member references derived from authored stable identity plus semantic reaction role; exclude private vertex IDs, dense indices, traversal order, insertion order, and witness choice.\n- A singleton SCC uses its least self-loop step as witness; a multi-member SCC uses its least non-self step plus the canonically tie-broken shortest return path.\n- Merge duplicate detection idempotently: members agree with the discriminator, canonical witness and scalar evidence agree exactly, and set-like evidence unions canonically.\n- Reject every current-reaction cycle before producing ValidatedNetwork; do not use fixed-point iteration, same-time microsteps, or evaluation order as cycle resolution.\n- Keep ValidatedNetwork opaque and constructible only on successful restricted validation.\n\n## Allowed implementation freedom\n\n- Tarjan, Kosaraju, or another established SCC algorithm; private graph storage, witness search, and topological-sort implementation are free.\n- Deterministic topological metadata may remain private or be retained in the opaque validated artifact; no dense compiled layout is authorized.\n\n## Expected change surface\n\n- `crates/mossignal/src/validation.rs`\n- `crates/mossignal/src/diagnostics.rs`\n- `crates/mossignal/src/lib.rs` for the deliberate validated-network public boundary\n- focused unit tests alongside validation and diagnostics\n- `docs/specs/contracts/current-reaction-cycle-validation.yaml` only for implementation-discovered reusable facts; any change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n- One self-loop SCC produces one diagnostic.\n- One multi-node SCC with several cycles produces one diagnostic; two disjoint cyclic SCCs produce two.\n- A multi-node SCC with self-loops remains one SCC diagnostic.\n- Witnesses are closed, use real dependencies, and remain within their reported SCC.\n- Duplicate detection is idempotent, and diagnostic identity and witness are invariant under definition, adjacency, hash, and SCC-traversal permutations.\n- A valid DAG yields deterministic topological order and may construct ValidatedNetwork; cyclic SCCs cannot.\n- `make check-dev`, then `make check-final` at implementation acceptance.\n\n## Readiness\n\nReady for implementation: independent review accepted the authoritative documentation amendment, the reviewed cycle-validation contract, and this bounded bead.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T13:35:37.306690Z","created_by":"mikkofinell","updated_at":"2026-07-27T15:32:17.306202Z","closed_at":"2026-07-27T15:32:17.306155Z","close_reason":"Implemented and verified restricted current-reaction cycle validation.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-hnz","title":"Implement minimal validated runtime policy and identity","description":"## Objective\n\nImplement Premium Foundation Roadmap item 16: establish the immutable validated RuntimePolicy and deterministic RuntimePolicyId foundation needed before machine spawning and transaction execution. Represent the complete initial five-limit public policy schema, but do not yet add machine state or budget accounting.\n\n## Contract basis\n\n- `mossignal.runtime.policy`\n  - `docs/specs/contracts/runtime-policy.yaml`\n  - status: reviewed, independently accepted for reuse\n  - role: governs policy construction, the five initial semantic limits, validation, PolicyFailure mapping, opaque canonical identity, future machine association, budget semantics, and verification.\n\n- `mossignal.diagnostics.problem_model`\n  - `docs/specs/contracts/diagnostic-problem-model.yaml`\n  - status: reviewed, used unchanged\n  - role: contextual catalogue-backed code, evidence, classification, and leaf-failure mapping rules for future complete PolicyFailure integration.\n\n- `mossignal.runtime.restricted_full_topological_evaluator`\n  - `docs/specs/contracts/restricted-full-topological-evaluator.yaml`\n  - status: reviewed, used unchanged\n  - role: contextual current Constant/Not execution boundary; this task does not change or budget that evaluator.\n\n## Included scope\n\n- Add public immutable RuntimePolicy, RuntimePolicyBuilder, RuntimePolicyId, and a focused PolicyFailure construction error.\n- Represent maximum internal reactions, maximum evaluated operations, maximum pending events, maximum events created per transaction, and maximum required provenance growth as the complete initial u64 policy schema.\n- Require every one of those fields on the plain builder before build succeeds.\n- Expose read-only inspection of the exact limits and policy identifier.\n- Compute RuntimePolicyId with the version-1 canonical digest record, unkeyed BLAKE3-256, and domain mossignal/runtime_policy_id/v1.\n- Include every initial named semantic limit in canonical policy identity independently of setter call order.\n- Add focused construction, validation, value-trait, identity-sensitivity, order-invariance, and golden-vector tests.\n\n## Exclusions and non-goals\n\n- Do not implement Machine, spawn, lifecycle state, initialization, transactions, InputDelta, output events, rollback, cloning, or transaction staging.\n- Do not enforce or account budgets in the existing full evaluator or any unsupported stateful, temporal, event, provenance, migration, reconfiguration, persistence, or replay subsystem.\n- Do not add runtime-policy persistence artifacts, encoding or decoding APIs, restoration, replay matching, policy rebinding, broad performance tuning, named preset policies, or dynamic mutation.\n- Do not expand the full diagnostic kernel merely to materialize catalogue Problem records for this bounded builder; preserve the semantic PolicyFailure categories for later integration.\n- Do not change network, input-schema, or time-domain identity and do not combine RuntimePolicyId with those values.\n\n## Implementation obligations\n\n- Respect runtime.policy rules explicit-immutable-validated-policy, initial-semantic-limits, complete-builder-validation, policy-failure-model, distinct-opaque-identity, canonical-policy-identity, and policy-identity-verification.\n- Keep RuntimePolicy immutable after successful construction and provide no process-global fallback.\n- Keep RuntimePolicyId a distinct opaque 32-byte value with stable hexadecimal formatting and ordinary value traits consistent with the existing identity family.\n- Use one deterministic version-1 canonical payload of named limits; setter order, private storage, hash iteration, and debug text must not affect its bytes or digest.\n- Keep missing-field and invalid-value failure categories distinct. The current u64 schema has no specification-defined narrower numeric domain, so choose the simplest deterministic validation rule, test it, and record the decision without claiming a stabilized compatibility promise.\n- Retain the expected 32-byte result for at least one deliberately versioned representative golden policy vector.\n\n## Allowed implementation freedom\n\n- Private policy and builder storage, consuming-setter details, accessor spelling, module placement, digest caching, and reuse or extraction of the existing private canonical writer are free.\n- The exact version-1 payload record nesting and named-field spelling are not fixed beyond canonical named limits and construction-order independence. Choose the simplest deterministic projection, test it with a golden vector, and record it for concrete review.\n- If all u64 values are accepted for the initial limits, runtime.policy_invalid_limit may remain reserved and unconstructible in this slice; missing required limits must still fail precisely.\n- A small dedicated PolicyFailure enum is sufficient until the complete catalogue-backed diagnostic surface is implemented, provided its categories preserve the required code mapping.\n\n## Expected change surface\n\n- a focused runtime-policy module under `crates/mossignal/src/`\n- `crates/mossignal/src/identity.rs` only if shared opaque digest or canonical-writer support is reused or minimally extracted\n- `crates/mossignal/src/lib.rs` for deliberate public policy exports\n- focused unit tests and a versioned RuntimePolicyId golden vector\n- `docs/specs/contracts/runtime-policy.yaml` only for implementation-discovered reusable specification facts; any implementation-time change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n1. Building succeeds when every initial limit is supplied and the resulting values are inspectable and immutable.\n2. Omitting each required field in turn yields the missing-limit PolicyFailure category identifying that field.\n3. Exercise the chosen numeric-domain rule at its relevant boundaries; if the full u64 domain is accepted, verify both zero and u64::MAX.\n4. Equal policies constructed through different setter orders produce equal canonical bytes and RuntimePolicyId values.\n5. Changing each of the five limits independently changes RuntimePolicyId.\n6. Assert RuntimePolicyId remains distinct from NetworkFingerprint and InputSchemaFingerprint at the type boundary, provides the recommended ordinary value traits, and uses a stable hexadecimal Display representation.\n7. Retain and verify a deliberately versioned representative golden policy vector and its expected BLAKE3-256 RuntimePolicyId.\n8. Verify no global default, dynamic mutation, machine creation, budget enforcement, or persistence API is introduced.\n9. Run focused tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Acceptance Criteria\n\n- Public construction yields an immutable fully specified five-limit RuntimePolicy or a precise missing/invalid PolicyFailure.\n- RuntimePolicyId is a deterministic opaque version-1 canonical identity sensitive to every represented limit and independent of builder order and private representation.\n- Focused validation and golden identity tests pass, and no machine or budget-accounting behavior is implemented.\n\n## Dependencies\n\n- Roadmap item 15 / `ms-771` is complete; this policy task is independently implementable on the accepted restricted compiled and input-artifact foundations.\n- Roadmap item 17 will consume RuntimePolicy through explicit machine spawning and store RuntimePolicyId.\n- Later transaction tasks own budget accounting, boundary enforcement, and atomic budget failure.\n\n## Readiness\n\nReady for implementation against the independently reviewed runtime-policy contract. No conflict, missing fundamental semantic rule, or frozen compatibility promise blocks this bounded slice. Numeric-domain and exact private canonical-payload choices are ordinary deterministic implementation freedom and must be tested and recorded.","notes":"Implementation close-out:\n- Accepted the complete u64 domain for all five initial limits; runtime.policy_invalid_limit remains a reserved PolicyFailure category.\n- Chose a version-1 canonical payload with the five public snake_case field names in lexicographic order inside the required domain/payload/version record.\n- Golden RuntimePolicyId for limits 1,2,3,4,5: 62e410cd29ddd5ddfae4888f648ad290d5c9633f25e7b35c2ff727e4f9ffa04b.\n\nVerification:\n- focused runtime-policy unit tests: passed\n- RuntimePolicyId compile-fail doctests: passed\n- make check-dev: passed\n- make check-final: passed\n\nCorrective acceptance review:\n- Reviewed against accepted implementation HEAD 0a7ef5b.\n- Preserved future catalogue evidence extensibility with non-exhaustive limit and failure shapes.\n- Removed the unnecessary public stable parameter-key promise and completed public policy API documentation.\n\nReview verification:\n- focused runtime-policy unit tests: passed\n- public and compile-fail doctests: passed\n- warning-free rustdoc: passed\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"task","assignee":"mikkofinell","created_at":"2026-07-27T19:52:28.782723Z","created_by":"mikkofinell","updated_at":"2026-07-27T20:24:53.106640Z","closed_at":"2026-07-27T20:05:20.120794Z","close_reason":"Implemented and verified immutable runtime policy construction and canonical identity.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-hnz","depends_on_id":"ms-771","type":"blocks","created_at":"2026-07-27T19:52:28.782723Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-ipi","title":"Implement level InputDelta and builder","description":"## Objective\n\nImplement Premium Foundation Roadmap item 19: add the owned level-only InputDelta<D> artifact and ordinary current-topology InputDeltaBuilder<D>. Bind each completed delta to the exact compiled network and input schema, retain only explicitly supplied external level observations, and preserve omission as later retention of the ready machines prior authoritative value. Stop before transaction execution.\n\n## Contract basis\n\n- `mossignal.runtime.level_input_delta`\n  - `docs/specs/contracts/level-input-delta.yaml`\n  - status: reviewed\n  - role: governs the owned opaque delta, current-topology builder, identity binding, explicit observations, omission semantics, deterministic construction, invalid-observation rejection, and lifecycle relationship.\n- `mossignal.identity.input_schema_fingerprint`\n  - `docs/specs/contracts/input-schema-fingerprint.yaml`\n  - status: reviewed and used unchanged\n  - required facet: `future-input-binding` keeps schema identity distinct from network identity and binds observations by stable endpoint identity.\n- `mossignal.compile.compiled_network`\n  - `docs/specs/contracts/compiled-network.yaml`\n  - status: reviewed and used unchanged\n  - required facets: `immutable-topology-boundary`, `validated-identity-retention`, and `opaque-dense-private-surface` supply the exact immutable builder context without exposing dense layout.\n- `mossignal.runtime.level_input_snapshot`\n  - `docs/specs/contracts/level-input-snapshot.yaml`\n  - status: reviewed and used unchanged as contextual established pattern\n  - required facet: snapshots and deltas share owned exact-bound artifact conventions but remain distinct, non-interchangeable public types.\n\nThe delta contract intentionally leaves prepared-patch target deltas in `known_uncovered`; no target revision, establish obligation, or removed-input behavior is required here.\n\n## Included scope\n\n- Add public opaque `InputDelta<D>` and `InputDeltaBuilder<D>` types for the restricted level-only model.\n- Add `CompiledNetwork<D>::input_delta()` as the ordinary builder origin for the exact current compiled topology.\n- Retain the exact NetworkKey, NetworkFingerprint, and InputSchemaFingerprint separately in each completed delta.\n- Add explicit `set(ExternalInputKey<Level>, LogicLevel)` construction for existing external level inputs.\n- Permit empty, partial, and complete deltas; omission records no replacement value and will mean retention when later ready-machine execution consumes the artifact.\n- Permit explicit reassertion of either level value.\n- Reject an endpoint outside the bound external level-input schema.\n- Reject repeated observations for one endpoint and preserve the existing structured distinction between an equivalent duplicate and a conflicting value.\n- Preserve equivalent semantic behavior independent of builder insertion order without promising a public iteration order.\n- Export the deliberate public types and document the identity, lifecycle, and omission boundaries.\n\n## Explicit exclusions and non-goals\n\n- No `Transaction::advance`, ready-machine `Machine::apply`, time advancement, evaluator invocation, external-level mutation, output events, provenance, staging, rollback, or lifecycle rejection implementation.\n- No pulse observations, PulseCount storage, pulse input builder method, or pulse transaction integration.\n- No prepared patch, target schema, `establish`, newly introduced or removed endpoint behavior, target revision binding, reconfiguration, or topology migration.\n- No binding projection, caller-object-model interpretation, ambiguous observations, or ergonomic bound-machine facade.\n- No persistence artifact encoding or decoding, replay, restoration, forecast, state digests, or serialization derives.\n- Do not merge InputSnapshot and InputDelta or make them interchangeable.\n- Do not change reviewed contracts unless implementation discovers a reusable specification-backed correction; any changed contract remains draft and requires independent review.\n\n## Implementation obligations\n\n- Implement `compiled-builder-origin`, `owned-opaque-network-bound-artifact`, `exact-current-schema-binding`, `explicit-level-observations`, `omission-means-retention`, `rejects-invalid-observations`, and `deterministic-unordered-batch` from `mossignal.runtime.level_input_delta` within this beads restricted construction boundary.\n- Obtain identity and endpoint membership from CompiledNetwork rather than accepting caller-supplied identity tokens or collection positions.\n- Reuse or deliberately extend the existing InputBuildFailure family so unknown, duplicate, and conflicting observations remain structured and comparable; do not add unreachable failure variants merely for future systems.\n- Keep snapshot completeness behavior unchanged. InputDelta::finish must not require omitted existing levels or infer them as Low.\n- Keep dense compiled indices, authored insertion order, and diagnostic metadata from affecting delta semantics.\n- Preserve generic trait behavior without adding unrelated bounds on D.\n\n## Allowed implementation freedom\n\n- Private storage, module placement, accessor details, helper names, and whether snapshot and delta builders share private helpers are free when the public artifact boundaries remain distinct.\n- Choose the simplest deterministic stable-keyed representation, test insertion-order-independent semantic behavior, and record the concrete choice in the implementation close-out without making iteration order or a particular equality implementation a permanent public promise.\n- The current-topology builder need not expose `establish` or a caller-supplied NetworkRevision. Exact machine revision belongs to the later transaction, while target revision context belongs to future prepared-patch deltas.\n- Derives beyond the semantic and testing needs of this slice are implementation choices; do not add persistence or broad compatibility promises.\n\n## Expected change surface\n\n- `crates/mossignal/src/input.rs` for the delta artifact, builder, construction failures, and focused tests.\n- `crates/mossignal/src/compile.rs` for the builder entry point and exact schema membership.\n- `crates/mossignal/src/lib.rs` for deliberate exports.\n- `docs/specs/contracts/level-input-delta.yaml` only for implementation-discovered reusable specification-backed corrections; any later change requires independent review.\n- `.beads/` when implementation acceptance closes and synchronizes the bead.\n\n## Required verification\n\n1. Build empty, single-input, partial, complete, and explicit reassertion deltas and verify exact network and schema identity retention.\n2. Verify omission remains absence from the delta rather than an inferred Low or a finish-time completeness error.\n3. Verify unknown external level inputs fail structurally.\n4. Verify repeated equal observations report duplicate input and repeated unequal observations report conflict, preserving the first and second values where represented.\n5. Build equivalent deltas through different insertion orders and verify equivalent semantic observations without requiring a public iteration-order guarantee.\n6. Verify deltas from networks with foreign network identity or schema remain distinguishable and cannot be positionally rebound.\n7. Verify InputSnapshot completeness and construction behavior remain unchanged and the two artifacts are distinct public types.\n8. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Acceptance criteria\n\n- The restricted CompiledNetwork creates owned level-only delta builders bound to its exact stable identities and endpoint schema.\n- Completed deltas retain exactly the explicit valid observations, permit omission without defaulting or completeness rejection, and remain deterministic across construction order.\n- Unknown, duplicate, and conflicting observations produce structured failures without corrupting the builder boundary or altering snapshot behavior.\n- No transaction execution, pulse, patch, projection, or persistence subsystem is introduced.\n- Focused verification and both repository gates pass.\n\n## Dependencies and readiness\n\nRoadmap item 18 (`ms-miv`) is closed and accepted. Reviewed compilation, identity, and snapshot contracts are unchanged. The delta contract is reviewed, and no conflicting requirement, missing fundamental semantic rule, or frozen compatibility promise blocks this bounded implementation slice. The bead is open, unblocked, and implementation-ready.","notes":"Implementation close-out:\n- Added distinct owned opaque InputDelta<D> and InputDeltaBuilder<D> public types.\n- Added CompiledNetwork<D>::input_delta() bound to the exact NetworkKey, NetworkFingerprint, InputSchemaFingerprint, and stable typed external level-input schema.\n- Delta construction accepts empty, partial, complete, and explicit reassertion batches; omitted inputs remain absent rather than becoming Low or causing completeness failure.\n- Reused InputBuildFailure for structured unknown, duplicate-equivalent, and conflicting observations.\n- Chose private BTreeSet membership and BTreeMap observation storage for simple deterministic stable-key semantics without promising public iteration order.\n- Preserved InputSnapshot completeness behavior and stopped before transaction execution, pulse, patch, projection, or persistence work.\n\nVerification:\n- Focused input tests: passed (6 tests)\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"task","assignee":"mikkofinell","created_at":"2026-07-28T04:21:49.340883Z","created_by":"mikkofinell","updated_at":"2026-07-28T06:33:21.245350Z","closed_at":"2026-07-28T06:33:21.245307Z","close_reason":"Implemented and verified level InputDelta construction.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-ipi","depends_on_id":"ms-miv","type":"blocks","created_at":"2026-07-28T04:22:15.728623Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-j0q","title":"Implement first variadic primitive: All","description":"## Objective\n\nImplement Premium Foundation Roadmap item 22: establish the canonical variadic primitive pattern by adding the total level-only `All` node through typed and dynamic authoring, validation, causality, compilation, evaluation, and the pre-stability canonical fingerprint projection.\n\n## Contract basis\n\n- `mossignal.node.variadic_all`\n  - `docs/specs/contracts/variadic-all.yaml`\n  - status: reviewed at the pre-stability fingerprint policy baseline\n  - role: All semantics, variadic stable ports, authoring, advisories, causality, execution, conformance, and canonical fingerprint representation.\n\n- `mossignal.identity.network_fingerprint`\n  - `docs/specs/contracts/network-fingerprint.yaml`\n  - status: reviewed at the pre-stability projection and All baseline\n  - role: pre-stability v1 canonical identity for Constant, Not, and All, including synchronized golden vectors.\n\n- `mossignal.authoring.typed_network_builder`\n  - `docs/specs/contracts/typed-network-builder.yaml`\n  - status: reviewed, used unchanged\n  - role: builder-scoped signals, foreign-signal behavior, canonical lowering, and completion boundary.\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed, used unchanged\n  - role: canonical unchecked network and stable-keyed dynamic records.\n\n- `mossignal.graph.structural_validation`, `mossignal.graph.current_reaction_dependency_graph`, `mossignal.graph.current_reaction_cycle_validation`, `mossignal.compile.compiled_network`, and `mossignal.runtime.restricted_full_topological_evaluator`\n  - reviewed, contextual restricted-slice contracts\n  - role: existing layer boundaries that All must extend without changing their non-All semantics.\n\n## Included scope\n\n- Add `All` to the canonical authored node kind with zero or more level input ports and one level output.\n- Add typed builder convenience and explicit keyed `All` construction, preserving builder-scope foreign-signal failures and deterministic local variadic-port allocation.\n- Extend structural validation with valid zero/unary arities and the applicable non-blocking empty, unary, and duplicate-source reports.\n- Extend dependency extraction, cycle handling, compilation, and full evaluation so every All input contributes a current-reaction dependency and the total conjunction law is settled deterministically.\n- Extend the pre-stability v1 network-fingerprint projection with the exact `[\"all\", null]` node-kind variant.\n- Add focused dynamic-versus-typed, zero-input, unary, duplicate-source, port-identity/permutation, cycle, compilation, observable-behavior, and fingerprint coverage.\n\n## Exclusions and non-goals\n\n- No Any, Parity, AtLeast, Select, pulse, stateful, temporal, module, persistence-decoding, reconfiguration, provenance, or explanation work.\n- Do not replace the existing unchecked graph, validation-report model, dependency graph, compiler, or full evaluator.\n- Do not add a successor fingerprint domain, historical reader, upgrader, migration path, or compatibility implementation.\n\n## Implementation obligations\n\n- Honor `total-commutative-port-law`, `stable-port-multiplicity`, `typed-and-dynamic-authoring`, `causality-and-execution`, `nonblocking-variadic-quality-findings`, `variadic-all-conformance`, and `canonical-fingerprint-boundary` from `mossignal.node.variadic_all`.\n- Honor `digest-construction`, `canonical-restricted-projection`, and `pre-stability-projection-policy` from `mossignal.identity.network_fingerprint`.\n- Keep every variadic input as a stable distinct port; commutativity must not collapse ports or repeated source connections.\n- Preserve the current valid artifact/report behavior: zero and unary All forms are warnings, not errors.\n- Keep direct dynamic authoring available for malformed definitions.\n\n## Allowed implementation freedom\n\n- Choose the private collection and compiled descriptor shape and a deterministic local port-allocation order. Test and record the selected behavior at implementation close-out.\n- Exact explicit-keyed API grouping is free within the represented typed-builder rules.\n\n## Required verification\n\n1. Verify `All([]) = High`, unary identity, and all small multi-input level valuations.\n2. Verify repeated one-source-to-many-ports multiplicity, the specified duplicate-source advisory behavior, and behavior under variadic port permutations with stable identities retained.\n3. Verify typed and direct dynamic equivalent All definitions validate, compile, and produce the same observable level outputs and dependency/cycle behavior.\n4. Verify zero and unary All forms retain a valid artifact alongside their canonical non-blocking diagnostics.\n5. Verify foreign builder signals and immediately determinable duplicate explicit node/port keys are structured authoring failures.\n6. Verify the exact v1 All canonical node-kind variant, port-sensitive fingerprinting, permutation invariance, and a hand-reviewed golden vector.\n7. Run the narrow relevant tests, then `make check-dev` and `make check-final`.\n\n## Resolved planning decision\n\nFingerprint-domain version 1 remains a development projection until an explicit stabilization baseline. This bead extends v1 with the authoritative `[\"all\", null]` variant and synchronizes its golden fixtures without adding compatibility machinery.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs`, `builder.rs`, `validation.rs`, `identity.rs`, compilation and evaluation modules, plus focused tests\n- authoritative fingerprint specification and verification policy\n- affected draft contracts and `.beads/` state\n\n## Dependencies\n\n- `ms-6qf` is complete.\n\n## Readiness\n\nImplemented proposal is ready for corrective acceptance under the accepted pre-stability fingerprint policy. Changed contracts have been independently reviewed and promoted.","notes":"Implementation close-out:\n- Added the total variadic level-only All primitive across dynamic and typed authoring, structural validation, current-reaction causality, compilation, full evaluation, transactions, and the pre-stability v1 network fingerprint.\n- Deterministic convenience allocation consumes the iterator immediately and assigns variadic InPortKeys in iterator order; explicit keyed construction preserves caller-supplied input order, stable identity, and multiplicity.\n- The pre-stability v1 fingerprint encodes All as [\"all\", null].\n- Independent review promoted docs/specs/contracts/network-fingerprint.yaml and docs/specs/contracts/variadic-all.yaml at the modified authoritative-specification baseline in commit f3bbf23.\n\nCorrective acceptance review:\n- Added direct coverage for deterministic local variadic-port allocation and duplicate explicit All node/output keys.\n- Added typed-versus-dynamic dependency and topological-order equivalence checks.\n- Added execution invariance under authored variadic-port order permutation while retaining stable keys and incidence.\n- Strengthened empty, unary, and duplicate-source diagnostic classification and canonical-evidence checks.\n- Strengthened settled-only transaction coverage with simultaneous opposite input changes that leave All Low without an output event.\n- Added specification traces for the empty conjunction identity and unique-operation indegree accounting with duplicate-source incidences.\n\nVerification:\n- cargo test -p mossignal all: passed (19 focused tests)\n- cargo test -p mossignal: passed (133 unit tests, 1 doctest, 14 compile-fail doctests)\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T09:29:54.191701Z","created_by":"mikkofinell","updated_at":"2026-07-28T10:28:33.686449Z","closed_at":"2026-07-28T10:28:33.686390Z","close_reason":"Implemented, independently contract-reviewed, corrected, and fully verified variadic All support.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-k34","title":"Implement problem and report kernel","description":"# Objective\n\nImplement Premium Foundation Roadmap item 7 as the durable common problem-reporting architecture consumed by the opening authored-graph and structural-validation tasks. Do not introduce a temporary diagnostic or identity model.\n\n## Contract basis\n\nAll four governing records are reviewed and accepted for reuse at their recorded evidence baselines:\n\n- `mossignal.diagnostics.subject_references`\n  - `docs/specs/contracts/diagnostic-subject-references.yaml`\n  - role: stable diagnostic subject identity, related-subject roles, and canonical ordering for the seven initial variants\n- `mossignal.diagnostics.problem_model`\n  - `docs/specs/contracts/diagnostic-problem-model.yaml`\n  - role: catalogue registry, codes, classifications, typed evidence, `Problem<D>`, and `Diagnostic<D>`\n- `mossignal.diagnostics.collections`\n  - `docs/specs/contracts/diagnostic-collections.yaml`\n  - role: `DiagnosticSet<D>`, canonical ordering, condition identity, deduplication, and evidence merging for the opening slice\n- `mossignal.diagnostics.artifact_reports`\n  - `docs/specs/contracts/artifact-reports.yaml`\n  - role: `Report<T, D>`, artifact blocking semantics, and complete diagnostic retention\n\n## Included scope\n\n- Implement the durable catalogue-backed diagnostic kernel.\n- Implement the specified `Severity` and `Responsibility` families with code-fixed lookup.\n- Implement `Problem<D>`, typed code-specific `ProblemEvidence<D>`, related subjects, safe suggestions, and the ReportFinding-only `Diagnostic<D>` wrapper.\n- Implement `SubjectRef` support needed by this slice for `Network`, `Node`, `InPort`, `OutPort`, `Connection`, `ExternalInput`, and `ExternalOutput`, including their permanent comparison tags and canonical payload order.\n- Implement only these opening structural-validation codes: `validation.duplicate_key`, `validation.missing_node`, `validation.missing_port`, `validation.missing_endpoint`, `validation.invalid_direction`, `validation.signal_kind_mismatch`, `validation.unsupported_multiple_drivers`, `validation.missing_required_input`, and `validation.invalid_fixed_arity`.\n- Implement `DiagnosticSet<D>` with canonical iteration, condition-key deduplication, and the specified exact-agreement, set-union, and duplicate-claim multiset behavior.\n- Implement `Report<T, D>` and `ReportFailure<D>` with complete diagnostic retention and blocking-artifact behavior.\n- Use one authoritative declarative registry for every implemented code; do not maintain parallel code, evidence, classification, delivery, discriminator, or merge tables.\n\n## Explicit exclusions\n\n- `SubjectRef` variants and payload comparison outside the seven initial variants.\n- Exhaustive catalogue implementation beyond the nine opening validation codes.\n- Module-binding, reassociation, and module-interface diagnostic identity contexts.\n- Authored graph storage and structural validation detector logic.\n- Runtime diagnostic occurrences.\n- Persistent diagnostic episodes.\n- Internal-defect delivery infrastructure beyond the representation needed for `internal.diagnostic_evidence_conflict`.\n- Provenance, rendering, localization, logging, observer delivery, persistence encoding, serialization, and schema decoding.\n- Any operation for combining two `Report` values or resolving two produced artifacts.\n- Rust compatibility shims or a second temporary problem representation.\n\nThese exclusions correspond to adjacent or `known_uncovered` contract facets and do not block this bead.\n\n## Implementation freedom\n\n- Private enum and struct names, role and category variant spelling, storage layout, helper types, construction APIs, comparison keys, and merge algorithms remain implementation choices when the specified distinctions and deterministic behavior are preserved.\n- A private diagnostic projection may preserve authored facts needed by evidence without exposing complete authored record storage shapes.\n- Canonical subject comparison may use a private comparator or ordering key; no public `Ord` implementation is required by this bead.\n- Implementation may create or extend draft contracts when it discovers reusable specification-backed knowledge already determined by the specifications and this bead. Such drafts require independent review before final acceptance and do not authorize scope expansion.\n\n## Expected change surface\n\n- A diagnostics-focused module or small module family under `crates/mossignal/src/`.\n- Public module integration in `crates/mossignal/src/lib.rs`.\n- Focused unit and compile-boundary documentation tests colocated with the implementation.\n- No unrelated changes to signal, time, key, or metadata semantics.\n\n## Required verification\n\n- Registry coverage and exact fixed severity, responsibility, delivery, evidence association, primary-subject rule, discriminator, and merge law for every implemented code.\n- Safe APIs cannot construct code/evidence, classification, or delivery mismatches.\n- Structured comparison excludes rendered prose.\n- Stable ordering across the seven initial `SubjectRef` variants, with tag comparison before payload comparison and `Level` before `Pulse` for erased signal-bearing keys.\n- Permutations of discovery and unordered evidence produce identical `DiagnosticSet` values and iteration order.\n- Duplicate-key evidence is independent of discovery order, symmetric, complete, canonically ordered, and retains meaningful claim multiplicity without multiplying repeated detection.\n- Deduplication covers repeated equal exact-agreement evidence, distinct subjects, distinct discriminators, partial conflicting-driver set union, partial fixed-arity port-set union, and contradictory exact-agreement evidence.\n- Contradictory exact-agreement evidence produces `internal.diagnostic_evidence_conflict`.\n- Report tests cover blocking artifact absence, non-blocking artifact presence, complete `ReportFailure` diagnostics, and accumulation of unrelated safe findings.\n- Run focused tests while iterating, then `make check-dev` and `make check-final`.\n\n## Blocking open questions\n\nNone.\n\n## Readiness\n\nOpen and claimable. The required stable key families already exist, the represented opening semantics are reviewed, and this bead has no dependency on the contracts' known-uncovered future facets.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-14T17:27:35.600621Z","created_by":"mikkofinell","updated_at":"2026-07-19T04:51:48.033066Z","closed_at":"2026-07-19T04:51:48.033014Z","close_reason":"Corrected diagnostic condition identity, merging, conflict retention, and regression coverage","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["contracts-required","premium-foundation"]}
{"id":"ms-l0b","title":"Implement restricted full topological evaluator","description":"## Objective\\n\\nImplement the restricted level-only Constant/Not full reference evaluator over CompiledNetwork<D>: accept external level facts and evaluate every reaction operation once in the retained deterministic topological order.\\n\\n## Contract basis\\n\\n- mossignal.runtime.restricted_full_topological_evaluator\\n  - docs/specs/contracts/restricted-full-topological-evaluator.yaml\\n  - status: reviewed\\n  - role: reference-evaluator boundary, root facts, settlement order, Constant/Not laws, and direct verification.\\n\\n- mossignal.compile.compiled_network\\n  - docs/specs/contracts/compiled-network.yaml\\n  - status: reviewed, used unchanged\\n  - role: immutable compiled topology, private dense execution metadata, and retained deterministic operation order.\\n\\n- mossignal.graph.current_reaction_dependency_graph\\n  - docs/specs/contracts/current-reaction-dependency-graph.yaml\\n  - status: reviewed, used unchanged\\n  - role: stable-subject reaction relation, external input root facts, and output observation.\\n\\n- mossignal.graph.authored_network_definition\\n  - docs/specs/contracts/authored-network-definition.yaml\\n  - status: reviewed, used unchanged\\n  - role: restricted authored Constant/Not and external-boundary structure behind validated compilation.\\n\\n## Included scope\\n\\n- Implement one full reference evaluator for the existing immutable restricted compiled topology.\\n- Accept caller-supplied external level facts keyed by represented external level inputs.\\n- Traverse the retained deterministic topological order exactly once, only after predecessors have settled.\\n- Evaluate Constant and Not, propagate node outputs, and observe external outputs.\\n- Add focused direct tests for node laws, multi-node chains, external inputs and outputs, exact-once execution, and predecessor availability.\\n\\n## Exclusions and non-goals\\n\\n- Do not implement Machine, initialization, lifecycle, transactions, cloning or staging, atomic publication, snapshots, deltas, input-completeness validation, runtime policy, or public runtime APIs.\\n- Do not implement pulse values, stateful or temporal nodes, events, dirty propagation, incremental evaluation, worklists, diagnostics, provenance, persistence, replay, graph-query APIs, or topology patches.\\n- Do not expose compiled dense indices, adjacency, arenas, or worklists publicly.\\n- Do not change validation, cycle analysis, fingerprint construction, or compiled topology semantics.\\n\\n## Implementation obligations\\n\\n- Evaluate only the immutable compiled Constant/Not topology and introduce no mutable machine state.\\n- Treat represented external level inputs as caller-provided root facts; external outputs observe their authored source without transformation.\\n- Evaluate every represented reaction operation exactly once in the compiled deterministic topological order, after all affected predecessor results are final.\\n- Apply the exact restricted node laws: Constant yields its configured LogicLevel; Not yields the logical complement of its one settled level input.\\n- Keep the evaluator clear and deterministic. It is the future correctness oracle for optimized incremental execution.\\n\\n## Allowed implementation freedom\\n\\n- The evaluator/result interface may remain crate-private or test-oriented; this task does not establish a general public runtime API.\\n- Choose the simplest deterministic input/result storage and error-free internal shape compatible with the already validated compiled topology.\\n- It may retain intermediate settled results for direct test inspection, but the specifications do not require a particular result interface.\\n- Input snapshot completeness, unknown-input rejection, duplicate-observation rules, and public builders are expressly deferred to roadmap item 15.\\n- Prefer direct auditable traversal over caching or allocation optimization; record and test any ordinary representation choice during implementation.\\n\\n## Expected change surface\\n\\n- crates/mossignal/src/compile.rs or a focused private evaluator module\\n- crates/mossignal/src/lib.rs only if a deliberate minimal test-facing internal boundary requires it\\n- focused unit tests adjacent to the evaluator\\n- docs/specs/contracts/restricted-full-topological-evaluator.yaml only for implementation-discovered reusable facts; any change remains draft\\n- .beads/ when implementation acceptance closes the bead\\n\\n## Required verification\\n\\n1. Directly test Constant High and Low and both Not input values against their node laws.\\n2. Test a hand-authored multi-node chain and its expected settled outcomes.\\n3. Test externally supplied level facts flow through Not and external-output observation without transformation at the boundary.\\n4. Instrument or otherwise prove every represented reaction operation executes exactly once and only after each predecessor result is available.\\n5. Run focused tests while iterating, then make check-dev; final acceptance additionally runs make check-final.\\n\\n## Dependencies\\n\\n- Roadmap item 13 / ms-d1f is complete and accepted.\\n- Roadmap item 15 adds the validated public InputSnapshot artifact; this bead intentionally consumes only raw supplied level facts.\\n\\n## Readiness\\n\\nReady for implementation. The remaining choices are ordinary deterministic implementation freedom; no conflicting requirement, missing fundamental semantic rule, or frozen compatibility promise blocks this restricted reference-evaluator slice.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T19:05:15.097561Z","created_by":"mikkofinell","updated_at":"2026-07-27T19:13:01.337945Z","closed_at":"2026-07-27T19:13:01.337870Z","close_reason":"Implemented restricted full topological evaluator","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-l0b","depends_on_id":"ms-d1f","type":"blocks","created_at":"2026-07-27T19:05:15.097561Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-miv","title":"Implement initialization transaction execution","description":"## Objective\n\nImplement Premium Foundation Roadmap item 18: execute the first restricted level initialization transaction against an `AwaitingInitialization` machine at an arbitrary logical time. Use one complete exact-bound `InputSnapshot`, the ordinary full Constant/Not evaluator, and correctness-oriented atomic staging. Success publishes one complete `Ready` machine and deterministic external `LevelEstablished` events; rejection leaves the complete machine unchanged.\n\n## Contract basis\n\n- `mossignal.runtime.initialization_transaction` (`docs/specs/contracts/initialization-transaction.yaml`), reviewed at the current evidence baseline, governs the explicit transaction value, exact preconditions, policy budgets, ordinary evaluation, Ready commit, output establishment, provenance, atomicity, and verification.\n- `mossignal.runtime.machine_lifecycle` (`docs/specs/contracts/machine-lifecycle.yaml`), reviewed and implemented by `ms-puk`, supplies the canonical machine ownership and `AwaitingInitialization` state consumed here.\n- `mossignal.runtime.level_input_snapshot`, `mossignal.runtime.restricted_full_topological_evaluator`, `mossignal.compile.compiled_network`, and `mossignal.runtime.policy` are reviewed and used unchanged for exact snapshot binding, settlement, immutable topology, and applicable budget behavior.\n\n## Included scope\n\n- Add the owned level-only `Transaction<D>::initialize(Time<D>, NetworkRevision, InputSnapshot<D>)` form and restricted `Machine::apply` path.\n- Require `AwaitingInitialization`, an exact current revision, and snapshot identities matching the machine's exact `NetworkKey`, `NetworkFingerprint`, and `InputSchemaFingerprint`.\n- Feed the snapshot's complete stable-keyed authoritative external levels to the existing ordinary full Constant/Not evaluator; do not create a special initialization evaluator or infer omitted inputs as `Low`.\n- Validate the associated runtime policy and enforce each semantic budget that has a real accounting boundary in this execution path.\n- Stage all fallible work against one complete private candidate and publish once through a non-fallible commit point.\n- On success, establish the requested time, preserve revision and policy identity, store authoritative external levels, complete settled levels, and every external level-output baseline, and transition to `Ready`.\n- Return one `LevelEstablished` event for each external level output, including a zero-length stream for an empty output set. Order same-time events canonically by stable output identity, independently of topology construction, map iteration, and dense storage order.\n- Give every returned establishment event an opaque `CauseRef`; the owned immutable result must retain a `ProvenanceView` resolving every returned cause to a structured acyclic derivation with authoritative initialization roots.\n- Preserve structured catalogue distinctions for every reachable lifecycle, revision, input-compatibility, evaluation, and budget rejection.\n\n## Exclusions and non-goals\n\n- No `InputDelta`, ready-machine execution, successful repeat initialization, pulse I/O, stateful or temporal nodes, pending events, deadlines, schedules, runtime occurrences, or persistent diagnostic episodes.\n- No topology patch, migration, revision advancement, reconfiguration, persistence, replay, restoration, forecast, observer delivery, public callbacks, or optimized sparse/copy-on-write staging.\n- No execution or observable state digests, machine snapshot, or complete explanation-oriented provenance beyond what returned initialization events require.\n- Do not change reviewed snapshot, evaluator, compiled-network, lifecycle, or runtime-policy contracts unless implementation discovers a reusable specification-backed correction; any such change returns to draft review.\n\n## Allowed implementation freedom\n\n- Private transaction, candidate-state, settled-result, event, provenance, and failure storage; module placement; accessor details; and canonical sorting mechanism are free when the reviewed contracts remain exact.\n- Use the simplest auditable clone-and-swap mechanism. Private staging does not create a public `Machine<D>: Clone` promise.\n- Keep result and failure fields private or non-exhaustive as appropriate so this bounded slice does not freeze unrelated later compatibility promises. Reachable failures must remain structured and every returned cause must resolve.\n- Do not invent counters for unsupported subsystems. Record and test ordinary deterministic representation choices in the implementation or close-out.\n\n## Required verification\n\n1. Initialize equivalent machines at zero and an arbitrary nonzero time; verify `Ready { now }`, unchanged revision and policy identity, authoritative external levels, complete settled levels, and output baselines.\n2. Cover representative Constant, external-input/output, Not, chained, fan-out, multiple-input, multiple-output, and empty-output restricted networks through the ordinary full evaluator.\n3. Verify every external level output produces one exact deterministic `LevelEstablished` whose cause resolves through the result, and that insertion/construction order cannot perturb the stream.\n4. Verify lifecycle incompatibility, stale revision, wrong network, wrong fingerprint/schema, and every other reachable rejection preserve complete semantic machine equality.\n5. For every implemented runtime-policy accounting boundary, test one below, exactly at, and one above the limit where representable, including complete rollback on failure.\n6. Run focused tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Acceptance criteria\n\n- A complete exact-bound level snapshot initializes an `AwaitingInitialization` restricted machine at any representable logical time through the ordinary full evaluator.\n- Success publishes one complete Ready state with authoritative and settled storage plus deterministic establishment events whose causes resolve through the owned result, without changing topology revision or policy association.\n- Every supported rejection is structured and proves complete semantic machine equality before and after application.\n- The implementation remains inside the reviewed Constant/Not, level-only, correctness-oriented staging boundary and passes the required gates.\n\n## Dependencies and readiness\n\n`ms-puk` is closed and accepted, and roadmap items 13 through 16 already supply compilation, full evaluation, complete level snapshots, and runtime policy. This bead is open, unblocked, reviewed, and implementation-ready. No conflicting requirement, missing fundamental semantic rule, or frozen compatibility promise blocks the bounded slice.","notes":"Implementation close-out:\n- Added owned explicit initialization transactions and atomic Machine application for the restricted Constant/Not runtime.\n- Promoted the existing full topological evaluator from test-only to crate-private production staging and retained complete settled operation values.\n- Added authoritative external-level storage, output baselines, Ready lifecycle transition, deterministic LevelEstablished events, and result-owned provenance resolution.\n- Chose stable ExternalOutputKey order as the canonical same-time event representation.\n- Chose result-scoped opaque CauseRef values with a deterministic private scope plus ordinal; no durable representation or private arena position is exposed.\n- Implemented semantic accounting for one internal reaction, compiled operation evaluations, emitted output events, and required provenance records. Pending-event accounting remains absent because initialization creates no pending work in this restricted slice.\n- Added exact structured lifecycle, stale-revision, wrong-network, foreign-schema, and budget rejection categories with atomic rollback.\n\nVerification:\n- Focused initialization tests: passed (6 tests)\n- Workspace tests: passed (103 tests)\n- make check-dev: passed\n- make check-final: passed\n\nCorrective acceptance review:\n- Audited the committed implementation against the reviewed initialization-transaction and machine-lifecycle contracts.\n- Strengthened complete-machine rollback observations for every reachable structured rejection and applicable budget failure.\n- Added equivalent zero-time and nonzero-time initialization coverage, including revision and runtime-policy identity retention.\n- Strengthened causal verification so every returned establishment cause is acyclic and terminates in authoritative initialization roots.\n- Completed documentation for the bounded TransactionResult accessors.\n\nReview verification:\n- Focused initialization tests: passed (7 tests)\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T20:42:00.871668Z","created_by":"mikkofinell","updated_at":"2026-07-28T03:54:50.568045Z","closed_at":"2026-07-28T03:44:31.840602Z","close_reason":"Implemented and verified restricted initialization transaction execution.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-miv","depends_on_id":"ms-puk","type":"blocks","created_at":"2026-07-27T20:42:04.614360Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-o3p","title":"Add Coalesce and Zip pulse primitives","description":"## Objective\n\nImplement Premium Continuation Roadmap item 41 as one bounded pulse-combinational expansion: add Coalesce and Zip across both typed builders and the canonical dynamic, validation, compilation, execution, provenance, identity, and transaction paths while preserving their distinct reaction-local multiplicity laws.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 41\n- Title: Remaining pulse-combinational primitives\n\nThis provenance records sequencing only. The referenced contracts and authoritative specifications govern product semantics.\n\n## Contract basis\n\n### Reviewed feature contract\n\n- `mossignal.node.pulse_combinational_expansion`\n  - `docs/specs/contracts/pulse-combinational-expansion.yaml`\n  - status: reviewed\n  - role: governs Coalesce and Zip laws, port and arity domains, authoring, validation, causality, settlement, provenance, fingerprint integration, and conformance.\n\n### Reviewed contracts reused without modification\n\n- `mossignal.node.pulse_merge` (`docs/specs/contracts/pulse-merge.yaml`): unchanged variadic Pulse port identity, typed and dynamic authoring pattern, dependency/compilation integration, grouped provenance, advisories, and conformance pattern.\n- `mossignal.runtime.reaction_scoped_pulse_foundation` (`docs/specs/contracts/reaction-scoped-pulse-foundation.yaml`): typed reaction-scoped counts, fan-out, input and transaction integration, output publication, grouped provenance, and failure atomicity. Its changed restricted-fingerprint source facet was rechecked directly for this task and is refined by the reviewed feature contract.\n- `mossignal.graph.authored_network_definition` (`docs/specs/contracts/authored-network-definition.yaml`) and `mossignal.authoring.typed_network_builder` (`docs/specs/contracts/typed-network-builder.yaml`): canonical unchecked node/port/connection representation and builder-scoped typed Signal rules.\n- `mossignal.authoring.typed_module_builder` (`docs/specs/contracts/typed-module-builder.yaml`): unchanged primitive-family parity, explicit module-local identity, canonical unchecked lowering, and foreign-signal rejection for ModuleBuilder.\n- `mossignal.graph.structural_validation` (`docs/specs/contracts/structural-validation.yaml`), `mossignal.graph.current_reaction_dependency_graph` (`docs/specs/contracts/current-reaction-dependency-graph.yaml`), and `mossignal.graph.current_reaction_cycle_validation` (`docs/specs/contracts/current-reaction-cycle-validation.yaml`): existing stable-subject validation, dependency, and acyclicity boundaries. Only the task-relevant generic facets apply; changed validated-owner and verification source facets do not alter the node laws researched here.\n- `mossignal.compile.compiled_network` (`docs/specs/contracts/compiled-network.yaml`) and `mossignal.runtime.restricted_full_topological_evaluator` (`docs/specs/contracts/restricted-full-topological-evaluator.yaml`): immutable descriptors, stable-to-dense privacy, compiled invariants, and complete deterministic settlement.\n- `mossignal.runtime.initialization_transaction` (`docs/specs/contracts/initialization-transaction.yaml`) and `mossignal.runtime.ready_level_transaction` (`docs/specs/contracts/ready-level-transaction.yaml`): canonical lifecycle, result, provenance, budget, and failure-atomic transaction behavior reused for pulse-bearing reactions.\n- `mossignal.identity.network_fingerprint` (`docs/specs/contracts/network-fingerprint.yaml`) and `mossignal.identity.module_fingerprint` (`docs/specs/contracts/module-fingerprint.yaml`): semantic node-kind, port, incidence, ordering, and metadata-exclusion identity rules. The accepted implementation must synchronize the provisional exact network projection and affected evidence after the concrete variants are chosen.\n- `mossignal.diagnostics.problem_model` (`docs/specs/contracts/diagnostic-problem-model.yaml`) and `mossignal.diagnostics.artifact_reports` (`docs/specs/contracts/artifact-reports.yaml`): catalogue-backed structured validation findings and non-blocking artifact-bearing reports.\n\n## Included scope\n\n- Add closed dynamic `NodeKind::Coalesce` and `NodeKind::Zip` support with one Pulse output, exactly one Pulse input for Coalesce, and one or more stable variadic Pulse inputs for Zip.\n- Add concise and explicit keyed Coalesce and Zip construction to both `NetworkBuilder<D>` and `ModuleBuilder<D>`, preserving immediate iterator consumption, builder ownership checks, exact-key dynamic reconstruction, and canonical lowering.\n- Extend structural validation for fixed shape, Pulse kinds, missing ports, Zip zero arity, unary Zip advisories, and duplicate-source advisories where applicable.\n- Extend current-reaction dependency extraction, cycle validation, compilation, compiled invariants, and full topological evaluation using every Pulse input-to-output influence.\n- Implement Coalesce as zero-to-zero and positive-to-one, retaining complete causal support despite the bounded output count.\n- Implement Zip as the reaction-local minimum input count, with one unordered all-port group per output occurrence and no retention of unmatched counts.\n- Integrate exact initialization and ready-transaction Pulse output behavior, grouped provenance, runtime budgets, and complete failure atomicity through the existing machine path.\n- Extend provisional network and module semantic fingerprint coverage for the two distinct node kinds and stable port incidences, synchronizing authoritative projection text, implementation, affected contract evidence, and golden vectors without historical compatibility machinery.\n- Add reusable conformance and focused integration tests for both typed builders, direct dynamic definitions, diagnostics, determinism, provenance, transactions, and identity.\n\n## Exclusions and non-goals\n\n- No PulseGate, PulseSelect, PulseRoute, edge detector, additional stateful node, or temporal node.\n- No level-controlled behavior, payload transport, pulse persistence, pending event, deadline, schedule change, or unmatched-count buffering.\n- No new external input/output artifact semantics, transaction API, evaluator architecture, runtime policy category, provenance identity model, or diagnostic namespace beyond applying existing conditions.\n- No standard-module `any_pulse` convenience; it remains later catalogue/convenience work even though Coalesce makes its expansion possible.\n- No general node inspection, current explanation, why-not rendering, snapshot codec, replay codec, topology patch, migration, or compatibility shim.\n- No compatibility migration for earlier provisional fingerprints or goldens; the repository is pre-release and the accepted revision must only be internally coherent.\n\n## Implementation obligations\n\n- Implement reviewed contract requirements `coalesce-unary-law`, `coalesce-retains-causal-support`, `zip-group-law`, `zip-port-identity-and-reaction-isolation`, `typed-and-dynamic-authoring`, `structural-validation-and-findings`, `causality-compilation-and-settlement`, `bounded-checked-count-results`, `joint-group-provenance`, `pre-stability-fingerprint-extension`, and `primitive-conformance`.\n- Reuse the existing Merge and Pulse foundation representations and evaluator path rather than adding a parallel pulse store, transaction path, or callback boundary.\n- Preserve stable port incidence and signal kind through unchecked definitions, validation candidates, dependency vertices, compiled descriptors, provenance subjects, and fingerprints; dense positions remain private revision-local implementation details.\n- Make zero-input Zip a blocking `validation.invalid_variadic_arity` finding, unary Zip a valid artifact with `validation.unary_degenerate_node`, and malformed Coalesce shapes use the existing fixed-shape and kind-aware validation conditions.\n- Preserve complete reaction batches: Coalesce must not lose upstream causal support when bounding the count, Zip must use every settled input count jointly, and neither node may carry unmatched Pulse occurrences into a later reaction.\n- Ensure typed and direct dynamic construction of equal stable structures produces equivalent validation, dependency graphs, compiled behavior, provenance, and fingerprints in both network and reusable-module contexts.\n- Keep production code free of `unwrap` and `expect`.\n\n## Allowed implementation freedom\n\n- Choose the smallest deterministic private descriptor variants, evaluator helpers, minimum-reduction strategy, and grouped-provenance record shape compatible with existing provenance inspection; test and record the concrete choice.\n- Choose deterministic locally allocated fixed-port and variadic-port ordering consistent with existing builder patterns. Exact keyed forms remain the durable reconstruction authority.\n- Choose exact provisional canonical variant spellings for Coalesce and Zip, update authority and goldens coherently, and do not add readers, migrations, aliases, or deprecations for the prior repository revision.\n- Reuse or extract shared NetworkBuilder and ModuleBuilder helpers where that reduces duplication without merging their distinct builder scopes.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs` and `crates/mossignal/src/builder.rs` for dynamic kinds and NetworkBuilder construction.\n- `crates/mossignal/src/module.rs` or existing module-builder support for primitive-family parity and canonical lowering.\n- `crates/mossignal/src/validation.rs` for Coalesce shape and Zip arity/quality findings.\n- `crates/mossignal/src/compile.rs` for descriptors, dependency-preserving compilation, invariant checks, evaluation, and grouped provenance inputs.\n- `crates/mossignal/src/identity.rs` and the authoritative restricted fingerprint projection for exact kind variants and golden vectors.\n- `crates/mossignal/src/transaction.rs` only as narrowly required for canonical grouped causes or conformance access; no separate application path.\n- Focused tests such as `crates/mossignal/tests/pulse_combinational.rs`, existing shared conformance support, and module-builder parity tests.\n- `docs/specs/persistence_canonical_encoding_and_compatibility_spec.md` plus affected fingerprint contract evidence only for the synchronized provisional exact projection.\n- `docs/premium_continuation_roadmap.md` for item-41 progress/completion state.\n- `.beads` only when the planning pass or later implementation is independently reviewed and synchronized.\n\n## Required verification\n\n1. Verify Coalesce for zero, one, two, a larger count, and `u64::MAX`, including absent input batches and no cross-reaction persistence.\n2. Verify Zip rejects zero arity, accepts unary with the exact non-blocking advisory, and computes minima for equal, unequal, zero-containing, duplicate-source, permuted-port, larger, and `u64::MAX` batches.\n3. Compare NetworkBuilder, ModuleBuilder, exact keyed, and direct dynamic forms for shapes, stable incidences, validation, dependency signatures, compilation, execution, provenance, and semantic identity.\n4. Exercise malformed fixed Coalesce ports, wrong kinds, missing inputs/outputs, zero-input Zip, duplicate keys, duplicate sources, and current-reaction cycles through exact structured diagnostics and deterministic report ordering.\n5. Verify every Coalesce input influence and every Zip port influence appears in the dependency relation and settles correctly in multi-layer, fan-out, and reconvergent Pulse graphs without traversal-order effects.\n6. Verify Coalesce output derivations retain all input-batch contributors and Zip derivations retain unordered joint support from every input port, with resolvable causes and permutation-invariant normalized provenance.\n7. Exercise initialization and multiple ready reactions, confirming exact Pulsed event count, time, revision, ordering, schedule neutrality, absence of persistent Pulse state, and no unmatched Zip carryover.\n8. Force applicable upstream overflow and runtime budget failures around both nodes and compare the complete machine before and after rejection.\n9. Verify insertion-order and metadata invariance plus sensitivity to Coalesce versus Zip versus Merge, stable port identity, Zip arity/incidence, NetworkKey, TimeDomainId, and module-local semantic structure; update hand-reviewed canonical bytes and digests.\n10. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Blocking questions\n\nNone. The authoritative specifications fix both multiplicity laws, arity domains, dependency signatures, reaction isolation, and provenance responsibilities. Private grouping representation, local port allocation, helper placement, and provisional canonical spelling are deterministic implementation freedoms rather than product blockers.\n\n## Readiness\n\nBounded with no product-semantics blocker. Contract review promoted `mossignal.node.pulse_combinational_expansion`; this bead is accepted as coherent and ready for implementation.","notes":"Implementation complete at the accepted working-tree proposal. Chosen private design: distinct compiled Coalesce and Zip variants reuse the existing complete-batch evaluator; all pulse-combinational nodes share one grouped PulseCombinational evaluation-cause representation; Zip reduces settled PulseCount values by deterministic minimum; Coalesce maps positivity to ONE; canonical fingerprint variants are coalesce and zip. Independent contract review accepted mossignal.identity.network_fingerprint, mossignal.runtime.reaction_scoped_pulse_foundation, and mossignal.node.pulse_combinational_expansion as reviewed reusable contracts. Corrective acceptance review fixed zero-input Zip validation so validation.invalid_variadic_arity remains independently reported alongside malformed output shape, and strengthened acceptance coverage for exact full canonical bytes, Coalesce counts, complete rollback observations, and successful reconvergent recovery. Focused pulse-combinational and identity tests, make check-dev, and make check-final passed.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-30T11:49:18.465350Z","created_by":"mikkofinell","updated_at":"2026-07-30T12:43:13.272403Z","closed_at":"2026-07-30T12:20:04.700190Z","close_reason":"Implemented and verified Coalesce and Zip across authoring, validation, execution, provenance, and identity","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["pulse","roadmap","runtime"]}
{"id":"ms-o5n","title":"Add level-controlled pulse primitives","description":"## Objective\n\nImplement Premium Continuation Roadmap item 42 as one bounded level-controlled Pulse family: add PulseGate, PulseSelect, and PulseRoute across typed and dynamic authoring, validation, current-reaction causality, compilation, execution, provenance, and semantic identity while preserving complete reaction-local multiplicity under fully settled Level controls.\n\n## Roadmap provenance\n\n- Document: docs/premium_continuation_roadmap.md\n- Item: 42\n- Title: Level-controlled pulse primitives\n\nThis records sequencing only. Contracts and authoritative specifications govern semantics.\n\n## Contract basis\n\n### Reviewed feature contract\n\n- mossignal.node.level_controlled_pulse (docs/specs/contracts/level-controlled-pulse.yaml), status reviewed: governs the three laws, fixed roles, authoring, validation, causality, settlement, provenance, identity, and bounded conformance.\n\n### Reviewed contracts reused without modification\n\n- mossignal.runtime.reaction_scoped_pulse_foundation (docs/specs/contracts/reaction-scoped-pulse-foundation.yaml): reaction-local counts, fan-out, transactions, Pulsed events, grouped provenance, and failure atomicity.\n- mossignal.graph.authored_network_definition (docs/specs/contracts/authored-network-definition.yaml), mossignal.authoring.typed_network_builder (docs/specs/contracts/typed-network-builder.yaml), and mossignal.authoring.typed_module_builder (docs/specs/contracts/typed-module-builder.yaml): canonical dynamic structure, scoped typed Signals, explicit stable keys, and primitive-family parity.\n- mossignal.graph.structural_validation (docs/specs/contracts/structural-validation.yaml), mossignal.graph.current_reaction_dependency_graph (docs/specs/contracts/current-reaction-dependency-graph.yaml), and mossignal.graph.current_reaction_cycle_validation (docs/specs/contracts/current-reaction-cycle-validation.yaml): stable-subject findings, conservative dependency extraction, and acyclicity. Changed generic source facets were rechecked directly and do not alter this family.\n- mossignal.compile.compiled_network (docs/specs/contracts/compiled-network.yaml) and mossignal.runtime.restricted_full_topological_evaluator (docs/specs/contracts/restricted-full-topological-evaluator.yaml): validated-only descriptors, stable-to-dense privacy, invariants, and deterministic complete settlement.\n- mossignal.runtime.initialization_transaction (docs/specs/contracts/initialization-transaction.yaml) and mossignal.runtime.ready_level_transaction (docs/specs/contracts/ready-level-transaction.yaml): lifecycle preconditions, complete publication, runtime budgets, and atomic rejection.\n- mossignal.identity.network_fingerprint (docs/specs/contracts/network-fingerprint.yaml) and mossignal.identity.module_fingerprint (docs/specs/contracts/module-fingerprint.yaml): semantic kind, typed port, role, incidence, order-invariance, and metadata exclusion.\n- mossignal.diagnostics.problem_model (docs/specs/contracts/diagnostic-problem-model.yaml) and mossignal.diagnostics.artifact_reports (docs/specs/contracts/artifact-reports.yaml): catalogue-backed structured findings and artifact blocking.\n\n## Included scope\n\n- Add closed dynamic PulseGate, PulseSelect, and PulseRoute node kinds with exact fixed Level and Pulse port shapes and semantic roles.\n- Add concise and exact keyed constructors to NetworkBuilder and ModuleBuilder. PulseRoute exposes distinct typed when-low and when-high outputs.\n- Extend structural validation for fixed arity, signal kinds, required drivers, semantic roles, duplicate claims, and independently safe findings using existing conditions.\n- Extend dependency extraction, cycle validation, compilation, invariants, and full topological execution for every conservative input-to-output influence.\n- Implement PulseGate as High-pass and Low-suppress, PulseSelect as complete selected-branch multiplicity, and PulseRoute as complete count on exactly one selected output.\n- Settle simultaneous controls and counts without consumption, buffering, hidden ordering, stored enable state, or pulse persistence.\n- Retain resolvable event provenance for the controlling Level fact and passed or selected Pulse batch.\n- Extend provisional network and module fingerprints for all three kinds and every semantic role, including PulseRoute output roles, synchronizing authority, implementation, and exact golden vectors.\n- Add focused reusable conformance for typed, direct dynamic, module-wrapped, malformed, multi-layer, and transaction cases.\n\n## Exclusions and non-goals\n\n- No edge detectors, state cells, stored enable state, pulse persistence, temporal windows, deadlines, pending events, callbacks, or host effects.\n- No low-enabled PulseGate variant; explicit Not composition supplies low polarity.\n- No new signal kind, pulse payload, transaction API, evaluator architecture, runtime policy category, or diagnostic namespace.\n- No general node-inspection, explanation, why-not, graph-query, snapshot, replay, topology-patch, or migration implementation. Exact inspection, current-explanation, and why-not projections remain known uncovered.\n- No compatibility shim, migration, legacy projection reader, or historical fingerprint support before an explicit frozen baseline.\n- No unrelated standard-module or convenience work.\n\n## Implementation obligations\n\n- Implement all requirements in mossignal.node.level_controlled_pulse.\n- Preserve the full passed or selected PulseCount, including zero and u64::MAX, and expire every suppressed or unrouted count with the reaction.\n- Preserve stable semantic role identity independently of signal kind and port key where roles distinguish equal-kind ports or outputs.\n- Reuse existing Level settlement and Pulse batch paths; do not add a parallel pulse store, control cache, microstep evaluator, or callback boundary.\n- Keep dependency signatures conservative even when the current control suppresses a path.\n- Keep production code free of unwrap and expect.\n\n## Allowed implementation freedom\n\n- Choose the smallest deterministic dynamic representation for fixed input and output roles that preserves malformed claims, exact reconstruction, validation, dependency extraction, provenance subjects, and fingerprints.\n- Choose private descriptors, evaluator helpers, pulse-slot access, and provenance layout compatible with complete settled inputs and deterministic observations.\n- Choose deterministic local fixed-port allocation; exact keyed forms remain the durable reconstruction authority.\n- When several representations conform, select the simplest, test it, and record the concrete decision without first turning it into permanent specification policy.\n\n## Expected change surface\n\n- crates/mossignal/src/authored.rs and crates/mossignal/src/builder.rs for dynamic kinds, roles, output handles, and both builders.\n- crates/mossignal/src/module.rs only as required for canonical module validation and lowering parity.\n- crates/mossignal/src/validation.rs, crates/mossignal/src/compile.rs, and crates/mossignal/src/transaction.rs for validation, compilation, execution, and provenance.\n- crates/mossignal/src/identity.rs and docs/specs/persistence_canonical_encoding_and_compatibility_spec.md for provisional identity projection and goldens.\n- Focused level-controlled-pulse tests plus shared conformance support.\n- docs/premium_continuation_roadmap.md and .beads for task state only.\n\n## Required verification\n\n1. Exhaustively cross Low and High controls with counts zero, one, two, a larger value, and u64::MAX for every law.\n2. Verify exact gate pass and suppression, both select branches and unselected expiration, and exactly one route output with deterministic event ordering.\n3. Exercise simultaneous control changes and pulse observations during initialization and multiple ready reactions; verify no stored enable or cross-reaction persistence.\n4. Compare NetworkBuilder, ModuleBuilder, exact keyed, and direct dynamic forms for shapes, roles, validation, dependencies, compilation, execution, provenance, and identity.\n5. Exercise malformed arity, wrong kinds, missing drivers, duplicate keys, role defects, multiple drivers, and cycles through structured deterministic diagnostics.\n6. Verify all dependency edges, multi-layer fan-out and reconvergence, and order invariance without traversal intermediates or pulse consumption.\n7. Verify nonzero event provenance resolves to current control and passed or selected support; suppressed paths do not become emitted contributors or retained state.\n8. Verify identity sensitivity to node kind, every input and route-output role, port key, incidence, NetworkKey, TimeDomainId, and module structure; verify insertion-order and metadata invariance with complete canonical bytes and digests.\n9. Force applicable upstream overflow and runtime-budget failures around all three nodes and compare complete machine observations before and after rejection.\n10. Run focused tests while iterating, then make check-dev; final acceptance additionally runs make check-final.\n\n## Blocking questions\n\nNone. Product laws, polarity, fixed shapes, dependency signatures, and reaction semantics are specified. Exact private role representation and helper layout are bounded deterministic freedoms. The repository has no explicit frozen fingerprint baseline.\n\n## Readiness\n\nBounded and claimable with no product-semantics blocker. The governing feature contract is reviewed and reusable at its recorded evidence baseline.","status":"closed","priority":1,"issue_type":"feature","created_at":"2026-07-30T12:58:38.988154Z","created_by":"mikkofinell","updated_at":"2026-07-30T13:53:05.910024Z","closed_at":"2026-07-30T13:53:05.909974Z","close_reason":"Implemented PulseGate, PulseSelect, and PulseRoute across authoring, validation, compilation, runtime, provenance, identity, modules, and conformance; make check-final passes.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["pulse","roadmap","runtime"],"dependencies":[{"issue_id":"ms-o5n","depends_on_id":"ms-o3p","type":"blocks","created_at":"2026-07-30T12:58:38.988154Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-pr8","title":"Add exact module instantiation and retained hierarchy","description":"## Objective\n\nImplement Premium Continuation Roadmap item 35 as one bounded module-composition slice: add exact typed and dynamic module instantiation to NetworkBuilder and ModuleBuilder, retain nested hierarchy and instance-qualified identity through validation and inspection, and stop before module-aware executable lowering.\n\n## Roadmap provenance\n\n- Document: docs/premium_continuation_roadmap.md\n- Item: 35\n- Title: Exact module instantiation and retained hierarchy\n\nThis provenance records task sequencing only. Authoritative specifications and the referenced contracts govern semantics.\n\n## Contract basis\n\n### Reviewed contract created for this item\n\n- `mossignal.module.instantiation_hierarchy`\n  - `docs/specs/contracts/module-instantiation-hierarchy.yaml`\n  - status: reviewed\n  - role: governs typed transactional instantiation, owned dynamic instances and bindings, typed output identity, nested containment, complete validation, cross-boundary causality, inspection, instance-sensitive identity, and the temporary structured compilation boundary.\n\n### Reviewed contracts reused without modification\n\n- `mossignal.authoring.typed_network_builder`\n  - `docs/specs/contracts/typed-network-builder.yaml`\n  - role: builder ownership, scoped Signal<S>, structured foreign-signal failure, and canonical lowering conventions.\n- `mossignal.authoring.typed_module_builder`\n  - `docs/specs/contracts/typed-module-builder.yaml`\n  - role: reusable-module authoring and nested-builder scope conventions.\n- `mossignal.module.unchecked_definition`\n  - `docs/specs/contracts/unchecked-module-definition.yaml`\n  - role: typed public interfaces, module-local stable identity, lossless unchecked claims, and explicit User origin.\n- `mossignal.module.validated_user_definition`\n  - `docs/specs/contracts/validated-user-module-definition.yaml`\n  - role: validated immutable module artifacts, interface conformance, encapsulation, deterministic inspection, and module-internal causality.\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - role: canonical owned unchecked-network boundary and malformed-claim retention.\n- `mossignal.graph.current_reaction_dependency_graph` and `mossignal.graph.current_reaction_cycle_validation`\n  - `docs/specs/contracts/current-reaction-dependency-graph.yaml`\n  - `docs/specs/contracts/current-reaction-cycle-validation.yaml`\n  - role: conservative dependency model, stable-subject correspondence, SCC rejection, and deterministic cycle evidence across module boundaries.\n- `mossignal.identity.module_fingerprint` and `mossignal.identity.network_fingerprint`\n  - `docs/specs/contracts/module-fingerprint.yaml`\n  - `docs/specs/contracts/network-fingerprint.yaml`\n  - role: semantic identity, ordering, exclusion, and pre-stability projection rules.\n- `mossignal.diagnostics.artifact_reports`\n  - `docs/specs/contracts/artifact-reports.yaml`\n  - role: blocking artifact omission and safe independent-finding accumulation.\n\nAll reused reviewed rules required by this bead cite unchanged sources for their represented facets.\n\n## Included scope\n\n- Add an owned dynamic ModuleBindingSet and ModuleInstanceDef<D> representation to canonical unchecked network and module definitions.\n- Add typed transactional generic instantiation over validated ModuleDef<D> for NetworkBuilder and ModuleBuilder.\n- Bind every Level and Pulse module input exactly once to a same-kind local Signal<S>; reject missing, duplicate, unknown, wrong-kind, and foreign bindings atomically.\n- Add kind-safe AddedModuleInstance output access retaining the exact ModuleInstanceKey plus ModuleOutputKey<S> source identity.\n- Reject duplicate explicit module-instance identity without overwriting an existing instance or leaving a partial instance claim.\n- Permit nested instances while retaining explicit acyclic containment, instance-qualified internal identity, public interface visibility, and private-internal encapsulation.\n- Extend unchecked lowering, ordinary validation, deterministic graph inspection, ModuleFingerprint, and NetworkFingerprint over instances, bindings, and state-relevant hierarchy.\n- Project validated module public dependency behavior across bindings for complete network current-reaction cycle validation without executable flattening.\n- Establish typed-versus-dynamic canonical equivalence for flat and nested instances.\n- Add the authoritative temporary compile boundary: module-containing ValidatedNetwork values compile to one deterministic `compilation.unsupported_module_instances` report with no artifact until roadmap item 36.\n\n## Exclusions and non-goals\n\n- No module-aware CompiledNetwork lowering or runtime execution; `compile` and `compile_ref` use the authoritative temporary unsupported-module report until roadmap item 36.\n- No standard catalogue descriptors, shortcuts, discovery, generated standard modules, or standard-origin validation.\n- No module replacement, topology patches, compatibility migration, state migration, or pending-event migration.\n- No persistence envelopes, snapshot encoding, restoration, replay, or historical fingerprint reader.\n- No optimized sharing, incremental compilation, callback evaluator, macro authoring language, or public access to private module internals.\n- No implementation of roadmap item 36; the temporary compile rejection is the complete staged boundary for this bead.\n\n## Implementation obligations\n\n- Implement every represented requirement in `mossignal.module.instantiation_hierarchy` after independent contract review.\n- Use one canonical owned instance and binding representation for typed and dynamic paths; do not introduce a builder-only or module-only semantic graph.\n- Preserve signal kind, exact public interface identity, instance identity, module fingerprint, binding incidence, hierarchy, and diagnostic metadata through lowering.\n- Check builder ownership and exact input/output membership before committing an instance; only successful completion adds the instance, and failed bind, duplicate identity, or finish must not leave a partial instance claim.\n- Validate every independently discoverable binding and hierarchy defect through the established structured catalogue and suppress invalid artifacts.\n- Include module public dependency projections in network causality and SCC validation so module boundaries never hide a current-reaction cycle.\n- Keep instance placement out of the referenced ModuleDef fingerprint while including nested semantic instance structure in a containing module and instance key, module fingerprint, bindings, and state-relevant hierarchy in NetworkFingerprint.\n- Implement the temporary compilation rejection exactly as specified without weakening validation or adding executable module lowering.\n- Keep production code free of unwrap and expect and preserve the repository failure discipline.\n\n## Allowed implementation freedom\n\n- Choose the smallest deterministic extension of non-exhaustive source identity that retains `(ModuleInstanceKey, ModuleOutputKey<S>)`; do not use collision-prone derived surrogate port keys.\n- Choose private binding collection layout, hierarchy indices, recursive ModuleDef sharing, transactional builder type layout, public dependency-summary caching, and deterministic convenience-allocation behavior after rejected instantiation.\n- Reuse shared private authoring and validation helpers or add focused modules; avoid speculative generic graph frameworks and public API beyond the represented surface.\n- Choose deterministic canonical ordering and exact pre-stability fingerprint field encoding consistent with the existing projection policy, then test and record the concrete choice.\n\n## Expected change surface\n\n- `crates/mossignal/src/key.rs` for typed instance-output source identity.\n- `crates/mossignal/src/authored.rs` for owned binding and instance claims in unchecked networks and modules.\n- `crates/mossignal/src/builder.rs` or a focused sibling for transactional typed instantiation and canonical lowering.\n- `crates/mossignal/src/module.rs` and `crates/mossignal/src/validation.rs` for nested module retention, binding/hierarchy validation, and dependency projection.\n- `crates/mossignal/src/identity.rs` for nested module and containing-network fingerprint contributions.\n- `crates/mossignal/src/diagnostics.rs`, `crates/mossignal/src/graph.rs`, and `crates/mossignal/src/lib.rs` only where required for structured evidence, hierarchy inspection, the temporary compilation finding, and deliberate public exports.\n- Focused unit and integration tests, synchronized `.beads`, and roadmap task state.\n- `docs/specs/contracts/module-instantiation-hierarchy.yaml` only for specification-backed corrections; implementation-discovered changes remain draft pending independent review.\n\n## Required verification\n\n1. Compare flat Level-only, Pulse-only, mixed, zero-input, and nested typed instances with exact dynamic ModuleInstanceDef<D> ModuleBindingSet definitions.\n2. Verify every required input binds exactly once and independently exercise missing, duplicate, unknown, wrong-kind, foreign, dangling, direction-invalid, duplicate-instance, missing-parent, and hierarchy-cycle rejection.\n3. Prove rejected typed instantiation never commits a partial instance, and test the chosen deterministic convenience-allocation behavior after rejection.\n4. Verify instance outputs retain exact kind, instance key, and public output key; unknown and wrong-kind access fail, fan-out preserves one source identity, and private internals cannot be connected through public typed APIs.\n5. Verify module input-to-output dependency projection for combinational, stateful, temporal, zero-dependency, and nested modules, including network cycles that cross one or several instances and genuine state/temporal barriers.\n6. Verify successful validation retains deterministic instance, binding, parent/child, public interface, and instance-qualified internal inspection independent of insertion order.\n7. Verify ModuleFingerprint and NetworkFingerprint invariance and sensitivity for instance order, metadata, instance keys, module definitions, bindings, nesting, and state-relevant hierarchy, including synchronized pre-stability golden vectors.\n8. Verify equivalent valid typed and dynamic construction produce identical unchecked claims, validated views, dependency relations, and fingerprints; independently verify typed failures and malformed dynamic claims at their respective boundaries.\n9. Verify module-free compilation remains unchanged and module-containing `compile` and `compile_ref` return an absent artifact with exactly one deterministic `compilation.unsupported_module_instances` finding containing canonical affected instance keys.\n10. Confirm no module runtime execution, standard catalogue, persistence, replacement, or migration surface is introduced.\n11. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Staged compilation boundary\n\n- Valid module-containing networks remain ValidatedNetwork artifacts.\n- Until roadmap item 36 implements module-aware executable lowering, `compile` and `compile_ref` return the authoritative `compilation.unsupported_module_instances` report with no artifact.\n- This rejection is temporary implementation scaffolding, not compatibility behavior. Roadmap item 36 must make it unreachable for supported module instances and retire the temporary rejection path, tests, diagnostic and evidence implementation, catalogue entry, API rule, and contract requirement unless later authority defines a distinct unsupported case.\n\n## Readiness\n\nReady. No unresolved product question blocks this bounded implementation slice. The governing contract has completed independent review and is accepted for reuse at its recorded evidence baseline.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-29T09:16:48.740957Z","created_by":"mikkofinell","updated_at":"2026-07-29T12:21:27.779859Z","closed_at":"2026-07-29T12:21:27.779805Z","close_reason":"Implemented exact typed and dynamic module instantiation with retained nested hierarchy, instance-qualified inspection, complete binding and hierarchy validation, cross-boundary current-reaction cycle detection, instance-sensitive fingerprints, and the temporary structured compilation boundary. Typed construction stages all bindings privately and mutates the owner only on successful finish; rejected transactions consume no builder allocation state. Canonical validated instance and binding order uses stable keys, while module-free fingerprint bytes remain unchanged.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["authoring","hierarchy","module","roadmap"],"dependencies":[{"issue_id":"ms-pr8","depends_on_id":"ms-am4","type":"blocks","created_at":"2026-07-29T09:16:48.740957Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-puk","title":"Implement machine spawning and lifecycle storage","description":"## Objective\n\nImplement Premium Foundation Roadmap item 17: allow one `CompiledNetwork<D>` to spawn independent mutable `Machine<D>` instances with explicit lifecycle storage, machine-local topology revision, and an explicit validated runtime-policy association. Establish the uninitialized machine boundary without executing a transaction or fabricating runtime values.\n\n## Contract basis\n\n- `mossignal.runtime.machine_lifecycle`\n  - `docs/specs/contracts/machine-lifecycle.yaml`\n  - status: reviewed at the current source baseline\n  - role: governs machine spawning, lifecycle states and inspection, uninitialized state, revision ownership, and independent mutable storage.\n\n- `mossignal.compile.compiled_network`\n  - `docs/specs/contracts/compiled-network.yaml`\n  - status: reviewed, used unchanged\n  - required facets: `immutable-topology-boundary`, `validated-identity-retention`, and `opaque-dense-private-surface` preserve immutable shared topology and stable identity while machine state remains separate.\n\n- `mossignal.runtime.policy`\n  - `docs/specs/contracts/runtime-policy.yaml`\n  - status: reviewed, used unchanged\n  - required facet: `explicit-machine-association` requires explicit validated policy at spawn and observable policy identity.\n\n## Included scope\n\n- Add the canonical opaque mutable `Machine<D>` and non-exhaustive `MachineStatus<D>` with `AwaitingInitialization` and `Ready { now: Time<D> }`.\n- Add opaque machine-local `NetworkRevision` storage and deliberate public lifecycle/identity accessors required for this slice.\n- Add `CompiledNetwork::spawn(policy)` using an explicit validated `RuntimePolicy`.\n- Retain the compiled topology and exact runtime policy, expose their distinct identities, and store the topology revision, lifecycle state, and only the restricted runtime storage needed before transactions exist.\n- Make a newly spawned machine observably awaiting initialization: `status()` reports `AwaitingInitialization`, `is_initialized()` is false, and `now()` is `None`.\n- Preserve compiled network fingerprint and policy identity through machine accessors.\n- Add focused tests showing that multiple machines spawned from one compiled topology retain the same immutable topology identity but do not share mutable lifecycle or runtime storage.\n\n## Exclusions and non-goals\n\n- No initialization or ready-machine transaction execution, evaluator invocation, staging, commitment, or lifecycle transition.\n- No `InputDelta`, output events, schedule or deadline access, state digests, runtime-value inspection, explanations, provenance, or persistent diagnostic episodes.\n- No external input valuation, settled signal valuation, output baseline, or implicit `Low` placeholders in a newly spawned machine.\n- No stateful-node, temporal-node, pulse, pending-event, or reconfiguration storage.\n- No snapshots, restoration, replay, forecast, explicit public machine fork, or ordinary `Clone` implementation for `Machine<D>`.\n- No topology patches or revision advancement.\n- Do not change the reviewed compiled-network or runtime-policy contracts unless implementation discovers a reusable specification-backed correction; any changed contract must return to draft review.\n\n## Implementation obligations\n\n- Implement `explicit-runtime-lifecycle`, `independent-machine-state`, `awaiting-initialization-state`, `no-fabricated-uninitialized-values`, `machine-local-revision-and-identity`, `basic-lifecycle-inspection`, `mutable-ownership-boundary`, and `ownership-verification` from `mossignal.runtime.machine_lifecycle`.\n- Implement `explicit-machine-association` from `mossignal.runtime.policy`.\n- Keep the canonical lifecycle runtime-valued rather than introducing `Machine<D, State>` typestates.\n- Keep immutable compiled topology shareable and private dense topology inaccessible; machine mutation must not be able to corrupt compiled invariants.\n- Retain the exact supplied `RuntimePolicy` and expose both it and its `RuntimePolicyId` from the machine.\n- Represent absence of initialization through lifecycle state. Do not allocate or populate external-input or settled-value arrays with `Low` as an absence convention.\n- Keep semantic mutation behind `&mut Machine<D>`; follow the `deliberate-machine-cloning` recommendation by not adding ordinary `Machine<D>: Clone` in this slice.\n- Add concise `SPEC:` comments only where a non-obvious storage choice enforces the lifecycle/absence invariant.\n\n## Allowed implementation freedom\n\n- The specifications do not freeze the initial numeric `NetworkRevision`; choose the simplest deterministic value, test it, and record the concrete choice in the implementation or close-out without treating it as a permanent compatibility promise.\n- Private lifecycle/store enums, ownership decomposition, immutable sharing mechanism, allocation strategy, helper names, and module placement are free when the contract invariants hold.\n- For the restricted Constant/Not model, use the smallest clear pre-transaction store. Do not create speculative public abstractions for future state families.\n- Independence may be verified with focused private unit-test inspection or another deterministic test seam that does not expose runtime storage publicly.\n\n## Expected change surface\n\n- `crates/mossignal/src/compile.rs` for the spawning entry point and any crate-private compiled support.\n- A focused runtime or machine module under `crates/mossignal/src/`.\n- `crates/mossignal/src/lib.rs` for deliberate public exports.\n- Focused unit tests beside the lifecycle implementation.\n- `docs/specs/contracts/machine-lifecycle.yaml` only for implementation-discovered reusable specification-backed facts; any change remains draft.\n- `.beads/` when implementation acceptance closes and synchronizes the bead.\n\n## Required verification\n\n- Verify spawn requires and retains the supplied validated runtime policy and exposes the matching `RuntimePolicyId`.\n- Verify every fresh machine reports `AwaitingInitialization`, false from `is_initialized()`, and no current time.\n- Verify fresh machines contain no authoritative external input valuation, settled signal values, output baseline, schedule, or fabricated `Low` state.\n- Verify two or more machines spawned from one compiled network retain equal network/fingerprint/policy identity while their mutable lifecycle/runtime storage is independent.\n- Verify the chosen initial revision is deterministic across equivalent spawns without asserting it as a permanent wire-format promise.\n- Add public API documentation or compile-time checks sufficient to preserve the canonical non-typestate surface; do not add ordinary `Machine<D>: Clone` in this slice.\n- Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Dependencies\n\n- Roadmap items 13 and 16 are implemented in the current baseline and supply `CompiledNetwork<D>` and validated `RuntimePolicy`.\n- Roadmap item 18 depends on this bead for the canonical awaiting-initialization store and owns the first lifecycle transition.\n\n## Readiness\n\nReviewed and implementation-ready. No conflicting requirement, missing fundamental semantic, or frozen compatibility promise blocks this bounded slice. The initial revision value and private storage shape are ordinary deterministic implementation freedoms.","notes":"Implementation close-out:\n- Added canonical runtime-valued Machine<D> and non-exhaustive MachineStatus<D> with an owned mutable lifecycle/revision store over a cheaply shared immutable CompiledNetwork<D>.\n- Chose deterministic initial NetworkRevision value zero as a private implementation detail, not a persistence or compatibility promise.\n- Retained the exact supplied RuntimePolicy and derive the observable RuntimePolicyId from that retained value; no redundant policy-identity storage was added.\n- AwaitingInitialization storage contains no external valuation, settled values, output baseline, schedule, explanations, or fabricated Low placeholders.\n- Machine<D> deliberately does not implement Clone.\n\nVerification:\n- focused machine lifecycle tests: passed (3 tests)\n- make check-dev: passed\n- make check-final: passed\n\nCorrective acceptance review:\n- Reviewed against implementation commit f522afe.\n- Expanded ownership verification to cover independent lifecycle and revision mutation, retained topology and policy identity, and unchanged compiled evaluation after machine-local mutation.\n- No production-code correction or contract change was required.\n\nReview verification:\n- focused machine lifecycle tests: passed (3 tests)\n- public and compile-fail doctests: passed\n- make check-dev: passed\n- make check-final: passed","status":"closed","priority":1,"issue_type":"task","assignee":"mikkofinell","created_at":"2026-07-27T20:34:12.244359Z","created_by":"mikkofinell","updated_at":"2026-07-27T21:29:06.713182Z","closed_at":"2026-07-27T21:06:57.186591Z","close_reason":"Implemented and verified machine spawning and lifecycle storage.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-r9u","title":"mossignal Implementation Contract: Stable Structural Key Types","description":"# `mossignal` Implementation Contract: Stable Structural Key Types\n\n**Status:** Ready for implementation-task transfer\n**Repository:** `Mikko-Finell/mossignal`\n**Specification baseline:** `74a63fd436ddad09837972158721bfc7b433cb6a`\n**Implementation baseline:** `9092456765eedd0daea415517e5b49cb5cc9fde9`\n**Task position:** Premium Foundation Roadmap, Part I, Task 5\n\n---\n\n## 1. Objective\n\nImplement the complete initial stable structural-key foundation for `mossignal`.\n\nThe accepted implementation must establish:\n\n* the initial structural key families;\n* type-safe signal-bearing keys;\n* typed signal-source keys;\n* erased signal-kind-aware key enums;\n* explicit durable `u128` construction;\n* deterministic equality, hashing, and canonical ordering;\n* a deterministic caller-owned `KeyAllocator`;\n* typed-to-erased conversions;\n* checked erased-to-typed conversions;\n* category-safety and conversion tests.\n\nThe implementation must preserve structural category and signal kind independently from the opaque numeric payload.\n\nFor example, all of the following may contain the same `u128` payload without representing the same identity:\n\n```rust\nNodeKey::from_u128(7);\nConnectionKey::from_u128(7);\nInPortKey::<Level>::from_u128(7);\nInPortKey::<Pulse>::from_u128(7);\n```\n\nThis task establishes structural identity values only. It must not begin graph storage, validation, compilation, runtime indexing, reconfiguration, or persistence encoding.\n\n---\n\n## 2. Source of truth\n\nThe repository specifications under `docs/specs/` are authoritative. This contract is derived from the following sections.\n\n### Primary concrete API authority\n\n* `docs/specs/concrete_rust_api_surface.md`\n\n  * § **Public module organization**\n  * § **Stable keys**\n  * § **Key construction**\n  * § **Erased stable keys**\n\nThis specification governs the required key families, public typed distinctions, durable construction, erased forms, and caller-owned allocation boundary.\n\n### Primary semantic authority\n\n* `docs/specs/api_and_semantics_spec.md`\n\n  * § **Ports and connections**\n  * § **Stable structural keys**\n\nThis specification establishes that structural keys:\n\n* identify distinct authored categories;\n* survive compatible revisions where the logical subject survives;\n* remain independent from compiled execution position;\n* require no global allocator;\n* carry no semantic meaning through numeric ordering.\n\n### Architectural authority\n\n* `docs/specs/processor_and_runtime_architecture.md`\n\n  * § **Stable keys and dense indices**\n\nThis specification establishes the architectural distinction between durable stable identity and revision-local dense execution representation.\n\n### Reconfiguration authority\n\n* `docs/specs/reconfiguration_and_topology_patch_spec.md`\n\n  * § **Ordinary stable-key preservation**\n  * § **Correspondence constraints**\n\nThese sections establish that equal stable payloads do not permit identity to change structural category, direction, or signal kind.\n\n### Persistence-adjacent authority\n\n* `docs/specs/persistence_canonical_encoding_and_compatibility_spec.md`\n\n  * § **Stable keys**\n\nThis section fixes the initial stable-key payload width as 128 bits and establishes that key category is determined independently from the payload.\n\nPersistence encoding itself remains excluded from this task.\n\n### Verification authority\n\n* `docs/specs/testing_and_verification_policy.md`\n\n  * § **Stable and resolved identity**\n\nOnly the stable-identity obligations applicable before resolved handles exist are in scope.\n\n### Roadmap authority\n\n* `docs/premium_roadmap.md`\n\n  * § **Stable structural key types**\n\nIf this contract conflicts with an authoritative repository specification at the stated baseline, the specification governs and the conflict must be surfaced rather than silently resolved.\n\n---\n\n## 3. Dependency boundary\n\nThis task builds on the already implemented signal foundation:\n\n```text\nmossignal::signal::Level\nmossignal::signal::Pulse\nmossignal::signal::SignalKind\nmossignal::signal::SignalType\n```\n\nThe implementation may import and use those values.\n\nIt must not alter their semantics or public guarantees.\n\nThe logical-time types are unrelated to structural key identity and must not become dependencies of the key module.\n\n---\n\n## 4. Scope boundary\n\n### Included\n\n* one public `key` module;\n* crate integration through `lib.rs`;\n* `NetworkKey`;\n* `NodeKey`;\n* `ConnectionKey`;\n* `ModuleInstanceKey`;\n* `ModuleInputKey<S>`;\n* `ModuleOutputKey<S>`;\n* `InPortKey<S>`;\n* `OutPortKey<S>`;\n* `ExternalInputKey<S>`;\n* `ExternalOutputKey<S>`;\n* `SignalSourceKey<S>`;\n* `AnyModuleInputKey`;\n* `AnyModuleOutputKey`;\n* `AnyInPortKey`;\n* `AnyOutPortKey`;\n* `AnyExternalInputKey`;\n* `AnyExternalOutputKey`;\n* `AnySignalSourceKey`;\n* `KeyKindMismatch`;\n* caller-owned deterministic `KeyAllocator`;\n* focused unit tests;\n* compile-fail documentation for category and signal-kind safety.\n\n### Explicitly excluded\n\n* graph storage;\n* node, port, connection, endpoint, or module definitions;\n* key uniqueness validation within a network;\n* `UncheckedNetwork`;\n* `ValidatedNetwork`;\n* `CompiledNetwork`;\n* builders;\n* global or process-wide identity allocation;\n* random identity generation;\n* UUID dependencies;\n* operating-system entropy;\n* dense runtime indices;\n* state, event, or provenance indices;\n* topology revisions;\n* revision-bound resolved handles;\n* regions;\n* diagnostic subjects;\n* `SubjectRef`;\n* `GraphSubjectRef`;\n* binding keys;\n* fingerprints or digests;\n* canonical byte encoding;\n* serialization or deserialization;\n* persistence artifacts;\n* migration or reassociation logic;\n* diagnostic codes, `Problem`, or reports;\n* new dependencies;\n* prelude design;\n* crate-root re-exports;\n* `no_std` policy changes.\n\n---\n\n## 5. Required public module surface\n\nThe implementation must expose the following public paths:\n\n```text\nmossignal::key::NetworkKey\nmossignal::key::NodeKey\nmossignal::key::ConnectionKey\nmossignal::key::ModuleInstanceKey\n\nmossignal::key::ModuleInputKey\nmossignal::key::ModuleOutputKey\nmossignal::key::InPortKey\nmossignal::key::OutPortKey\nmossignal::key::ExternalInputKey\nmossignal::key::ExternalOutputKey\nmossignal::key::SignalSourceKey\n\nmossignal::key::AnyModuleInputKey\nmossignal::key::AnyModuleOutputKey\nmossignal::key::AnyInPortKey\nmossignal::key::AnyOutPortKey\nmossignal::key::AnyExternalInputKey\nmossignal::key::AnyExternalOutputKey\nmossignal::key::AnySignalSourceKey\n\nmossignal::key::KeyKindMismatch\nmossignal::key::KeyAllocator\n```\n\nThe expected source shape is:\n\n```text\ncrates/mossignal/src/lib.rs\ncrates/mossignal/src/key.rs\n```\n\n`lib.rs` must publicly declare the `key` module.\n\nDo not create empty future modules or crate-root re-exports.\n\n---\n\n## 6. Stable-key representation\n\n## O1. Use one opaque 128-bit payload\n\nEvery directly allocated stable key must contain one private `u128` payload.\n\nThe non-generic families are:\n\n```rust\npub struct NetworkKey(/* private u128 */);\npub struct NodeKey(/* private u128 */);\npub struct ConnectionKey(/* private u128 */);\npub struct ModuleInstanceKey(/* private u128 */);\n```\n\nThe typed families are:\n\n```rust\npub struct ModuleInputKey<S: SignalType> {\n    /* private u128 */\n}\n\npub struct ModuleOutputKey<S: SignalType> {\n    /* private u128 */\n}\n\npub struct InPortKey<S: SignalType> {\n    /* private u128 */\n}\n\npub struct OutPortKey<S: SignalType> {\n    /* private u128 */\n}\n\npub struct ExternalInputKey<S: SignalType> {\n    /* private u128 */\n}\n\npub struct ExternalOutputKey<S: SignalType> {\n    /* private u128 */\n}\n```\n\nThe exact private field and marker representation may differ.\n\nRequired properties:\n\n* the numeric payload is exactly `u128`;\n* every `u128` value is valid, including zero and `u128::MAX`;\n* fields remain private;\n* keys are ordinary owned values;\n* keys borrow neither a network nor a builder;\n* there is no invalid, null, unknown, or unassigned key sentinel;\n* numeric payload alone does not determine structural category;\n* numeric payload alone does not determine signal kind;\n* no global registry is consulted.\n\nDo not expose mutable access to a key payload.\n\n---\n\n## O2. Provide explicit durable construction\n\nEvery directly allocated key family must provide:\n\n```rust\npub const fn from_u128(value: u128) -> Self;\npub const fn as_u128(self) -> u128;\n```\n\nThis applies to:\n\n```text\nNetworkKey\nNodeKey\nConnectionKey\nModuleInstanceKey\nModuleInputKey<S>\nModuleOutputKey<S>\nInPortKey<S>\nOutPortKey<S>\nExternalInputKey<S>\nExternalOutputKey<S>\n```\n\nRequired properties:\n\n* construction is infallible over the complete `u128` domain;\n* construction performs no allocation;\n* construction consults no global state;\n* `as_u128` returns the exact original payload;\n* both methods are usable in constant contexts;\n* separately reconstructed keys with the same concrete type and payload compare equal;\n* callers can deliberately retain keys across independently rebuilt definitions.\n\nDo not add:\n\n* string parsing;\n* formatting-based construction;\n* integer conversion traits;\n* random constructors;\n* UUID constructors;\n* implicit conversions between key categories.\n\n---\n\n## O3. Preserve signal kind in typed keys\n\nEvery generic key family must require:\n\n```rust\nS: SignalType\n```\n\nThe valid signal markers are therefore limited to:\n\n```text\nLevel\nPulse\n```\n\nRequired properties:\n\n* `InPortKey<Level>` and `InPortKey<Pulse>` are distinct Rust types;\n* equivalent distinctions hold for every generic key family;\n* a custom type cannot be used as the signal marker;\n* there is no untyped generic fallback;\n* there is no runtime constructor accepting an arbitrary `SignalKind` and returning one generic typed key;\n* changing signal kind requires explicit erasure followed by checked conversion or explicit reconstruction in another typed family.\n\nThe implementation must not expose a cast, transmute helper, or unchecked kind reinterpretation.\n\n---\n\n## 7. Value traits and deterministic behavior\n\n## O4. Implement stable value traits\n\nEvery stable key type must implement:\n\n```text\nDebug\nClone\nCopy\nPartialEq\nEq\nPartialOrd\nOrd\nHash\n```\n\nThis includes:\n\n* all directly allocated keys;\n* `SignalSourceKey<S>`;\n* every erased key enum.\n\nGeneric key traits must not impose additional trait requirements on `S` beyond `SignalType`.\n\nIn particular, key traits must not depend on derives that accidentally require the marker to implement unrelated traits.\n\nRequired equality law for one concrete key type:\n\n```text\nkey_a == key_b iff key_a.as_u128() == key_b.as_u128()\n```\n\nRequired homogeneous ordering law:\n\n```text\nkey_a.cmp(key_b) == key_a.as_u128().cmp(&key_b.as_u128())\n```\n\nThe ordering exists only to provide deterministic canonical ordering.\n\nIt must not be documented or treated as:\n\n* creation order;\n* graph order;\n* topology order;\n* priority;\n* age;\n* ownership;\n* causal order;\n* allocation chronology across categories.\n\n`Debug` must identify the concrete key type and provide useful opaque-value inspection. Exact debug wording is not a compatibility contract.\n\nDo not implement:\n\n* `Display`;\n* `Default`;\n* arithmetic operators;\n* successor or predecessor operations;\n* iteration;\n* borrowing traits that expose the private field by reference;\n* serialization derives.\n\n---\n\n## O5. Domain-separate structural hashing\n\nHashing must include the structural key category.\n\nFor signal-bearing key families, hashing must also include the signal kind.\n\nConceptually, the hashed input for one direct key is:\n\n```text\nstructural category tag\nsignal kind tag when applicable\nu128 payload\n```\n\nExamples of distinct hash domains include:\n\n```text\nNetworkKey\nNodeKey\nConnectionKey\nModuleInstanceKey\nModuleInputKey<Level>\nModuleInputKey<Pulse>\nModuleOutputKey<Level>\nModuleOutputKey<Pulse>\nInPortKey<Level>\nInPortKey<Pulse>\nOutPortKey<Level>\nOutPortKey<Pulse>\nExternalInputKey<Level>\nExternalInputKey<Pulse>\nExternalOutputKey<Level>\nExternalOutputKey<Pulse>\n```\n\nRequired properties:\n\n* two equal values of one concrete key type feed identical data to a hasher;\n* different payloads of one type feed different payload data;\n* equal payloads in different structural categories feed category-distinct data;\n* equal payloads in different signal kinds feed kind-distinct data;\n* hash behavior does not depend on allocation history;\n* hash behavior does not consult global state.\n\nThe resulting numeric output of any particular hasher is not a stable persistence digest and must not become a compatibility contract.\n\nTests should verify the domain-separated hash input using an appropriate test hasher rather than assuming that arbitrary distinct hash inputs can never collide under `DefaultHasher`.\n\n---\n\n## 8. Typed signal sources\n\n## O6. Implement `SignalSourceKey<S>`\n\nProvide a typed signal-source enum broadly equivalent to:\n\n```rust\n#[non_exhaustive]\npub enum SignalSourceKey<S: SignalType> {\n    ExternalInput(ExternalInputKey<S>),\n    OutPort(OutPortKey<S>),\n}\n```\n\nThe exact variant names must be:\n\n```text\nExternalInput\nOutPort\n```\n\nA signal source is therefore either:\n\n* a typed external input; or\n* a typed output port.\n\nRequired properties:\n\n* the signal kind remains encoded by `S`;\n* the source category remains encoded by the enum variant;\n* equal numeric payloads in the two variants remain distinct;\n* signal sources are not independently allocated;\n* no third source category is introduced;\n* input ports cannot become sources;\n* external outputs cannot become sources;\n* module input or output keys are not substituted for network signal sources.\n\nProvide:\n\n```rust\nimpl<S: SignalType> From<ExternalInputKey<S>> for SignalSourceKey<S>;\nimpl<S: SignalType> From<OutPortKey<S>> for SignalSourceKey<S>;\n```\n\nTyped signal-source ordering must be:\n\n```text\nExternalInput before OutPort\nthen underlying key payload\n```\n\nThis is canonical deterministic ordering only and conveys no semantic precedence.\n\nHashing must include:\n\n```text\nsignal-source wrapper category\nsignal kind\nsource variant\nunderlying payload\n```\n\n---\n\n## 9. Erased signal-bearing key enums\n\n## O7. Implement the complete erased key family\n\nProvide:\n\n```rust\n#[non_exhaustive]\npub enum AnyModuleInputKey {\n    Level(ModuleInputKey<Level>),\n    Pulse(ModuleInputKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnyModuleOutputKey {\n    Level(ModuleOutputKey<Level>),\n    Pulse(ModuleOutputKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnyInPortKey {\n    Level(InPortKey<Level>),\n    Pulse(InPortKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnyOutPortKey {\n    Level(OutPortKey<Level>),\n    Pulse(OutPortKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnyExternalInputKey {\n    Level(ExternalInputKey<Level>),\n    Pulse(ExternalInputKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnyExternalOutputKey {\n    Level(ExternalOutputKey<Level>),\n    Pulse(ExternalOutputKey<Pulse>),\n}\n\n#[non_exhaustive]\npub enum AnySignalSourceKey {\n    Level(SignalSourceKey<Level>),\n    Pulse(SignalSourceKey<Pulse>),\n}\n```\n\nRequired properties:\n\n* erasure removes the compile-time signal marker from the outer type but retains it in the enum variant;\n* structural category remains fixed by the erased enum type;\n* signal kind remains recoverable without inspecting or decoding the numeric payload;\n* no unknown or custom signal-kind variant is introduced;\n* erased values remain owned, copyable values;\n* erased values borrow no source key;\n* erasure performs no allocation.\n\nEvery erased enum must provide:\n\n```rust\npub const fn kind(self) -> SignalKind;\n```\n\nThe method must return:\n\n```text\nLevel variant -> SignalKind::Level\nPulse variant -> SignalKind::Pulse\n```\n\nDo not provide an API that returns only the raw payload while discarding the variant.\n\n---\n\n## O8. Define canonical erased ordering\n\nEvery erased signal-bearing enum must implement ordering explicitly.\n\nThe required order is:\n\n```text\nSignalKind::Level before SignalKind::Pulse\nthen underlying typed-key order\n```\n\nFor `AnySignalSourceKey`, ordering is:\n\n```text\nsignal kind\nthen source category:\n    ExternalInput before OutPort\nthen payload\n```\n\nThe implementation must not rely implicitly on:\n\n* enum declaration discriminants;\n* source declaration position;\n* compiler-selected discriminant values;\n* debug text;\n* hash output.\n\nThe enum declaration should still use the intuitive `Level`, then `Pulse` layout, but the `Ord` implementation must encode the intended comparison deliberately.\n\nThe ordering is canonical representation order only. It does not make level signals semantically prior to pulse signals.\n\n---\n\n## O9. Domain-separate erased hashing\n\nErased key hashing must include:\n\n```text\nerased structural category\nsignal kind\nunderlying category where applicable\npayload\n```\n\nRequired distinctions include:\n\n```text\nAnyInPortKey::Level(InPortKey::from_u128(5))\n!= hash domain of\nAnyInPortKey::Pulse(InPortKey::from_u128(5))\n```\n\nand:\n\n```text\nAnySignalSourceKey::Level(\n    SignalSourceKey::ExternalInput(\n        ExternalInputKey::from_u128(5)\n    )\n)\n```\n\nmust use a distinct hash input from:\n\n```text\nAnySignalSourceKey::Level(\n    SignalSourceKey::OutPort(\n        OutPortKey::from_u128(5)\n    )\n)\n```\n\nHashing remains ordinary Rust `Hash`, not canonical persistence hashing.\n\n---\n\n## 10. Typed and erased conversion API\n\n## O10. Provide infallible typed-to-erased conversions\n\nEvery typed key must convert into its corresponding erased enum through `From`.\n\nRequired implementations include:\n\n```rust\nFrom<ModuleInputKey<Level>> for AnyModuleInputKey\nFrom<ModuleInputKey<Pulse>> for AnyModuleInputKey\n\nFrom<ModuleOutputKey<Level>> for AnyModuleOutputKey\nFrom<ModuleOutputKey<Pulse>> for AnyModuleOutputKey\n\nFrom<InPortKey<Level>> for AnyInPortKey\nFrom<InPortKey<Pulse>> for AnyInPortKey\n\nFrom<OutPortKey<Level>> for AnyOutPortKey\nFrom<OutPortKey<Pulse>> for AnyOutPortKey\n\nFrom<ExternalInputKey<Level>> for AnyExternalInputKey\nFrom<ExternalInputKey<Pulse>> for AnyExternalInputKey\n\nFrom<ExternalOutputKey<Level>> for AnyExternalOutputKey\nFrom<ExternalOutputKey<Pulse>> for AnyExternalOutputKey\n\nFrom<SignalSourceKey<Level>> for AnySignalSourceKey\nFrom<SignalSourceKey<Pulse>> for AnySignalSourceKey\n```\n\nRequired properties:\n\n* conversion is lossless;\n* the correct signal-kind variant is selected;\n* payload and source category remain unchanged;\n* conversion performs no allocation;\n* conversion cannot fail.\n\nDo not add blanket conversions that erase structural category into one universal key enum.\n\n---\n\n## O11. Provide checked erased-to-typed conversions\n\nEvery erased key enum must support `TryFrom` into both valid typed forms.\n\nRepresentative examples:\n\n```rust\nimpl TryFrom<AnyInPortKey> for InPortKey<Level> {\n    type Error = KeyKindMismatch;\n}\n\nimpl TryFrom<AnyInPortKey> for InPortKey<Pulse> {\n    type Error = KeyKindMismatch;\n}\n```\n\nEquivalent implementations are required for:\n\n```text\nModuleInputKey<Level>\nModuleInputKey<Pulse>\nModuleOutputKey<Level>\nModuleOutputKey<Pulse>\nInPortKey<Level>\nInPortKey<Pulse>\nOutPortKey<Level>\nOutPortKey<Pulse>\nExternalInputKey<Level>\nExternalInputKey<Pulse>\nExternalOutputKey<Level>\nExternalOutputKey<Pulse>\nSignalSourceKey<Level>\nSignalSourceKey<Pulse>\n```\n\nRequired properties:\n\n* matching-kind conversion returns the original typed value exactly;\n* wrong-kind conversion returns `KeyKindMismatch`;\n* wrong-kind conversion never changes or reinterprets the payload;\n* wrong-kind conversion does not panic;\n* conversion does not use unsafe code;\n* source-category variants survive `AnySignalSourceKey` round trips.\n\nThere must be no unchecked erased-to-typed conversion.\n\n---\n\n## O12. Implement `KeyKindMismatch`\n\nProvide one dedicated public error type broadly equivalent to:\n\n```rust\n#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]\npub struct KeyKindMismatch {\n    expected: SignalKind,\n    actual: SignalKind,\n}\n```\n\nThe exact private field representation may differ.\n\nProvide:\n\n```rust\nimpl KeyKindMismatch {\n    pub const fn expected(self) -> SignalKind;\n    pub const fn actual(self) -> SignalKind;\n}\n```\n\nRequired properties:\n\n* the error retains the expected and actual signal kinds;\n* fields remain private;\n* callers cannot construct arbitrary values through public fields;\n* the type implements:\n\n  * `Debug`;\n  * `Clone`;\n  * `Copy`;\n  * `PartialEq`;\n  * `Eq`;\n  * `Hash`;\n  * `Display`;\n  * `std::error::Error`;\n* `Display` gives a concise description of the expected and actual signal kinds;\n* exact wording is not a compatibility contract;\n* construction requires no heap allocation;\n* the error is sufficient for future lossless mapping to structured kind-mismatch diagnostics.\n\nDo not implement the diagnostic framework in this task.\n\nIn particular, do not add:\n\n* `DiagnosticCode`;\n* `Problem`;\n* severity;\n* responsibility;\n* problem evidence;\n* validation reports;\n* runtime failure wrappers.\n\n---\n\n## 11. Caller-owned allocation\n\n## O13. Implement `KeyAllocator`\n\nProvide:\n\n```rust\npub struct KeyAllocator {\n    /* private */\n}\n```\n\nand:\n\n```rust\nimpl KeyAllocator {\n    pub const fn new(namespace: u64) -> Self;\n\n    pub fn network(&mut self) -> NetworkKey;\n    pub fn node(&mut self) -> NodeKey;\n    pub fn connection(&mut self) -> ConnectionKey;\n    pub fn module_instance(&mut self) -> ModuleInstanceKey;\n\n    pub fn module_input<S: SignalType>(&mut self) -> ModuleInputKey<S>;\n    pub fn module_output<S: SignalType>(&mut self) -> ModuleOutputKey<S>;\n\n    pub fn in_port<S: SignalType>(&mut self) -> InPortKey<S>;\n    pub fn out_port<S: SignalType>(&mut self) -> OutPortKey<S>;\n\n    pub fn external_input<S: SignalType>(&mut self) -> ExternalInputKey<S>;\n    pub fn external_output<S: SignalType>(&mut self) -> ExternalOutputKey<S>;\n}\n```\n\nThe allocator is an ordinary caller-owned deterministic value.\n\nIt must not use:\n\n* global mutable state;\n* static counters;\n* atomics;\n* thread-local state;\n* randomness;\n* operating-system entropy;\n* UUID generation;\n* timestamps;\n* process identifiers;\n* memory addresses;\n* hidden registries.\n\n`KeyAllocator::new` must be usable in a constant context.\n\n---\n\n## O14. Use namespaced deterministic payloads\n\nEvery allocator-produced payload must use:\n\n```text\nhigh 64 bits = allocator namespace\nlow 64 bits  = domain-local counter\n```\n\nConceptually:\n\n```rust\n((namespace as u128) << 64) | counter as u128\n```\n\nCounters begin at zero.\n\nRequired consequences:\n\n```text\nKeyAllocator::new(10).node().as_u128()\n    = (10 << 64) | 0\n\nthe next node allocation\n    = (10 << 64) | 1\n```\n\nThe same rule applies independently to every allocation domain.\n\nRequired properties:\n\n* equal namespace and equal domain-local allocation history reproduce equal keys;\n* different namespaces produce different payloads for corresponding allocations;\n* the allocator provides deterministic local allocation, not universal uniqueness;\n* namespace coordination remains caller responsibility;\n* validation of duplicate keys within a network remains a later subsystem responsibility;\n* the allocator does not record network identity;\n* allocator state is not itself a structural key.\n\nDo not reserve any namespace or counter value.\n\n---\n\n## O15. Keep allocation domains independent\n\nThe allocator must maintain one independent counter for every concrete key domain.\n\nThe allocation domains are:\n\n```text\nNetworkKey\nNodeKey\nConnectionKey\nModuleInstanceKey\n\nModuleInputKey<Level>\nModuleInputKey<Pulse>\n\nModuleOutputKey<Level>\nModuleOutputKey<Pulse>\n\nInPortKey<Level>\nInPortKey<Pulse>\n\nOutPortKey<Level>\nOutPortKey<Pulse>\n\nExternalInputKey<Level>\nExternalInputKey<Pulse>\n\nExternalOutputKey<Level>\nExternalOutputKey<Pulse>\n```\n\nThere are therefore sixteen independent counter domains.\n\nRequired properties:\n\n* allocating a node does not advance the connection counter;\n* allocating a connection does not advance the node counter;\n* allocating a level input port does not advance the pulse input-port counter;\n* allocating an external input does not advance an output-port counter;\n* unrelated structural additions do not perturb later allocations in another domain;\n* first allocations in several categories may intentionally have equal numeric payloads;\n* category and signal-kind safety must therefore come from the type and erased variant, not from embedding category into the payload.\n\nExample:\n\n```rust\nlet mut allocator = KeyAllocator::new(4);\n\nlet node = allocator.node();\nlet connection = allocator.connection();\nlet level_input = allocator.in_port::<Level>();\nlet pulse_input = allocator.in_port::<Pulse>();\n\nassert_eq!(node.as_u128(), connection.as_u128());\nassert_eq!(node.as_u128(), level_input.as_u128());\nassert_eq!(node.as_u128(), pulse_input.as_u128());\n```\n\nThese values remain distinct identities despite equal payloads.\n\n---\n\n## O16. Define allocator exhaustion behavior\n\nEach domain supports the complete `u64` counter range:\n\n```text\n0 ..= u64::MAX\n```\n\nThe allocator must:\n\n* issue the `u64::MAX` counter value exactly once;\n* never wrap to zero;\n* never repeat a key within one allocator domain;\n* panic when allocation is attempted after all `2^64` values in that domain have been issued.\n\nThe panic is an unreachable-capacity guard, not an ordinary caller-controlled validation failure.\n\nThe implementation should use a private representation capable of distinguishing:\n\n```text\nnext counter = u64::MAX\n```\n\nfrom:\n\n```text\ndomain exhausted\n```\n\nNo public allocation error type is introduced in this task.\n\nTests may establish near-exhaustion behavior by constructing private allocator state from the module-local test module. They must not perform a large allocation loop.\n\n---\n\n## O17. Prevent accidental allocator duplication\n\n`KeyAllocator` must not implement:\n\n```text\nClone\nCopy\nDefault\n```\n\nRationale:\n\n* cloning allocator state would allow accidental duplicate issuance;\n* copying allocator state would do the same implicitly;\n* a default namespace would obscure caller ownership and namespace choice.\n\n`KeyAllocator` should implement `Debug`.\n\nIts debug representation may expose namespace and allocation progress for development use, but exact wording is not a compatibility contract.\n\nDo not add:\n\n* equality;\n* ordering;\n* hashing;\n* serialization;\n* allocator merging;\n* allocator splitting;\n* namespace mutation;\n* counter reset;\n* arbitrary counter setters;\n* public access to the internal counter table.\n\n---\n\n## 12. Documentation requirements\n\n## O18. Document the identity boundary\n\nEvery public item introduced by this task must have concise Rust documentation sufficient for:\n\n```bash\nRUSTDOCFLAGS=\"-Dwarnings\" cargo doc --workspace --all-features --no-deps\n```\n\nDocumentation must make these facts clear where relevant:\n\n* structural keys are durable authored identities;\n* keys are independent from dense runtime positions;\n* numeric ordering carries no semantic meaning;\n* equal numeric payloads in different categories remain distinct;\n* typed keys preserve `Level` versus `Pulse`;\n* erased enums retain signal kind in their variants;\n* explicit `u128` construction supports deliberate durable reconstruction;\n* `KeyAllocator` is caller-owned and deterministic;\n* allocator namespaces are caller-coordinated;\n* the allocator is not a global uniqueness service;\n* separate key domains may intentionally produce equal payloads;\n* wrong-kind conversion returns `KeyKindMismatch`.\n\nDocumentation must not claim that:\n\n* graph storage already exists;\n* allocated keys have been validated as unique in a network;\n* keys are persisted automatically;\n* ordering represents creation or topology;\n* allocator output is globally unique;\n* dense runtime resolution exists;\n* reconfiguration exists;\n* canonical encoding exists.\n\n---\n\n## 13. Required verification\n\nTests must establish the semantic and type boundaries rather than merely instantiate each type.\n\n## 13.1 Direct key-family verification\n\nFor every directly allocated key family, test:\n\n```text\n0\n1\na representative mixed-bit value\nu128::MAX\n```\n\nVerify:\n\n* `from_u128` and `as_u128` round-trip exactly;\n* equality follows payload equality within one concrete type;\n* ordering follows payload ordering within one concrete type;\n* copying preserves value;\n* cloning preserves value;\n* debug output is non-empty;\n* hashing is deterministic for equal values.\n\nThe test may use shared helpers or macros, but failure output must identify the involved key family.\n\n---\n\n## 13.2 Typed-trait verification\n\nUse a marker type that cannot satisfy unrelated traits to ensure generic trait implementations do not accidentally acquire unnecessary marker bounds where that can be demonstrated internally.\n\nAt minimum, verify that every generic key family has the required value traits for both:\n\n```text\nLevel\nPulse\n```\n\nA compile-fail doctest must demonstrate that a custom type cannot be used as the signal marker.\n\nRepresentative failure:\n\n```rust\nuse mossignal::key::InPortKey;\n\nstruct CustomSignal;\n\nlet _ = InPortKey::<CustomSignal>::from_u128(1);\n```\n\nThis must fail because `CustomSignal` does not implement the sealed `SignalType`.\n\n---\n\n## 13.3 Category-safety compile failures\n\nCompile-fail documentation must demonstrate at least:\n\n### Structural category mismatch\n\n```rust\nuse mossignal::key::{ConnectionKey, NodeKey};\n\nfn accepts_node(_: NodeKey) {}\n\naccepts_node(ConnectionKey::from_u128(1));\n```\n\n### Signal-kind mismatch\n\n```rust\nuse mossignal::key::InPortKey;\nuse mossignal::signal::{Level, Pulse};\n\nfn accepts_level(_: InPortKey<Level>) {}\n\naccepts_level(InPortKey::<Pulse>::from_u128(1));\n```\n\n### Direction mismatch\n\n```rust\nuse mossignal::key::{InPortKey, OutPortKey};\nuse mossignal::signal::Level;\n\nfn accepts_input(_: InPortKey<Level>) {}\n\naccepts_input(OutPortKey::<Level>::from_u128(1));\n```\n\nThe implementation must not add implicit conversions that make these examples compile.\n\n---\n\n## 13.4 Equal-payload category verification\n\nConstruct equal payloads across all major distinctions.\n\nAt minimum, include:\n\n```text\nNodeKey versus ConnectionKey\nNodeKey versus ModuleInstanceKey\nInPortKey<Level> versus OutPortKey<Level>\nInPortKey<Level> versus InPortKey<Pulse>\nExternalInputKey<Level> versus ExternalOutputKey<Level>\nModuleInputKey<Pulse> versus ModuleOutputKey<Pulse>\nSignalSourceKey::ExternalInput versus SignalSourceKey::OutPort\n```\n\nVerify:\n\n* their raw payloads may be equal;\n* the Rust type system prevents direct equality across different concrete types;\n* erased enum variants preserve signal kind;\n* signal-source variants preserve source category;\n* hash input is domain-separated;\n* canonical erased ordering remains deterministic.\n\nDo not attempt to prove type inequality through runtime equality between incompatible Rust types.\n\n---\n\n## 13.5 Hash-domain verification\n\nProvide a test hasher that records the bytes or primitive writes supplied through `Hash`.\n\nUse it to verify:\n\n* equal values of one type produce identical hash input;\n* equal payloads in different non-generic categories produce distinct hash input;\n* equal payloads under `Level` and `Pulse` produce distinct hash input;\n* external-input and output-port signal sources produce distinct hash input;\n* erased structural categories remain separated;\n* no test depends on one platform-specific `DefaultHasher` result.\n\nThe test must not claim cryptographic collision resistance.\n\n---\n\n## 13.6 Homogeneous ordering verification\n\nFor each direct key family, verify:\n\n```text\n0 < 1 < representative value < u128::MAX\n```\n\nFor generic families, verify the rule independently for `Level` and `Pulse`.\n\nOrdering tests must not compare incompatible concrete Rust types.\n\n---\n\n## 13.7 Erased ordering verification\n\nFor every erased enum, verify:\n\n```text\nLevel(payload_a) < Level(payload_b) when payload_a < payload_b\nLevel(any payload) < Pulse(any payload)\nPulse(payload_a) < Pulse(payload_b) when payload_a < payload_b\n```\n\nFor `AnySignalSourceKey`, verify:\n\n```text\nLevel ExternalInput before Level OutPort\nLevel sources before Pulse sources\nPulse ExternalInput before Pulse OutPort\npayload ordering within one kind and source category\n```\n\nTests should include deliberately inverted payload magnitudes to prove that variant ordering is considered before payload ordering.\n\nExample:\n\n```text\nLevel(u128::MAX) < Pulse(0)\n```\n\n---\n\n## 13.8 Typed-to-erased conversion verification\n\nFor every typed-to-erased `From` implementation, verify:\n\n* the correct enum variant;\n* exact payload preservation;\n* exact source-category preservation;\n* correct `kind()` result.\n\nExercise at least payloads:\n\n```text\n0\n1\nu128::MAX\n```\n\nThe full conversion matrix must be covered.\n\n---\n\n## 13.9 Erased-to-typed conversion verification\n\nFor every erased enum:\n\n* convert the level variant to its level typed form successfully;\n* reject conversion of the level variant to its pulse typed form;\n* convert the pulse variant to its pulse typed form successfully;\n* reject conversion of the pulse variant to its level typed form.\n\nFor every failure, verify:\n\n```text\nexpected kind\nactual kind\n```\n\nFor `AnySignalSourceKey`, verify that successful round trips preserve:\n\n```text\nExternalInput\nOutPort\n```\n\nWrong-kind conversion must never panic.\n\n---\n\n## 13.10 Complete round-trip verification\n\nFor every applicable typed key:\n\n```text\ntyped\n-> erased\n-> matching typed\n```\n\nmust equal the original typed value exactly.\n\nFor signal sources, the round trip must preserve both:\n\n```text\nsignal kind\nsource category\n```\n\n---\n\n## 13.11 Allocator sequence verification\n\nFor one namespace, verify each domain begins at counter zero and advances independently.\n\nFor representative domains:\n\n```text\nfirst allocation  -> counter 0\nsecond allocation -> counter 1\nthird allocation  -> counter 2\n```\n\nVerify the exact payload formula.\n\nAt minimum, test all sixteen allocator domains at their first allocation.\n\n---\n\n## 13.12 Allocator namespace verification\n\nCreate allocators with at least:\n\n```text\nnamespace 0\nnamespace 1\nnamespace u64::MAX\n```\n\nVerify:\n\n* corresponding domain counters begin at zero;\n* namespaces occupy the high 64 bits;\n* counter values occupy the low 64 bits;\n* corresponding allocations from different namespaces differ;\n* no namespace value is reserved.\n\n---\n\n## 13.13 Allocator domain-independence verification\n\nInterleave allocations across unrelated domains.\n\nVerify that each domain produces the same sequence it would have produced without the interleaving.\n\nRequired examples include:\n\n```text\nnode allocations interleaved with connections\nlevel input ports interleaved with pulse input ports\ninput ports interleaved with output ports\nexternal inputs interleaved with external outputs\nmodule inputs interleaved with network ports\n```\n\nAlso verify that the first allocation from several distinct domains may have the same `u128` payload.\n\n---\n\n## 13.14 Deterministic reconstruction verification\n\nCreate two independent allocators with:\n\n```text\nthe same namespace\nthe same sequence within each allocation domain\n```\n\nVerify that they produce equal keys in each corresponding concrete key type.\n\nThen vary either:\n\n```text\nnamespace\nor domain-local sequence\n```\n\nand verify that corresponding keys differ.\n\nThe test must not claim uniqueness across separately coordinated callers.\n\n---\n\n## 13.15 Exhaustion verification\n\nThrough module-private test construction, place one allocation domain at:\n\n```text\nnext counter = u64::MAX\n```\n\nVerify:\n\n* the maximum counter key is issued;\n* the domain becomes exhausted;\n* the next allocation in that domain panics;\n* another allocation domain remains usable;\n* no counter wraps;\n* no key is repeated.\n\nThe test must not loop through the entire counter domain.\n\n---\n\n## 13.16 Allocator trait-bound verification\n\nThe public API must demonstrate that `KeyAllocator` is not clonable or copyable.\n\nA compile-fail doctest should demonstrate that cloning is unavailable.\n\nRepresentative failure:\n\n```rust\nuse mossignal::key::KeyAllocator;\n\nlet allocator = KeyAllocator::new(1);\nlet _duplicate = allocator.clone();\n```\n\nNo compile-fail test is required for `Default` if the absence is already clear and covered through API review.\n\n---\n\n## 13.17 Repository gates\n\nThe completed implementation must pass:\n\n```bash\nmake check-dev\nmake check-final\n```\n\nA test count is not an acceptance criterion.\n\nComplete coverage of the key families, category distinctions, conversion matrix, allocator domains, and boundary behavior is required.\n\n---\n\n## 14. Forbidden outcomes\n\nThe implementation is noncompliant if it does any of the following:\n\n* uses a payload width other than `u128`;\n* makes zero an invalid or missing key;\n* exposes key fields publicly;\n* uses one untyped key struct for all structural categories;\n* uses type aliases instead of distinct key types;\n* permits `InPortKey<Level>` where `InPortKey<Pulse>` is required;\n* permits a custom signal marker;\n* infers structural category from numeric ranges or bit patterns;\n* embeds category tags into the allocator payload instead of preserving category through types and variants;\n* treats equal payloads in different categories as one subject;\n* treats equal level and pulse payloads as one typed key;\n* exposes implicit cross-category conversions;\n* exposes unchecked erased-to-typed conversion;\n* loses source category through `SignalSourceKey` erasure;\n* omits a required erased enum;\n* adds a universal `AnyStructuralKey`;\n* adds `SubjectRef` or `GraphSubjectRef`;\n* makes ordering depend on hash output;\n* gives numeric ordering semantic meaning;\n* makes hashing depend only on payload while omitting category and signal kind;\n* treats Rust `Hash` output as a persisted digest;\n* uses enum declaration discriminants as an undocumented canonical ordering mechanism;\n* uses a single shared allocator counter for all key domains;\n* allows unrelated allocation categories to perturb one another;\n* uses global mutable allocation state;\n* uses atomics or thread-local allocation;\n* uses randomness, UUIDs, timestamps, process IDs, or memory addresses;\n* claims allocator output is globally unique;\n* silently wraps an exhausted counter;\n* repeats a key after exhaustion;\n* makes `KeyAllocator` cloneable, copyable, or default-constructible;\n* exposes allocator counter mutation or reset;\n* adds graph storage;\n* validates key uniqueness within a graph;\n* adds dense indices;\n* adds resolved handles;\n* adds revisions or fingerprints;\n* adds persistence encoding;\n* adds serialization derives;\n* introduces new dependencies;\n* changes existing signal or time semantics;\n* creates speculative compatibility shims or placeholder future APIs.\n\n---\n\n## 15. Expected implementation shape\n\nA straightforward implementation is expected to contain:\n\n```text\nprivate category and allocation-domain tags\nsmall macros or shared helpers for direct key families\nmanual generic trait implementations where needed\ntyped signal-source enums\nerased signal-kind enums\nconversion implementations\none structured mismatch error\none caller-owned allocator\nfocused unit tests\ncompile-fail doctests\n```\n\nMacros may reduce repetition, but generated behavior must remain readable and auditable.\n\nAvoid a macro design that obscures:\n\n* which public types exist;\n* which category tag each type uses;\n* which allocator domain each type uses;\n* the explicit conversion matrix;\n* ordering rules;\n* signal-kind distinctions.\n\nThe implementation should favor simple closed-set dispatch over speculative generic frameworks.\n\n---\n\n## 16. Completion criteria\n\nThis task is complete only when:\n\n1. every required key family exists under `mossignal::key`;\n2. every direct key stores one private `u128`;\n3. every direct key supports explicit durable construction;\n4. generic key families accept only sealed signal types;\n5. structural categories remain distinct despite equal payloads;\n6. level and pulse keys remain distinct despite equal payloads;\n7. deterministic equality, ordering, and domain-separated hashing are implemented;\n8. `SignalSourceKey<S>` preserves typed source category;\n9. all seven erased enums exist;\n10. erased ordering is explicit and canonical;\n11. all typed-to-erased conversions exist;\n12. all matching erased-to-typed conversions succeed;\n13. all wrong-kind conversions return structured mismatch errors;\n14. `KeyAllocator` is caller-owned and deterministic;\n15. allocator payloads use namespace plus domain-local counter;\n16. all sixteen allocation domains are independent;\n17. exhaustion cannot wrap or repeat a key;\n18. `KeyAllocator` is not cloneable, copyable, or defaultable;\n19. public documentation describes the identity boundary accurately;\n20. focused tests and compile-fail doctests cover the required guarantees;\n21. no excluded subsystem has been started;\n22. `make check-dev` passes;\n23. `make check-final` passes.\n\nThe final implementation should leave the repository ready for later authored graph work without coupling stable identity to graph storage, runtime layout, global allocation, or persistence representation.\n","notes":"Implementation approval:\\nStatus: Approved\\nReviewed against accepted HEAD: 7491274dcee2f78b7c333284af20db7719b8b0d9\\n\\nVerification:\\n- make check-final: passed\\n- focused structural-key tests: passed\\n\\nAccepted scope:\\n- stable direct and typed key families\\n- typed and erased signal-source identities\\n- checked typed/erased conversions and KeyKindMismatch\\n- deterministic sixteen-domain KeyAllocator with exhaustion protection\\n- public documentation, compile-fail boundaries, and focused verification","status":"closed","priority":2,"issue_type":"task","created_at":"2026-07-13T19:59:36.327306Z","created_by":"mikkofinell","updated_at":"2026-07-13T20:26:35.569992Z","closed_at":"2026-07-13T20:26:35.569941Z","close_reason":"Implemented and verified the complete stable structural-key foundation.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-structural-validation-7ah","title":"Implement restricted authored-graph structural validation","description":"## Objective\n\nImplement Premium Foundation Roadmap item 9: the structural validation phase for the restricted level-only `Constant`/`Not` authored graph. It must report all independently discoverable structural defects deterministically and produce only the private candidate consumed by roadmap item 10.\n\n## Contract basis\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed, used unchanged\n  - role: lossless unchecked graph records, stable keys, endpoint categories, malformed-claim retention, and restricted node shapes.\n\n- `mossignal.diagnostics.subject_references`\n  - `docs/specs/contracts/diagnostic-subject-references.yaml`\n  - status: reviewed, used unchanged\n  - role: stable diagnostic subjects and initial canonical subject ordering.\n\n- `mossignal.diagnostics.problem_model`\n  - `docs/specs/contracts/diagnostic-problem-model.yaml`\n  - status: reviewed, used unchanged\n  - role: opening validation codes, fixed classifications, typed evidence, and problem construction.\n\n- `mossignal.diagnostics.collections`\n  - `docs/specs/contracts/diagnostic-collections.yaml`\n  - status: reviewed, used unchanged\n  - role: canonical order, condition identity, evidence merge laws, and opening structural-validation diagnostic behavior.\n\n- `mossignal.diagnostics.artifact_reports`\n  - `docs/specs/contracts/artifact-reports.yaml`\n  - status: reviewed, used unchanged\n  - role: artifact absence under blocking findings and safe report accumulation.\n\n- `mossignal.graph.structural_validation`\n  - `docs/specs/contracts/structural-validation.yaml`\n  - status: draft, created by preparation\n  - role: task-specific reusable boundary for restricted structural validation and its private pre-semantic candidate.\n\n## Included scope\n\n- Add the structural validation entry point for `UncheckedNetwork<D>` and the internal validation workspace/candidate it needs.\n- Detect duplicate stable keys in the opening node, input-port, output-port, connection, external-input, and external-output scopes without losing conflicting authored claims.\n- Validate connection endpoint existence and incidence, source/destination direction, and signal-kind compatibility.\n- Enforce the opening single-driver input policy while preserving intrinsic fan-out.\n- Validate fixed port groups for the restricted shapes: Constant has zero inputs and one level output; Not has one level input and one level output. Missing required Not input must be diagnosed separately from malformed port-group arity.\n- Validate every external-output source reference as a structural source endpoint.\n- Emit only the applicable opening blocking `validation.*` conditions with the existing diagnostic kernel's canonical ordering, condition identity, and evidence merge semantics.\n- Collect independent safe findings and produce no public artifact when any blocking error remains.\n- On structural success, retain only private structurally usable data required by the reaction-dependency extraction task.\n\n## Exclusions and non-goals\n\n- Do not extract reaction dependencies, inspect SCCs, detect reaction cycles, define topological order, or construct `ValidatedNetwork`.\n- Do not add dense indices, compiled descriptors, evaluator behavior, machine state, input artifacts, modules, hierarchy, persistence, or topology patches.\n- Do not extend the diagnostic catalogue or create non-blocking quality diagnostics for this slice.\n- Do not discard, repair, default, or panic on malformed unchecked data.\n- Do not expose a new public lifecycle stage or public validation-candidate type.\n\n## Implementation obligations\n\n- Respect every required facet of `mossignal.graph.structural_validation`, especially rules `validates-restricted-structural-invariants`, `reports-opening-blocking-conditions`, `accumulates-safe-independent-findings`, `preserves-stable-subjects`, and `candidate-is-private-and-pre-semantic`.\n- Keep diagnostic primary subjects, discriminators, and evidence aligned with the reviewed opening diagnostic contracts; stable keys and subject references, not traversal or storage positions, are authoritative.\n- Treat malformed definitions as caller-controlled data and return structured findings without panicking.\n- Preserve enough lossless association to continue checking unrelated defects safely after individual malformed claims.\n- Keep candidate construction private to crate internals; it must not imply that structural validation alone satisfies the public validated-network lifecycle.\n\n## Allowed implementation freedom\n\n- Private candidate type, internal storage, pass decomposition, map choice, and helper names are free.\n- Multiple deterministic validation passes are permitted.\n- The implementation may retain private stable-keyed lookup views for later task 10, but may not introduce dense compiled indices.\n- Use concise `SPEC:` comments only for non-obvious safeguards at the public-lifecycle or malformed-claim boundaries.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs`\n- `crates/mossignal/src/diagnostics.rs` only where the existing opening diagnostic kernel needs a specification-backed correction to support the already-reviewed laws\n- `crates/mossignal/src/lib.rs`\n- one new private validation module if a separate module clarifies the boundary\n- focused unit tests alongside the relevant modules\n- `docs/specs/contracts/structural-validation.yaml` only for implementation-discovered, reusable specification facts; any change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n- Start each defect-class correction with a focused failing regression test and observe the failure before changing production behavior.\n- Add at least one focused valid and invalid case for each included structural rule.\n- Verify independent malformed conditions accumulate where safe, blocking reports omit artifacts, diagnostics are structurally asserted, and output is invariant under permutations of unordered authored claims.\n- Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Dependencies\n\n- Roadmap item 8 / `ms-unchecked-authored-graph-wsj` is complete.\n- Roadmap item 10 depends on this bead's private candidate boundary and must not be folded into it.\n\n## Readiness\n\nDraft — requires independent contract and bead review. No unresolved observable product question blocks the stated slice; the only intentional deferral is reaction-dependency semantics owned by item 10.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-19T05:40:30.000751Z","created_by":"mikkofinell","updated_at":"2026-07-19T05:58:43.403875Z","closed_at":"2026-07-19T05:58:43.403830Z","close_reason":"Completed","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-sxa","title":"Extract restricted current-reaction dependencies","description":"## Objective\n\nImplement Premium Foundation Roadmap item 10: derive the private current-reaction dependency graph for the structurally valid restricted Constant/Not authored graph. Establish deterministic, stable-subject graph inspection for the following cycle-validation task without constructing a public validated or compiled artifact.\n\n## Contract basis\n\n- `mossignal.graph.structural_validation`\n  - `docs/specs/contracts/structural-validation.yaml`\n  - status: reviewed, used unchanged\n  - role: private structurally usable candidate boundary and the restricted graph invariants already established before extraction.\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed, used unchanged\n  - role: stable-keyed authored nodes, ports, connections, endpoints, and restricted Constant/Not shapes.\n\n- `mossignal.graph.current_reaction_dependency_graph`\n  - `docs/specs/contracts/current-reaction-dependency-graph.yaml`\n  - status: reviewed, accepted by independent review\n  - role: reaction graph model, restricted signatures, stable correspondence, and deterministic extraction verification.\n\n## Included scope\n\n- Add a private reaction-operation and dependency-graph representation consumed by the next validation phase.\n- Derive graph vertices and directed edges from the private structurally checked candidate, preserving stable correspondence to the relevant authored subjects.\n- Implement the static signatures for the restricted nodes only: Constant contributes no current input-to-output dependency; Not contributes its level input to its level output.\n- Represent external-input root facts, connection-mediated current dependencies, and external-output observation edges needed by the restricted graph.\n- Provide deterministic private graph inspection sufficient for focused tests and the cycle-validation bead.\n- Keep the authored structural graph distinct from the derived current-reaction graph.\n\n## Exclusions and non-goals\n\n- Do not perform SCC decomposition, detect cycles or self-loops, emit cycle diagnostics, or choose a topological order.\n- Do not construct or expose ValidatedNetwork, CompiledNetwork, dense indices, evaluator descriptors, or machine state.\n- Do not add signatures for stateful, temporal, module, variadic, or other future node kinds.\n- Do not revisit structural validation rules or expand the public diagnostic catalogue.\n\n## Implementation obligations\n\n- Respect `graph-model`, `conservative-static-signatures`, `stable-subject-correspondence`, and `pre-semantic-private-boundary` from `mossignal.graph.current_reaction_dependency_graph`.\n- Derive only dependencies that can affect a current-reaction result; do not model proposed state or future temporal effects as graph inputs.\n- Use stable authored subjects and deterministic ordering; storage or authored-claim insertion order must not determine the observable relation.\n- Retain the graph privately so structural success alone still does not establish the public validated lifecycle.\n\n## Allowed implementation freedom\n\n- Private operation variants, graph storage, adjacency representation, helper names, and test inspection APIs are free when they preserve the specified relation and stable correspondence.\n- The restricted slice may defer reaction roots not represented by the current authored model.\n\n## Expected change surface\n\n- `crates/mossignal/src/validation.rs` or a new private dependency module\n- `crates/mossignal/src/lib.rs` only if private module integration requires it\n- focused unit tests beside the private extraction implementation\n- `docs/specs/contracts/current-reaction-dependency-graph.yaml` only for implementation-discovered reusable facts; any change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n- Add focused positive and negative structural assertions for Constant and Not dependency signatures.\n- Assert the derived relation distinguishes authored structural edges from reaction-operation dependencies.\n- Verify deterministic graph inspection and identical dependency relations under permutations of unordered authored claims.\n- Verify no public validated or compiled artifact becomes constructible from this phase.\n- Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Dependencies\n\n- Roadmap item 9 / `ms-structural-validation-7ah` is complete.\n- Roadmap item 11 depends on this private dependency-graph boundary and owns SCC validation, cycle diagnostics, and topological ordering.\n\n## Readiness\n\nAccepted for implementation. No unresolved observable product question blocks the restricted extraction slice.","notes":"Independent review accepted the current-reaction dependency contract and this bead for implementation. Review correction: the represented external inputs are current-reaction root facts, so extraction must include their connection-mediated dependencies (including external-input-to-Not) as well as external-output observation edges. Only previous-state and due-event roots remain intentionally deferred. The contract is now reviewed at its recorded evidence baseline.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-19T06:21:41.430430Z","created_by":"mikkofinell","updated_at":"2026-07-19T06:28:56.273356Z","closed_at":"2026-07-19T06:28:56.273307Z","close_reason":"Implemented deterministic restricted current-reaction dependency extraction","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-tjg","title":"Validate reusable user modules and semantic identity","description":"## Objective\n\nImplement Premium Continuation Roadmap item 33 as one bounded validation-and-identity slice: validate the existing UncheckedModule<D> representation into an immutable reusable user ModuleDef<D>, preserve encapsulation and deterministic inspection, and compute its canonical ModuleFingerprint without introducing authoring or instantiation.\n\n## Roadmap provenance\n\n- Document: docs/premium_continuation_roadmap.md\n- Item: 33\n- Title: Module validation, encapsulation, and semantic identity\n\nThis provenance records task sequencing only. The authoritative specifications and reviewed contract baseline govern implementation semantics.\n\n## Contract basis\n\n### Reviewed contracts created for this item\n\n- mossignal.module.validated_user_definition\n  - docs/specs/contracts/validated-user-module-definition.yaml\n  - status: reviewed\n  - role: governs one-way module validation, complete interface conformance, ordinary primitive internal validation, module-local current-reaction acyclicity, immutable User ModuleDef ownership, encapsulation, deterministic inspection, and blocking reports.\n- mossignal.identity.module_fingerprint\n  - docs/specs/contracts/module-fingerprint.yaml\n  - status: reviewed\n  - role: governs the opaque domain-separated ModuleFingerprint, semantic inclusion and exclusion, insertion-order invariance, identity sensitivity, validated ownership.\n\n### Reviewed contracts reused without modification\n\n- mossignal.module.unchecked_definition\n  - docs/specs/contracts/unchecked-module-definition.yaml\n  - role: supplies the accepted owned unchecked interface and primitive-internal claims, User origin, module-local stable keys, metadata separation, and lossless malformed-claim boundary.\n- mossignal.diagnostics.artifact_reports, mossignal.diagnostics.problem_model, and mossignal.diagnostics.collections\n  - role: supply artifact omission for blocking findings, typed catalogue-backed problems, and deterministic report collection.\n- mossignal.diagnostics.subject_references\n  - role: supplies stable module-definition, module-input, module-output, and internal subject categories. Its cited concrete subject section has changed only by an unrelated TimeDomainId clarification; the module categories required here were rechecked and remain unchanged.\n- mossignal.graph.current_reaction_dependency_graph and mossignal.graph.current_reaction_cycle_validation\n  - role: supply the established graph/SCC concepts and deterministic algorithmic pattern. Their module exclusions remain intact; module-specific integration is represented only by the new validated-module contract.\n\nThe exact version-1 user-module fingerprint field schema and module-specific private diagnostic condition keys are not frozen by authority. They are bounded deterministic implementation choices, not blockers.\n\n## Included scope\n\n- Add public opaque ModuleFingerprint and immutable ModuleDef<D> for successfully validated reusable user modules.\n- Add consuming validate and borrowing validate_ref transitions on UncheckedModule<D>, returning the existing Report<ModuleDef<D>, D> boundary.\n- Validate public input/output declaration uniqueness, mapping declaration existence, endpoint existence and direction, signal-kind equality, output completeness and uniqueness, and ordinary internal-input driver policy across interface mappings and internal connections.\n- Validate the complete existing primitive internal graph using the established node-shape, port, connection, semantic parameter, initial-state, dependency-signature, and stable-key rules.\n- Derive module-local current-reaction dependencies with public inputs as roots and public outputs as observations; reject every cyclic SCC and self-loop before ModuleDef construction.\n- Retain explicit User origin, metadata, public interfaces, mappings, nodes, and connections in an immutable artifact with deterministic read-only inspection.\n- Enforce private-internal encapsulation at public authoring boundaries while keeping internal subjects visible to read-only definition inspection and structured diagnostics.\n- Compute a domain-separated BLAKE3-256 ModuleFingerprint from a canonical stable-keyed user-module semantic projection.\n- Reuse the existing diagnostic/report architecture and extend the minimum module subject/evidence/code surface required for structured deterministic findings.\n- Add focused positive, negative, order-invariance, metadata-exclusion, identity-sensitivity, and golden-vector verification.\n\n## Exclusions and non-goals\n\n- No ModuleBuilder<D>, builder-scoped module signals, typed module conveniences, or changes to NetworkBuilder<D>.\n- No module instantiation, bindings, AddedModuleInstance, instance-qualified identity, nested modules, or hierarchy validation.\n- No ModuleOrigin::Standard, StandardModuleDeclaration, catalogue descriptors, canonical standard expansion, or standard conveniences.\n- No network compilation, module flattening, evaluation, Machine state, transactions, runtime inspection, or explanation.\n- No persistence envelope, encoder/decoder API, restoration, replay, compatibility migration, topology patch, or reconfiguration.\n- No non-blocking module quality-analysis campaign beyond findings already produced safely by the reused validation path.\n- Do not expose mutable validated graph state, private dense identities, callback nodes, compatibility shims, or speculative future APIs.\n\n## Implementation obligations\n\n- Implement every requirement represented by mossignal.module.validated_user_definition and mossignal.identity.module_fingerprint within their stated scope as independently reviewed.\n- Preserve all mossignal.module.unchecked_definition requirements: unchecked construction remains lossless and does not validate or repair claims.\n- Treat one public module input as a typed source that may fan out to multiple private input ports or remain unused; do not misclassify repeated input-key mappings as duplicate interface declarations. Count those mappings together with internal connections when enforcing private input driver policy.\n- Require every public output to have exactly one declared, existing, direction-valid, same-kind internal source; reject unknown, duplicate, missing, dangling, direction-invalid, and wrong-kind output mappings.\n- Reuse the established primitive structural and reaction-signature machinery rather than creating a second graph validator or causality model.\n- Prevent ModuleDef construction whenever any blocking structural, interface, parameter, dependency, or cycle diagnostic remains; caller-controlled malformed modules must never panic.\n- Keep ModuleDef immutable and ensure private internals cannot become external connection handles merely through inspection.\n- Use stable module-interface and internal identities in diagnostics and inspection, never metadata, caller order, dense positions, or private allocation identity.\n- Include User origin, typed interface keys, output incidence, all supported internal primitive semantic structure, parameters, roles, state/temporal schemas, and stable identities in ModuleFingerprint.\n- Exclude diagnostic metadata, authored insertion order, private representation, module-instance placement, network bindings, and containing-network identity from ModuleFingerprint.\n- Keep production code free of unwrap and expect.\n\n## Allowed implementation freedom\n\n- Reuse or extract the existing validation passes, reaction graph, SCC implementation, canonical writer, and primitive fingerprint projections, or add focused private module adapters; choose the smallest coherent design.\n- Normalize validated collections or retain raw storage behind canonical iterators. Choose one deterministic inspection behavior, test insertion-order equivalence, and record it.\n- Choose the smallest forward-extensible User-only ModuleOrigin<D> representation without fabricating standard declarations.\n- Define the smallest deterministic private module-specific diagnostic discriminator and evidence merge projections consistent with the catalogue. Test them, but do not present them as frozen compatibility policy.\n- Select and record the exact pre-stability version-1 user-module fingerprint record and variant spelling consistent with the reviewed fingerprint contract. Preserve its complete canonical bytes in golden tests; no historical reader or migration is required.\n- Add narrow read-only iterator or definition-graph view types needed by ModuleDef without implementing the broader compiled graph-query surface.\n\n## Expected change surface\n\n- crates/mossignal/src/authored.rs for validation entry points or narrowly needed authored access.\n- crates/mossignal/src/validation.rs or a focused module-validation sibling for ModuleDef, interface/internal validation, reaction dependency integration, SCC rejection, and immutable inspection.\n- crates/mossignal/src/identity.rs for ModuleFingerprint and canonical user-module projection, preferably reusing the existing canonical writer and primitive projections.\n- crates/mossignal/src/diagnostics.rs for the smallest module-definition/interface subject, evidence, and validation-code integration required by this slice.\n- crates/mossignal/src/lib.rs for deliberate public exports.\n- Focused unit/integration tests and versioned ModuleFingerprint golden bytes.\n- The two reviewed item-33 contracts only if implementation discovers reusable specification-backed corrections; any such contract change returns that contract to draft pending independent review.\n- docs/premium_continuation_roadmap.md and .beads/ for task state only.\n\n## Required verification\n\n1. Validate representative empty and nonempty User modules with mixed Level and Pulse interfaces and every currently supported primitive kind; inspect exact origin, public interface, internal stable subjects, semantic configuration, metadata, reaction relation, and fingerprint.\n2. For each interface rule, cover a valid positive case and focused negative cases for duplicate declarations, unknown mapping keys, missing output mapping, duplicate output mapping, dangling endpoint, invalid direction, wrong signal kind, conflicting drivers, and an unused public input that remains valid.\n3. Reuse focused primitive validation cases to prove malformed internal keys, ports, connections, arity, parameters, initial state, and dependency signatures cannot enter ModuleDef and do not panic.\n4. Verify module input fan-out remains valid and contributes root dependencies, while public outputs observe exactly their mapped internal sources.\n5. Reject self-loops and multi-node current-reaction cycles, accept structural cycles broken by established state or strictly later time, and verify deterministic SCC reporting under claim/traversal permutations.\n6. Verify blocking findings omit ModuleDef, independent safe findings accumulate deterministically, validate and validate_ref agree, and an unchecked module remains unchanged after borrowed validation.\n7. Verify ModuleDef is immutable, User origin is explicit, interface and graph inspection are deterministic, and inspection does not expose an external-authoring path to private internals.\n8. Verify equal semantic modules constructed in different claim orders share canonical bytes and ModuleFingerprint; metadata-only changes preserve them; changing each included interface, incidence, stable-key, kind, role, parameter, initial-state, state-schema, temporal-schema, or connection category changes the projection.\n9. Retain complete version-1 canonical digest-input bytes and expected BLAKE3-256 ModuleFingerprint for representative minimal and nontrivial user modules, and verify domain/type separation from NetworkFingerprint and other digest identities.\n10. Verify no builder, instantiation, standard catalogue, nested hierarchy, compilation, execution, persistence envelope, or reconfiguration API is introduced.\n11. Run focused Rust tests while iterating, then make check-dev; final implementation acceptance additionally runs make check-final.\n\n## Dependencies\n\n- Roadmap item 32 / ms-bnh is closed and accepted.\n- The stable module key vocabulary, unchecked module artifact, complete current primitive family, ordinary validation/reaction machinery, report model, and canonical network fingerprint infrastructure exist in the accepted baseline.\n\n## Blocking questions\n\nNone. The missing exact private diagnostic projections and version-1 module payload record permit several reasonable conforming implementations. Select the simplest deterministic choices, test them, and record the concrete result for review.\n\n## Readiness\n\nThe task is bounded and has no product or design blocker. Independent review has promoted both governing contracts and accepted this bounded bead for implementation.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-29T06:30:25.797139Z","created_by":"mikkofinell","updated_at":"2026-07-29T07:38:02.274630Z","closed_at":"2026-07-29T07:38:02.274578Z","close_reason":"Implemented validated reusable user modules, deterministic inspection and diagnostics, module-local causality validation, and canonical ModuleFingerprint; final acceptance passed.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["identity","module","roadmap","validation"],"dependencies":[{"issue_id":"ms-tjg","depends_on_id":"ms-bnh","type":"blocks","created_at":"2026-07-29T06:30:25.797139Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-u88","title":"Implement ready-machine level transactions","description":"## Objective\n\nImplement Premium Foundation Roadmap item 20: extend the restricted runtime with owned ready-machine level transactions at strictly later caller times. Apply exact-bound InputDelta values over the prior authoritative external valuation, run the existing full Constant/Not evaluator, atomically replace settled Ready state, and publish only genuine external LevelChanged events. Stop before pulse, stateful, temporal, patch, persistence, or optimized execution work.\n\n## Contract basis\n\n- `mossignal.runtime.ready_level_transaction`\n  - `docs/specs/contracts/ready-level-transaction.yaml`\n  - status: reviewed and independently accepted for reuse\n  - role: governs explicit advancement values, Ready preconditions, strict time, delta retention, full settlement, atomic state replacement, LevelChanged events and causes, budgets, rollback, and verification.\n- `mossignal.runtime.level_input_delta`\n  - `docs/specs/contracts/level-input-delta.yaml`\n  - status: reviewed and used unchanged\n  - required facets: `exact-current-schema-binding`, `explicit-level-observations`, `omission-means-retention`, `deterministic-unordered-batch`, and `requires-prior-authoritative-valuation`.\n- `mossignal.runtime.machine_lifecycle`\n  - `docs/specs/contracts/machine-lifecycle.yaml`\n  - status: reviewed and used unchanged\n  - required facets: `explicit-runtime-lifecycle`, `machine-local-revision-and-identity`, and `mutable-ownership-boundary`.\n- `mossignal.runtime.restricted_full_topological_evaluator`\n  - `docs/specs/contracts/restricted-full-topological-evaluator.yaml`\n  - status: reviewed and used unchanged\n  - required facets: `compiled-immutable-input`, `deterministic-complete-topological-settlement`, and `reference-path-verification`.\n- `mossignal.runtime.policy`\n  - `docs/specs/contracts/runtime-policy.yaml`\n  - status: reviewed and used unchanged\n  - required facets: `explicit-machine-association`, `atomic-budget-failure`, and `implemented-budget-boundaries`.\n- `mossignal.runtime.initialization_transaction`\n  - `docs/specs/contracts/initialization-transaction.yaml`\n  - status: reviewed and used unchanged as contextual established behavior\n  - required facets: the existing initialization constructor, apply path, result-owned provenance, budget accounting, and failure behavior must remain unchanged while Transaction and Machine::apply gain Ready advancement.\n\nTemporal deadlines, complete provenance and result surfaces, state digests, and patches remain known-uncovered or excluded by the reviewed ready-transaction contract and are not blockers for this restricted slice.\n\n## Included scope\n\n- Add `Transaction<D>::advance(Time<D>, NetworkRevision, InputDelta<D>)` while retaining the distinct initialization constructor.\n- Make the opaque transaction representation and pre-execution inspection lifecycle-aware without panics or compatibility shims.\n- Dispatch `Machine<D>::apply` by transaction kind while preserving accepted initialization behavior.\n- Require Ready lifecycle, exact expected revision, exact NetworkKey, NetworkFingerprint, and InputSchemaFingerprint binding, and a requested time strictly later than the current machine time.\n- Add structured ready-application failures for delta-before-initialization and non-increasing time while preserving existing stale revision, wrong-network, foreign-schema, stale-schema, and budget distinctions.\n- Overlay explicit level observations onto a cloned authoritative external valuation and retain every omitted value exactly.\n- Invoke the existing full topological evaluator once at the requested time for the restricted Constant/Not topology.\n- Atomically install the complete candidate external levels, settled levels, output baselines, output causes, provenance roots, and Ready time while retaining topology revision and runtime policy.\n- Add `OutputEvent::LevelChanged` and emit exactly one deterministic event per external level output whose settled value differs from its prior baseline; emit none for unchanged outputs.\n- Retain an immutable result-owned provenance view resolving every returned changed-output cause.\n- Enforce every already implemented applicable runtime-policy accounting boundary.\n\n## Explicit exclusions and non-goals\n\n- No pulse inputs or outputs, PulseCount, stateful nodes, successor state, temporal nodes, event calendar, pending deadlines, chronological internal reactions, or schedules.\n- No topology patch, target-bound delta, reconfiguration, migration, output establishment or removal, or revision advancement.\n- No binding projection, caller metadata, expected execution digest, forecast, observer delivery, replay, persistence, snapshots, or semantic digest implementation.\n- No incremental dirty propagation, sparse staging, undo log, callback-based execution, or performance optimization.\n- Do not expand the complete diagnostic Problem kernel or full RuntimeFailure layering beyond the exact reachable categories needed by this slice.\n- Do not change InputSnapshot completeness or initialization LevelEstablished behavior.\n\n## Implementation obligations\n\n- Implement `owned-explicit-advance-value`, `exact-ready-preconditions`, `strictly-later-caller-time`, `delta-overlay-retention`, `ordinary-restricted-settlement`, `complete-ready-state-on-success`, `changed-output-events`, `resolvable-ready-causes`, `applicable-runtime-policy-budgets`, `single-atomic-publication`, `failure-preserves-complete-machine`, and `restricted-ready-verification` from `mossignal.runtime.ready_level_transaction`.\n- Use stable identities from the compiled machine and delta rather than dense positions or caller-supplied surrogate schema context.\n- Preserve the first observation when rejecting invalid construction earlier; application consumes an already valid delta and must not reinterpret omission as Low.\n- Compare every newly settled external level output against the prior committed baseline before replacement. Event order must be deterministic by stable semantic identity and must not encode causal order.\n- Every returned CauseRef must resolve through the returned TransactionResult provenance after later transactions run.\n- Keep every fallible step before one complete MachineStore replacement. Rejections must preserve a full semantic before/after observation, including prior provenance sharing and output causes.\n- A successful state-only transaction advances Ready time but keeps before_revision equal to after_revision.\n- Preserve generic behavior without adding unrelated bounds on D.\n\n## Allowed implementation freedom\n\n- Private Transaction variant representation, lifecycle-aware input accessor spelling, helper extraction between initialization and advancement, and module placement are free. The current initialization-only `Transaction::input()` accessor is not an authoritative cross-variant promise; replace it with a non-panicking coherent inspection surface rather than retain it through panic or a compatibility shim.\n- Clone-and-swap or an equivalently simple complete MachineStore candidate is preferred. Choose the simplest deterministic implementation, test it, and record the concrete choice.\n- Provenance may reuse immutable prior records or construct a replacement view when result-owned causes remain resolvable and required current roots survive.\n- Private event and provenance ordering/storage are free and are not public iteration or persistence promises.\n- Budgets with no represented work in this slice need no invented accounting.\n\n## Expected change surface\n\n- `crates/mossignal/src/transaction.rs` for transaction variants, ready application, failures, LevelChanged results, provenance, and focused tests.\n- `crates/mossignal/src/input.rs` only for a minimal crate-private owned delta projection if needed.\n- `crates/mossignal/src/machine.rs` only for candidate-state or current-cause support needed by ready application.\n- `crates/mossignal/src/lib.rs` only for deliberate public exports or adjusted transaction inspection types.\n- `docs/specs/contracts/ready-level-transaction.yaml` only for implementation-discovered reusable specification-backed corrections; any implementation change remains draft and requires independent review.\n- `.beads/` when implementation acceptance closes and synchronizes the bead.\n\n## Required verification\n\n1. Initialize, then apply empty, single-input, partial, complete, and explicit-reassertion deltas at strictly later arbitrary times.\n2. Verify omitted Low and High external levels retain their exact prior values and explicit observations form one unordered batch.\n3. Verify complete Constant/Not reevaluation and settled state replacement, including multi-layer and reconvergent restricted graphs.\n4. Verify LevelChanged carries exact from, to, time, unchanged revision, stable output identity, and a result-resolvable acyclic cause; unchanged outputs emit no event.\n5. Verify deterministic equivalent results under delta insertion order, authored insertion order, and diagnostic metadata changes without treating representation order as causality.\n6. Verify successful state-only transactions advance time while retaining NetworkRevision and RuntimePolicyId.\n7. Verify structured rejection for advance-before-initialization, equal time, earlier time, stale revision, wrong network, foreign schema, stale schema, and each applicable budget one below, exactly at, and one above its boundary where representable.\n8. For every rejection, compare the complete machine observation before and after, including time, external levels, settled levels, baselines, causes, and provenance ownership.\n9. Verify initialization transactions still produce the accepted Ready state and LevelEstablished events, and initialization applied to Ready remains rejected without mutation.\n10. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Dependencies and readiness\n\nRoadmap item 19 (`ms-ipi`) is closed and accepted. The reviewed delta, lifecycle, evaluator, policy, and initialization contracts are unchanged. The ready-machine transaction contract is reviewed, internally consistent, and has no unresolved product question, conflicting requirement, missing fundamental semantic rule, or frozen compatibility blocker. This bead is bounded and ready for implementation.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T07:06:42.288038Z","created_by":"mikkofinell","updated_at":"2026-07-28T07:32:51.659051Z","closed_at":"2026-07-28T07:32:51.659012Z","close_reason":"Completed","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-u88","depends_on_id":"ms-ipi","type":"blocks","created_at":"2026-07-28T07:06:42.288038Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-unchecked-authored-graph-wsj","title":"Implement unchecked authored graph representation","description":"## Objective\n\nImplement premium foundation roadmap item 8: a deliberately restricted, authoritative `UncheckedNetwork<D>` representation for level-only `Constant` and `Not` definitions. It must retain malformed authored structure for the following validation task rather than repairing or rejecting it during construction.\n\n## Contract basis\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed (accepted by independent review)\n  - required facets: authoritative stable-keyed structural graph; owned unchecked construction; typed and erased endpoint forms; retention of malformed claims; metadata attachment; Constant/Not shapes; external boundaries; deferred validation transition.\n\n- `mossignal.diagnostics.artifact_reports`\n  - `docs/specs/contracts/artifact-reports.yaml`\n  - status: reviewed, contextual only\n  - role: governs the later validation report, which this task does not implement.\n\n## Included scope\n\n- Add the authored graph module and export its deliberate public unchecked-definition types.\n- Represent network identity, external level inputs and outputs, `NodeDef<D>`, `NodeKind<D>` restricted to `Constant` and `Not`, typed input/output ports, connections, and `DiagnosticMeta`.\n- Use existing stable typed/erased key types to retain signal kind in endpoints while allowing heterogeneous collections.\n- Provide direct, safe construction suitable for deserializers, editors, generated definitions, fuzzing, and test fixtures.\n- Preserve all supplied structural claims losslessly, including duplicate keys, dangling references, mismatched kinds, invalid directions, conflicting drivers, and absent fixed ports; duplicate-key storage must not overwrite earlier claims.\n- Add focused representation tests covering a valid restricted definition, both level and pulse endpoint representation, metadata attachment, direct malformed construction, and duplicate-key retention.\n\n## Exclusions and non-goals\n\n- No structural validation, diagnostic emission, `Report` production, private validation candidate, reaction dependencies, cycle checking, `ValidatedNetwork`, dense indices, compilation, machine/runtime execution, modules, persistence encoding, or topology patches.\n- Do not add placeholder semantics for node kinds beyond `Constant` and `Not`.\n- Do not make constructor-time validity checks that discard or rewrite malformed authored input.\n\n## Implementation obligations\n\n- Keep the representation authoritative and stable-keyed; no dense runtime positions or evaluator scheduling belong here.\n- Model external endpoints as network boundaries rather than nodes.\n- Constant definitions must record their `LogicLevel` configuration and level output shape; Not definitions must record their one level input and level output shape.\n- Preserve signal kind in all typed endpoints; erasure is allowed only for heterogeneous storage and must retain the kind.\n- Keep the public API owned and non-callback-based.\n- Add concise `SPEC:` comments only where a non-obvious lossless-storage choice prevents weakening the malformed-input boundary.\n\n## Allowed implementation freedom\n\n- Private collection layout is free provided it losslessly preserves duplicate and malformed claims.\n- Exact constructor/helper naming and module-internal layout are free.\n- The representation may use simple vectors as the canonical opening storage.\n\n## Expected change surface\n\n- `crates/mossignal/src/lib.rs`\n- new authored-graph source module and its focused unit tests\n- `docs/specs/contracts/authored-network-definition.yaml` only if implementation discovers additional specification-backed facts (any such change remains draft)\n- `.beads/` state when the implementation is accepted\n\n## Required verification\n\n- Run focused authored-graph tests while iterating.\n- Run `make check-dev` before completion and `make check-final` for final acceptance.\n- Do not claim validation behavior; the next roadmap item owns validation-focused positive/negative conformance cases.\n\n## Readiness\n\nAccepted for implementation. No unresolved observable product question blocks this bounded slice.","notes":"Independent review accepted the contract and bounded implementation scope. The reviewed contract requires an unchecked endpoint-claim form that preserves endpoint category and signal kind even for direction-invalid connections; exact public spelling and storage remain implementation freedom.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-19T05:10:32.913097Z","created_by":"mikkofinell","updated_at":"2026-07-19T05:29:29.592261Z","closed_at":"2026-07-19T05:29:29.592219Z","close_reason":"Implemented unchecked authored network representation with lossless malformed claims and focused tests.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0}
{"id":"ms-v7j","title":"Implement stateful reaction foundation with Toggle","description":"## Objective\n\nImplement Premium Foundation Roadmap item 25 as one bounded vertical slice: establish previous/proposed state ownership and atomic state commitment through the existing reaction and transaction architecture, using the Pulse-controlled `Toggle` as the single opening stateful primitive.\n\n## Contract basis\n\n### Reviewed contracts for this item\n\n- `mossignal.runtime.stateful_reaction_foundation`\n  - `docs/specs/contracts/stateful-reaction-foundation.yaml`\n  - status: reviewed; accepted for reuse at the recorded evidence baseline\n  - role: governs previous/proposed state isolation, declared-state initialization, state-family slots, same-reaction visibility, atomic commitment, direct inspection, and foundation verification.\n- `mossignal.node.toggle`\n  - `docs/specs/contracts/toggle.yaml`\n  - status: reviewed; accepted for reuse at the recorded evidence baseline\n  - role: governs Toggle's fixed shape, explicit initial state, parity law, authoring, causality, compilation, inspection, provenance, identity, and conformance.\n\n### Reviewed contracts reused unchanged\n\n- `mossignal.runtime.reaction_scoped_pulse_foundation` and `mossignal.node.pulse_merge`\n  - role: established typed Pulse batches, exact-bound input artifacts, reaction-local multiplicity, grouped provenance, and the existing Pulse authoring/runtime path consumed by Toggle.\n- `mossignal.graph.authored_network_definition`, `mossignal.authoring.typed_network_builder`, and `mossignal.graph.structural_validation`\n  - role: established dynamic definitions, typed and explicit-key builder patterns, malformed-definition retention, and shape validation.\n- `mossignal.graph.current_reaction_dependency_graph` and `mossignal.graph.current_reaction_cycle_validation`\n  - role: established conservative dependency extraction and cycle rejection, including the rule that a whole stateful node is not automatically a causality barrier.\n- `mossignal.compile.compiled_network` and `mossignal.runtime.restricted_full_topological_evaluator`\n  - role: established immutable descriptors, private dense storage, deterministic full settlement, and compiled invariant checks. Changed evidence in this contract, the structural-validation contract, and the reaction dependency and cycle contracts was rechecked against current authority; it preserves the represented stable-subject, private-candidate, deterministic-cycle, and private-dense-layout rules without contradiction.\n- `mossignal.runtime.machine_lifecycle`, `mossignal.runtime.initialization_transaction`, and `mossignal.runtime.ready_level_transaction`\n  - role: established independent machine stores, ordinary initialization and ready reactions, clone-and-swap staging, single publication, failure atomicity, and result-owned provenance.\n- `mossignal.identity.network_fingerprint`\n  - role: established semantic parameter and state-schema contribution, insertion-order independence, golden vectors, and the synchronized pre-stability projection policy.\n\nThe contracts' snapshot/restoration and topology-migration `known_uncovered` facets are intentionally outside this item. Authored Toggle cycle conformance also waits for a separately implemented Level-to-Pulse primitive; this slice verifies Toggle's immediate dependency signature without adding another primitive.\n\n## Included scope\n\n- Add public `ToggleConfig` with an explicit initial `LogicLevel` and add closed dynamic `NodeKind::Toggle` with exactly one Pulse input and one Level output.\n- Add typed convenience, explicit keyed, and exact fixed-port Toggle construction through the existing `NetworkBuilder` and unchecked authored representation.\n- Extend structural validation, current-reaction dependency extraction, cycle validation integration, compilation, and invariant checks for Toggle.\n- Add exactly one Toggle-family state cell per valid Toggle, with declared state in AwaitingInitialization and independent mutable state per spawned machine.\n- Extend the full topological evaluator so every operation reads previous committed Toggle state, computes the parity-law current output and one proposed successor, exposes that output downstream in the same reaction, and never overwrites committed state during evaluation.\n- Integrate proposed-state staging and one atomic successor commit into both initialization and ready transactions, preserving complete failure atomicity and result-resolvable causal support.\n- Add the smallest owned node-inspection surface needed to expose Toggle's declared initial state before initialization and its committed state, observed revision/time, and applicable latest inversion cause when ready.\n- Extend the pre-stability version-1 network fingerprint projection with Toggle's exact kind, fixed port roles, state schema, and initial-level parameter; synchronize the authoritative projection, network-fingerprint contract, and golden vectors in the accepted implementation change.\n- Add focused stateful-foundation and exhaustive Toggle conformance verification.\n\n## Exclusions and non-goals\n\n- No RisingEdge, FallingEdge, AnyEdge, set/reset latch, SampleHold, counter, finite-state-machine, additional state family, or broader pulse-combinational primitive.\n- No temporal node, pending event, schedule change, deadline execution, topology patch, state migration, persistence encoding or decoding, snapshot restoration, replay, or semantic state digest.\n- No reset port or resettable Toggle primitive; resettable behavior remains a later standard module or separately specified node.\n- No generic inspection-query compiler, subscriptions, observer deltas, retained pulse history system, or complete explanation/why-not renderer.\n- No incremental dirty propagation, sparse state overlay, undo log, callback evaluator, trait-object node state, or performance optimization.\n- Do not make proposed state publicly visible as committed state, feed it back within the same reaction, or treat Toggle as a blanket current-reaction causality barrier.\n- Do not require `StateChange` publication in this opening slice; direct committed-state inspection is required, and later reporting may add the recommended change-set representation.\n\n## Implementation obligations\n\n- Implement the product and verification requirements within this bead's stated scope from the two reviewed item-25 contracts; their `known_uncovered` facets remain out of scope.\n- Reuse the existing authored graph, report model, reaction dependency graph, compiler, full evaluator, machine lifecycle, transaction, input artifact, output-event, provenance, and fingerprint paths; do not create a parallel stateful execution architecture.\n- Preserve the complete previous-state vector until all reaction operations have produced their settled outputs and proposals, then commit all successors exactly once with the rest of the candidate machine.\n- Treat Toggle's complete simultaneous count by parity: zero and every even count retain the prior level; every odd count inverts it once, regardless of count magnitude or insertion order.\n- Preserve current downstream visibility: Toggle's current Level output may drive later combinational or stateful operations in the same acyclic topological reaction.\n- Use declared initial state as the first reaction's previous state and process initialization pulses through the ordinary law before publishing `LevelEstablished` and committing the first successor.\n- Preserve latest inversion causality across even-count reactions; an even batch may support the current output explanation but must not fabricate a new inversion.\n- Ensure every structured transaction rejection preserves Toggle state, settled signals, output baselines, logical time, provenance, and every existing complete-machine observation.\n\n## Allowed implementation freedom\n\n- Choose the simplest deterministic Toggle state-family vectors, dense state-slot index, descriptor fields, proposal staging, and clone-and-swap decomposition; test and record the concrete choice.\n- Fixed port keys may be derived deterministically or allocated consistently with the established builder, while exact-keyed dynamic construction remains durable reconstruction authority.\n- Choose and record the exact canonical Toggle and state-schema variant spelling for the synchronized pre-stability projection; no historical reader or migration is required before fingerprint stabilization.\n- The focused inspection value and accessor names are ordinary API design freedom if they are owned, revision/time-aware when ready, distinguish declared from committed state, and fail structurally for unavailable runtime facts.\n\n## Expected change surface\n\n- `crates/mossignal/src/authored.rs` and `crates/mossignal/src/builder.rs` for Toggle configuration, ports, and typed/dynamic authoring.\n- `crates/mossignal/src/validation.rs` and dependency/cycle integration for shape and current-reaction causality.\n- `crates/mossignal/src/compile.rs` for Toggle descriptors, state-slot layout, full evaluation, proposed successors, and invariant checks.\n- `crates/mossignal/src/machine.rs` for declared/committed Toggle state ownership and focused node inspection.\n- `crates/mossignal/src/transaction.rs` for state staging, atomic commitment, output/provenance integration, and rejection regression tests.\n- `crates/mossignal/src/identity.rs` and the authoritative restricted fingerprint projection for exact Toggle identity and golden vectors.\n- `crates/mossignal/src/lib.rs` for deliberate public configuration and inspection exports.\n- `docs/specs/contracts/network-fingerprint.yaml` only as required by the accepted exact pre-stability projection; the selected reviewed contracts remain unchanged unless implementation discovers a reusable specification-backed correction.\n- `.beads/` when implementation acceptance synchronizes the bead.\n\n## Required verification\n\n1. Exhaust both previous levels with counts zero, one, two, three, a larger even count, and a larger odd count; verify current output and committed successor exactly follow parity.\n2. Initialize Low and High Toggle definitions with absent, even, and odd pulse batches; verify declared state is the previous state, settled output is established, and the successor commits once.\n3. Apply later absent/even/odd pulse transactions and verify strict reaction isolation, state retention or one inversion, exact LevelChanged suppression/publication, and latest inversion cause retention.\n4. Build equivalent Toggle networks through typed, explicit-keyed, and direct dynamic paths; compare validation, dependency graph, compilation, fingerprints, initialization, later transactions, inspection, and observable outputs.\n5. Verify Pulse→Toggle→Level-combinational downstream chains settle in the same reaction and verify dependency extraction records Toggle's instantaneous Pulse-to-Level edge without treating the whole stateful node as a causality barrier. Authored cycle conformance waits for a separately implemented Level-to-Pulse primitive.\n6. Spawn multiple machines from one compiled network and verify their Toggle state evolves independently while the compiled topology remains immutable.\n7. Verify pre-initialization inspection exposes definition and declared initial state but rejects unavailable current runtime facts; ready inspection exposes committed state, revision/time, and latest inversion cause without exposing proposed state.\n8. Force each applicable existing structured rejection and compare the complete machine before and after, including Toggle state and state causes; no proposal or partial output may leak.\n9. Verify same-topology insertion-order fingerprint invariance, sensitivity to initial level and Toggle versus other node kinds, exact fixed roles/state schema, and synchronized hand-reviewed golden bytes and digests.\n10. Run focused Rust tests while iterating, then `make check-dev` and `make check-final` at implementation acceptance.\n\n## Blocking questions\n\nNone. Private state layout, focused inspection spelling, and the exact pre-stability canonical Toggle variant are bounded deterministic implementation choices, not missing fundamental semantics or frozen compatibility blockers.\n\n## Readiness\n\nConfirmed as a bounded roadmap-item-25 implementation bead against the two reviewed contracts. No product or design blocker prevents progress.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-28T14:16:06.013290Z","created_by":"mikkofinell","updated_at":"2026-07-28T14:59:31.558903Z","closed_at":"2026-07-28T14:59:31.558859Z","close_reason":"Implemented stateful reaction foundation with Toggle, synchronized fingerprint authority and reviewed contracts, and passed final acceptance.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["roadmap","runtime","stateful"],"dependencies":[{"issue_id":"ms-v7j","depends_on_id":"ms-0ha","type":"blocks","created_at":"2026-07-28T14:16:06.013290Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-vbt","title":"Implement restricted semantic fingerprints and input-schema identity","description":"## Objective\n\nImplement Premium Foundation Roadmap item 12 for the restricted level-only Constant/Not model: make caller-owned time-domain identity intrinsic to authored networks, compute exact version-1 network and input-schema fingerprints after successful validation, and expose those identities before dense compilation and input artifacts.\n\n## Contract basis\n\n- `mossignal.graph.authored_network_definition`\n  - `docs/specs/contracts/authored-network-definition.yaml`\n  - status: reviewed, used unchanged\n  - role: stable-keyed restricted authored structure and the exclusion of diagnostic metadata from semantics.\n\n- `mossignal.graph.current_reaction_cycle_validation`\n  - `docs/specs/contracts/current-reaction-cycle-validation.yaml`\n  - status: reviewed, used unchanged\n  - role: successful restricted validation and the opaque ValidatedNetwork lifecycle boundary.\n\n- `mossignal.identity.time_domain`\n  - `docs/specs/contracts/time-domain-identity.yaml`\n  - status: reviewed\n  - role: caller-owned identity, authored-stage binding, lifecycle retention, persistence verification, and fingerprint contribution.\n\n- `mossignal.identity.network_fingerprint`\n  - `docs/specs/contracts/network-fingerprint.yaml`\n  - status: reviewed\n  - role: public opaque value, exact restricted v1 projection, BLAKE3 domain separation, exclusions, ownership, and verification.\n\n- `mossignal.identity.input_schema_fingerprint`\n  - `docs/specs/contracts/input-schema-fingerprint.yaml`\n  - status: reviewed\n  - role: exact restricted v1 external-input-schema identity, separate network binding, ownership, and verification.\n\n## Included scope\n\n- Introduce public opaque `TimeDomainId`, `NetworkFingerprint`, and `InputSchemaFingerprint` values for the restricted model.\n- Bind one caller-supplied `TimeDomainId` to `UncheckedNetwork<D>` construction and carry it unchanged through successful validation.\n- Compute and expose both fingerprints on `ValidatedNetwork<D>`; preserve them for item 13 to carry unchanged into `CompiledNetwork<D>`.\n- Implement only the narrow canonical CBOR value encoder needed for the two exact version-1 digest projections.\n- Encode the frozen restricted network projection covering version 1 core and built-in semantics, network and time-domain identity, Constant/Not nodes, stable typed ports and roles, connections, external endpoints, and Constant values.\n- Encode the frozen restricted input-schema projection covering canonically ordered external level inputs with required establishment semantics.\n- Compute domain-separated unkeyed BLAKE3-256 values with the exact network and input-schema labels.\n- Provide stable lowercase hexadecimal formatting and the specified ordinary value traits.\n- Add golden-vector, semantic-sensitivity, domain-separation, exclusion, and permutation tests.\n\n## Exclusions and non-goals\n\n- Do not implement `CompiledNetwork`, dense indices, evaluator descriptors, machine state, `RuntimePolicy`, `InputSnapshot`, `InputDelta`, topology revisions, patches, snapshots, replay, state digests, or complete persistence artifacts.\n- Do not implement artifact prefixes, envelopes, encoding APIs, decoding, schema upgrades, compatibility tables, or persistence restoration.\n- Do not add modules, hierarchy, stateful or temporal nodes, pulse execution, or unsupported node families to the restricted projections.\n- Do not fingerprint unchecked or blocking-invalid authored definitions as validated semantic networks.\n- Do not derive identity from Rust type identity, debug formatting, discriminants, allocation order, hash iteration, diagnostic metadata, topological order, reaction-operation identity, SCC data, caches, or private representation.\n- Do not introduce a late validation or compilation identity context or a new public bound-network lifecycle stage.\n- Do not include `TopologyPatchSemanticsVersion` in the restricted network projection.\n\n## Implementation obligations\n\n- Respect every represented requirement of the three identity contracts after independent review.\n- Preserve exact 16-byte `TimeDomainId` and distinct non-interchangeable 32-byte fingerprint value types.\n- Require caller-owned time-domain identity before validation; `validate()` remains context-free and successful validation fixes one intrinsic semantic identity.\n- Use domain version 1, `CoreSemanticsVersion(1)`, and `BuiltInNodeSemanticsVersion(1)`.\n- Encode the exact canonical record field names, nesting, closed variant names, stable-key bytes, collection orders, and digest wrappers specified for both restricted projections.\n- Use `mossignal/network_fingerprint/v1` and `mossignal/input_schema_fingerprint/v1` with unkeyed BLAKE3-256.\n- Keep `InputSchemaFingerprint` independent of network identity and unrelated topology while network-bound artifacts later carry both identities separately.\n- Ensure no public construction path can expose validated fingerprints for a blocking-invalid network.\n- Leave item 13 able to carry both fingerprints and `TimeDomainId` unchanged without recomputation from dense layout.\n\n## Allowed implementation freedom\n\n- Exact `NetworkBuilder` and `UncheckedNetwork` constructor spelling and argument grouping remain free so long as identity is caller-supplied before validation.\n- Private projection structs, encoder decomposition, caching, helper names, and module layout are free when the authoritative canonical bytes remain exact.\n- The narrow canonical encoder may be private and need not anticipate complete persistence encoding or decoding.\n\n## Expected change surface\n\n- a focused identity or fingerprint module under `crates/mossignal/src/`\n- `crates/mossignal/src/authored.rs` for authored `TimeDomainId` ownership and access\n- `crates/mossignal/src/validation.rs` for fingerprint construction only after successful validation\n- `crates/mossignal/src/lib.rs` for deliberate public identity exports\n- `crates/mossignal/Cargo.toml` and the workspace lockfile for BLAKE3 and any minimal canonical-encoding support\n- focused unit tests and versioned golden vectors\n- the three identity contracts only for implementation-discovered reusable facts; any implementation-time change remains draft\n- `.beads/` when implementation acceptance closes the bead\n\n## Required verification\n\n1. Equal stable semantic networks with permuted node, port, connection, and external-endpoint insertion order produce identical canonical bytes and fingerprints.\n2. Metadata-only changes leave both fingerprints unchanged.\n3. Changing only `TimeDomainId` changes `NetworkFingerprint`.\n4. Changing only `NetworkKey` changes `NetworkFingerprint`.\n5. Changing a Constant value changes `NetworkFingerprint`.\n6. Changing connection incidence changes `NetworkFingerprint`.\n7. Changing topological or private representation leaves both fingerprints unchanged.\n8. Other topology changes that preserve the complete external-input schema preserve `InputSchemaFingerprint`.\n9. Adding or removing an external input changes `InputSchemaFingerprint`.\n10. Equal canonical payload bytes under the network and input-schema domains produce different digests.\n11. Hand-reviewed golden canonical input bytes and expected BLAKE3-256 results cover a minimal network and a nontrivial Constant/Not network.\n12. Blocking-invalid definitions cannot publish validated fingerprints.\n13. `make check-dev`, then `make check-final` at implementation acceptance.\n\n## Dependencies\n\n- Roadmap item 11 / `ms-f2d` is complete and accepted.\n- Roadmap item 13 depends on this bead producing intrinsic validated semantic identity for `CompiledNetwork`.\n- Roadmap items 15 and 19 depend on the input-schema identity established here.\n\n## Readiness\n\nReviewed and ready for implementation. The item-12 planning decisions are represented in authoritative specifications and reviewed contracts; no unresolved observable product question or later-roadmap dependency blocks this bounded slice.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-07-27T16:42:12.592789Z","created_by":"mikkofinell","updated_at":"2026-07-27T18:41:59.637918Z","closed_at":"2026-07-27T18:41:59.637872Z","close_reason":"Implemented restricted semantic fingerprints and input-schema identity","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"ms-vbt","depends_on_id":"ms-f2d","type":"blocks","created_at":"2026-07-27T16:42:19.131609Z","created_by":"mikkofinell","metadata":"{}","thread_id":""}]}
{"id":"ms-xgn","title":"Add application bindings and bound execution façade","description":"## Objective\n\nImplement Premium Continuation Roadmap item 40 as the first complete application-binding adapter: map caller-owned input and output identifiers to one compiled network's typed stable endpoints, project caller observations into ordinary snapshots and deltas, project outputs back to caller identifiers, and provide a thin bound-machine façade without changing machine semantics.\n\n## Roadmap provenance\n\n- Document: `docs/premium_continuation_roadmap.md`\n- Item: 40\n- Title: Application bindings and bound execution façade\n\nThis provenance records sequencing only. The referenced contracts and authoritative specifications govern product semantics.\n\n## Contract basis\n\n### Reviewed feature contract\n\n- `mossignal.binding.application_adapter`\n  - `docs/specs/contracts/application-bindings.yaml`\n  - status: reviewed\n  - role: governs caller-owned non-semantic identifiers, compiled-schema binding, bidirectional mapping, input and output projection, binding failures, and bound-machine equivalence.\n\n### Reviewed contracts reused without modification\n\n- `mossignal.compile.compiled_network` (`docs/specs/contracts/compiled-network.yaml`): immutable compiled topology, retained stable network/input-schema identity, graph access, and machine/input-builder entry points. Only unchanged identity, topology, and opaque-dense facets are reused; changed validation-owner source facets are outside this task.\n- `mossignal.runtime.level_input_snapshot` (`docs/specs/contracts/level-input-snapshot.yaml`) and `mossignal.runtime.level_input_delta` (`docs/specs/contracts/level-input-delta.yaml`): exact-bound owned input artifacts, complete level snapshots, delta omission/retention, typed observations, and structured input failures. Only unchanged represented facets apply; prepared-patch targets remain outside this task.\n- `mossignal.runtime.reaction_scoped_pulse_foundation` (`docs/specs/contracts/reaction-scoped-pulse-foundation.yaml`): typed pulse observations, checked multiplicity, reaction-scoped publication, and absence of persistent current pulse values. Only unchanged non-fingerprint facets apply.\n- `mossignal.runtime.machine_lifecycle` (`docs/specs/contracts/machine-lifecycle.yaml`), `mossignal.runtime.initialization_transaction` (`docs/specs/contracts/initialization-transaction.yaml`), and `mossignal.runtime.ready_level_transaction` (`docs/specs/contracts/ready-level-transaction.yaml`): canonical machine ownership, lifecycle, transaction, scheduling, provenance, result, and failure-atomicity semantics that the façade must preserve.\n- `mossignal.diagnostics.problem_model` (`docs/specs/contracts/diagnostic-problem-model.yaml`) and `mossignal.diagnostics.subject_references` (`docs/specs/contracts/diagnostic-subject-references.yaml`): catalogue-backed failure conditions, typed evidence, and binding-slot subject identity. The changed authoritative binding-subject section was rechecked during preparation; canonical ordering for later SubjectRef variants remains known-uncovered and is not required by this leaf-failure slice.\n\n## Included scope\n\n- Add a public binding module containing immutable `BindingSet<I, O>`, its compiled-network-borrowing builder, `InputProjector<D, I>`, projected output values, and a minimal `BoundMachine<D, I, O>` façade.\n- Bind caller-owned input and output identifiers to typed external endpoints from exactly one `CompiledNetwork<D>` while retaining network, topology-revision, and input-schema compatibility context.\n- Provide deterministic endpoint-to-caller and caller-to-endpoint lookup for Level and Pulse inputs and outputs without exposing private compiled positions.\n- Reject unknown endpoints, wrong-kind paths, duplicate endpoints, duplicate or ambiguous caller identifiers, incomplete requested projections, wrong networks, and stale topology revisions or schemas through structured binding failures and the applicable catalogue codes/evidence.\n- Project caller-keyed Level and Pulse observations into ordinary `InputSnapshot<D>` and `InputDelta<D>` values by delegating through the compiled network's existing builders.\n- Project every ordinary LevelEstablished, LevelChanged, and Pulsed output event to its caller output identifier while retaining exact values/counts, time, cause, revision, and order.\n- Expose ready current Level output observation by caller identifier through the existing machine baseline; do not invent current Pulse state.\n- Provide a small bound-machine façade that owns or wraps one compatible ordinary machine and binding set, supports initialization and ready advancement from caller observations, and returns projected outputs alongside access to the unchanged ordinary result/provenance.\n- Add focused public-surface, exact-projection, diagnostics, determinism, and direct-versus-bound execution equivalence tests.\n\n## Exclusions and non-goals\n\n- No callbacks, observer subscriptions, delivery cursors, acknowledgement state, async runtime integration, or event-handler registry.\n- No domain-specific entity model, payload transport, arbitrary signal values, or caller object-model interpretation.\n- No replacement or deprecation of structural endpoint-key APIs, ordinary input builders, transactions, or `Machine<D>`.\n- No module-instance binding changes, authored graph semantics, new evaluator path, new node kind, runtime policy change, or provenance rewriting.\n- No persistence or codecs for caller identifiers/binding sets, binding participation in snapshots or replay frames, or fingerprint/schema changes.\n- No topology patch implementation, automatic rebinding, migration, stale-projector refresh, or compatibility shim for earlier repository revisions.\n- No general bound inspection/explanation/state-change façade beyond current external Level outputs and projected output events.\n\n## Implementation obligations\n\n- Implement the `mossignal.binding.application_adapter` requirements `caller-owned-nonsemantic-identifiers`, `immutable-compiled-schema-adapter`, `unambiguous-bidirectional-mapping`, `binding-construction-failures`, `exact-input-projection`, `lossless-output-projection`, `bindings-outside-machine-semantics`, and `bound-machine-is-equivalent-delegation`.\n- Build endpoint inventories from stable public graph/schema information or the smallest new read-only compiled accessor; never expose or bind through dense execution indices.\n- Preserve signal kind in every mapping. Caller identifiers remain opaque equality/hash keys and never determine semantics, ordering, fingerprints, diagnostics identity, or evaluator behavior.\n- Make safe completed mapping/projection state immutable. Validate all inputs needed by a multi-binding or projection operation before publishing a completed artifact; failed operations must not partially mutate an existing immutable artifact or machine.\n- Reuse `CompiledNetwork::input_snapshot` and `CompiledNetwork::input_delta` as the canonical artifact builders so projected artifacts retain exact network and input-schema identity and all existing Level/Pulse boundary laws.\n- Keep output projection lossless except for substituting the uniquely bound caller output identifier. Preserve the ordinary `TransactionResult` and its `ProvenanceView`; do not copy, normalize, or fabricate causes.\n- Ensure `BoundMachine` delegates to `Machine::apply`, preserves the ordinary result and failure, and exposes access to the underlying semantic machine/bindings sufficient to demonstrate that scheduling, policy, lifecycle, revision, digests where implemented, and failure atomicity are unchanged.\n- Integrate binding conditions into the existing structured failure/diagnostic model without requiring caller identifiers to become `SubjectRef` identity or rendered strings.\n- Keep production code free of `unwrap` and `expect`.\n\n## Allowed implementation freedom\n\n- Choose the smallest deterministic map/set representation and iteration order. Record and test the concrete order without making caller identifier ordering semantic or requiring `Ord`.\n- Choose whether `BindingSetBuilder::finish` requires all bindings or permits a partial immutable set whose requested projector/façade rejects missing mappings; every successful requested projection must be complete and unambiguous.\n- Choose concise owned caller-observation builder types or direct methods for snapshot/delta projection. The exact method grouping, borrowing, iterator types, and projected output wrapper names are free if the typed responsibilities and failure distinctions remain clear.\n- Choose whether the minimal `BoundMachine` owns or is constructed from an ordinary machine, and whether projection helpers return paired ordinary/projected results or a wrapper with accessors. Favor the simplest design that preserves direct access to canonical semantic artifacts and proves equivalence.\n- Where safe typed APIs make an ambiguity unrepresentable, retain the catalogue condition in the failure/code mapping and test the preventing invariant rather than adding a malformed public constructor solely to emit it.\n\n## Expected change surface\n\n- A focused public binding module under `crates/mossignal/src/`, plus deliberate exports from `crates/mossignal/src/lib.rs`.\n- Narrow read-only external-endpoint/schema access in `crates/mossignal/src/validation.rs`, `crates/mossignal/src/compile.rs`, or an existing graph view if required.\n- `crates/mossignal/src/diagnostics.rs` only for the applicable binding codes, evidence, subjects, and leaf failure integration not already present.\n- Focused integration tests such as `crates/mossignal/tests/application_bindings.rs` and only narrowly required shared test support.\n- `docs/premium_continuation_roadmap.md` for the item-40 progress/completion heading.\n- `.beads` only when implementation is later closed and synchronized.\n\n## Required verification\n\n1. Bind mixed Level/Pulse inputs and outputs in permuted insertion orders and prove the same immutable bidirectional endpoint mappings without requiring caller-key ordering.\n2. Exercise unknown endpoint, wrong signal kind where dynamically reachable, duplicate endpoint, duplicate caller identifier, missing required mapping, wrong network, and stale topology revision or schema failures; structurally verify applicable codes, subjects, evidence, and absence of ambiguous suggestions.\n3. Build complete snapshots and partial deltas from caller identifiers, including Level completeness/retention, absent and nonzero Pulse observations, duplicate/conflicting/unknown caller observations, and exact underlying network/input-schema identities.\n4. Compare projector-produced snapshots/deltas with direct structural-key artifacts for the same observations and verify identical ordinary transaction behavior.\n5. Project LevelEstablished, LevelChanged, and Pulsed events and assert exact caller output, value/count, time, cause, revision, and flat ordering while the retained cause resolves through the unchanged ordinary provenance view.\n6. Verify ready current Level output lookup in both endpoint and caller directions, structured absence before initialization or for unknown/unbound keys, and no persistent current Pulse observation.\n7. Run equivalent initialization and later transactions through direct `Machine` and `BoundMachine` paths and compare lifecycle, time, revision, output stream, settled outputs, schedule, provenance, runtime policy, and available state/observable digests after only caller-key normalization.\n8. Inject projection, compatibility, runtime, and budget failures and prove both direct and bound paths return equivalent structured failures and preserve the complete machine unchanged.\n9. Prove bindings do not change authored/compiled structure, NetworkFingerprint, InputSchemaFingerprint, machine/replay identity, scheduling, evaluator operation, provenance subjects, or diagnostics unrelated to binding validation.\n10. Run focused Rust tests while iterating, then `make check-dev`; final acceptance additionally runs `make check-final`.\n\n## Blocking questions\n\nNone. The specifications fix the adapter boundary, identity separation, failure conditions, and semantic-equivalence requirement. Storage, completeness timing, caller-observation API shape, projected wrapper shape, and bound-machine ownership are bounded implementation freedoms with deterministic testable choices.\n\n## Readiness\n\nReady as a bounded implementation task. The `mossignal.binding.application_adapter` contract is independently reviewed, and no product-semantics blocker remains.","status":"closed","priority":1,"issue_type":"feature","assignee":"mikkofinell","created_at":"2026-07-30T06:27:58.039096Z","created_by":"mikkofinell","updated_at":"2026-07-30T06:53:12.166982Z","closed_at":"2026-07-30T06:53:12.166917Z","close_reason":"Implemented application bindings, canonical input/output projection, structured binding failures, and equivalent bound-machine delegation; final acceptance passed.","source_repo":"mossignal","source_repo_path":"/Users/mikkofinell/code/mossignal","compaction_level":0,"original_size":0,"labels":["binding","roadmap","runtime"]}
