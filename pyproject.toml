[project]
name = "mossignal-agent-tools"
version = "0.0.0"
description = "Repository-local Python tooling for mossignal."
requires-python = ">=3.14,<3.15"
dependencies = [
    "PyYAML>=6,<7",
]

[tool.uv]
package = false
