interface:
  display_name: "Contract Task Prep"
  short_description: "Research specification contracts and prepare an implementation bead"
  default_prompt: "Use $contract-task-prep to reuse reviewed contracts, research uncovered specification facets, and prepare a bounded implementation-ready bead without blocking on ordinary design freedom."

policy:
  allow_implicit_invocation: true
